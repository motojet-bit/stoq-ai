# 作業履歴アーカイブ（〜 Phase 2 Step 5.1）

> `PROGRESS.md` が 300 行を超えたため繰り下げたもの。
> 最新の履歴は `PROGRESS.md` を参照。

---

### 2026-08-03 — 第74章: コンテキスト分離 ＋ 進捗固定 ＋ スコア演出 ✅
- **採点段に直前の出力を渡すのをやめた。** 前回の分割では各段に前段の本文を
  渡していたため、段を追うごとにプロンプトが積み上がって切断されていた
- 各段は資料と財務データだけを見て独立に採点。通しの体裁は最後にフロントで組む
- **最終段だけ評価テーブルの「行」を渡す**（矛盾の指摘には必要。節は落とす）
- 進捗と消費トークンをスクロール領域の外へ出して固定
- 平均スコアは**生成中は出さず**、完了後にカウントアップで確定
  （途中の平均は未採点を除いた値でしかなく、確定値と取り違える）
- 決算期は本文で読めなければ**ファイル名**から（判定元も画面に出す）
- 「本決算／アドホック」の選択を常時表示（見通しを実績と混ぜない）
- エラーの生ログをモーダルでコピー可能に
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 873 件 / Rust 259 件）

### 2026-08-03 — 第73章: 分割直列実行 ＋ 使用量・診断・進捗 ✅
- 20 項目を 4 段に分けて直列実行し、段ごとに DB へチェックポイント保存。
  **一度に書かせると出力上限で切れ、保存も走らず「何も残らない」状態になっていた**
- 再開は先頭から連続している段だけを土台にする（歯抜けは信用しない）
- 中断された段は確定させない（欠けた表を次回の土台にしない）
- 資料なしのときは「最新期／過去の期」を選ばせ、指定期の SEC 書類を取得。
  **見つからなければ止める**（別の期の中身がその期の分析として保存される）
- SSE から usage を拾ってコストを概算（「消費: 12,400 tkn / 約 ¥5.2」）。
  **取れなければ 0 のまま**・単価不明のモデルは金額を出さない
- 429 / 401 / 残高不足 などを切り分け、**直るものだけ再試行を勧める**
- 5 段階の進捗メーター。終わった段は緑で確定＝保存済みを示す
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 854 件 / Rust 259 件）

### 2026-08-03 — 第72章: 決算期の承認 ＋ SEC 自動取得 ＋ 入れ子ツリー ✅
- 資料つきの分析は**決算期を確認してから**進む。判別できなかったときも聞く
  （黙って期なしで進めると、あとから期と突き合わせられない）
- 確定した期はメモリのみ。永続化すると資料を入れ替えても古い確定が効く
- 添付 0 件なら SEC の 10-Q（無ければ 10-K）を自動取得。
  **自動取得した旨をトースト・根拠欄・プロンプトの 3 か所に出す**
  （渡していない資料が根拠に入るため。プロンプトにも書かないと
  「添付いただいた資料」と書かれる）
- `analysis_history` に `parent_id` / `branch_no` を追加（ALTER TABLE）。
  期中の開示を `Q2-01` 形式で四半期の下へ
- **親を消しても子は残す。** 親を失った子は根として出す
- **枝番は保存時に採番。** 表示のたびに数え直すと、削除で既存の番号がずれる
- アドホックは `analyses`（最新 1 件）を上書きしない
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 806 件 / Rust 255 件）

### 2026-08-03 — 第69・71章: 参照付き根拠 ＋ 決算期の自動特定と YF 連動 ✅
- 秘匿プロンプトに「数値の出所」節を新設。添付資料の数値には
  `【参照: p.XX "原文"】` を必ず添えさせる
- **「参照を付けられない数値は、書いてはいけない数値」**という言い方に統一。
  「推測で断定しない」だけでは、それらしい値を置く余地が残る
- 出力に「参照した原文」節を追加。`citations.ts` で参照を機械的に抽出できる
  （**確かめられなければ、守られていない出力に気づけない**）
- `fiscalPeriod.ts` で決算期を自動特定（英日・全角・通期に対応）。
  **先頭 4000 字だけを見る**（本文の前年同期に引っ張られないため）
- **判別できなければ null**。推測で埋めると別の期のデータと突き合わせた分析が出る
- `periodLink.ts` で Yahoo の四半期に突き合わせ、当時の値を表でプロンプトへ載せ、
  資料の数値との食い違いを指摘させる
- **当たらなくてもエラーにしない。** 取得できなかった旨を明記して PDF 単体で続ける
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 783 件 / Rust 255 件）
- **未了**: 参照の抽出結果を画面に出す導線（バッジ等）は次の区切りで

### 2026-08-03 — 2分割 AIクロスディベート（Stage 2）✅
- 設定に「ディベート」タブ。**メイン分析とは別のプロバイダ／モデル**を選べる
- 未設定ならメインへ落とす。ただし同じになる場合は画面で注意する
  （同じモデルでは同じ思い込みがそのまま残る）
- 分析画面を左右 2 分割（🔥 で開閉、既定は閉）。
  **閉じているときは分割そのものを作らない**（幅ゼロのペインは掴めるのに動かない）
- 「🔥 1ターンディベート実行」は **批判 → 反論 の 1 往復で打ち止め**。
  自動で次を始める経路が無く、走行中の再実行も拒否する
- 反論はメイン分析と同じプロバイダが答える（元の分析を書いた本人が答える構図）
- ディベートのプロンプトは Rust の `prompts/debate_*.md`。フロントへは返さない

### 2026-08-03 — 設定モーダル ＋ SEC UA 補完 ＋ 同期ガイド ＋ 履歴削除 ✅
- タブ名を折り返さず横スクロールへ。**単語の途中で切れるとどのタブか読めない**
- モーダルを 90vw × 85vh に収め、中身は縦横スクロール。
  **`max-w` を二重に指定せず、覆いの余白を vw/vh で取った**
  （二重指定はどちらが勝つか CSS の並び順まかせになる）
- SEC の User-Agent はメールだけなら `StoQ-App <mail>` を補う。
  名前入りはそのまま通す（重ねると二重になる）
- クラウド同期に折り畳みガイド（`CloudSyncGuide.tsx`、既定は閉）
- 分析履歴に削除を追加。**開く操作とは別ボタン**にし、確認ダイアログを挟む。
  画面からは先に消して、失敗したら戻す
- 直書き検出テストが `{式} 日本語` を素通りしていたのを修正 →
  出てきた 11 ファイル・21 件を辞書へ（辞書 948 キー）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 728 件 / Rust 246 件）
- **要確認**: 同期ガイドの文面が実装（OAuth・フォルダ選択なし）と合っていない

### 2026-08-02 — LP: ローソク足の背景 ＋ トライアル条件カード ✅
- `lp/hero.svg` を新規作成（100 本のローソク足のみ、緩やかな上昇 → 暴騰 → 急落）
- **移動平均線・トレンドラインは入れない。**
  線を引くと「そのラインで売買せよ」という含みが出て、投資助言に寄って見える
- 急落は上昇局面の帯で止める。始値を割ると縦の縮尺が急落に食われ、前半が潰れる
- data URI をやめて外部ファイル参照へ。ぼかしを外し、読みやすさは
  `rgba(13, 17, 23, 0.85)` のオーバーレイ側で確保する
- ダウンロードの**直上**に無料トライアルの条件カードを追加
  （10 銘柄 / 3 週間 → ロック → キーで永続解錠）。
  **解錠後まで同じ枠に書く**。制限だけだと試用版の押し売りに見える
- **公開**: `git subtree push --prefix lp origin gh-pages` ✅（`1b9aa5a` → `ec36cc1`）
- **検証**: `lp/` のみの変更。アプリ側は同セッションの
  `npm run verify` ✅ / `cargo test --lib` ✅（フロント 726 件 / Rust 241 件）から変化なし

### 2026-08-02 — LP: Core Value ＋ V0 特別価格 ＋ 価格シナリオ検討ページ ✅
- 比較カードを `flex h-full flex-col` ＋ 本文 `flex-1` にして左右の高さ・行位置を統一。
  **見出しの行数が違うと本文とポイント枠が互い違いにずれる**ため、見出しに min-h も確保
- ヒーロー直後に Core Value を新設（自動取得 / 網羅性 / 1 画面集約）。
  **「何ができるか」より先に「何をしなくてよくなるか」**を見せる並び
- 価格を「$228 打ち消し → $62」＋ V0 バッジ＋「V1 で予告なく通常価格へ」の注釈
- 購入前同意に 5 項目目（メジャーアップデートの有償化）を追加。
  **無料の範囲を同じ項目内に併記**しないと、以後の修正まで課金されると受け取られる
- `lp/pricing-scenarios.html` を新規作成。第 2 段階（$98 / $28 月）・
  第 3 段階（$228 / $49 月）を損益分岐つきで比較。
  **noindex ＋ index からリンクしない**（検討中の価格が実売価格と取り違えられるのを防ぐ）
- **公開**: `git subtree push --prefix lp origin gh-pages` ✅（`e293683` → `a522228`）
- **検証**: `lp/` のみの変更。アプリ側は同セッションの
  `npm run verify` ✅ / `cargo test --lib` ✅（フロント 726 件 / Rust 241 件）から変化なし

### 2026-08-02 — LP: BYOK 解説 ＋ 購入前の同意モーダル ✅
- API・外部データの解説を**価格の手前**に新設
  （買ったあとで別途費用を知るのがいちばん不満の残る順番）
- 「Auto-recharge を OFF にすれば予期せぬ課金は起きない」を同じ場所に明記
- 購入ボタンから決済へ直行させず、4 項目（投資助言ではない / API 費用は別 /
  個別サポート無し / 対応言語は 2 つ）の同意を取る
- 全項目にチェックが入るまで進めない。開き直したらチェックはリセット
- **`href` は残して `preventDefault`**。JS が動かない環境では決済へ直行する
- **公開**: `git subtree push --prefix lp origin gh-pages` ✅（`81729f3` → `e293683`）
- **検証**: `lp/` のみの変更。アプリ側は同セッションの
  `npm run verify` ✅ / `cargo test --lib` ✅（フロント 726 件 / Rust 241 件）から変化なし

### 2026-08-02 — LP の文面刷新（対象読者 ＋ SmartScreen ＋ 改行）✅
- 「Persona / ペルソナ」の語を全廃。**アンカー ID も `#audience` へ**
  （ID はアドレスバーに出るので、見出しだけ直しても語が残る）
- カードを 左＝脱・初級者 / 右＝中級者以上 の順へ。リード文と読む順を揃えた
- ダウンロード欄に SmartScreen の回避手順（詳細情報 → 実行）を追記。
  **署名が無い以上必ず出る画面**なので、先に言わないと離脱要因になる
- キャッチコピーを 3 行に明示改行（日本語の自動折り返しは読点を無視するため）
- **公開**: `git subtree push --prefix lp origin gh-pages` ✅（`e365563` → `81729f3`）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 726 件 / Rust 241 件）

### 2026-08-02 — 66章 LP ブラッシュアップ ＋ 67章 残り日本語の辞書化 ✅
- アプリ内の直書き日本語を再走査して辞書へ（ja/en 各 916 キー）
- **初版の走査は辞書の値と一致する文字列を除外していて 52 件取りこぼしていた。**
  「保存」のような短い語は辞書にも直書きにも同じ形で出る
- LP: 価格 $62 に統一 ＋ 差額アップグレードの注記、背景画像（SVG データ URI）、
  言語切替を 🇯🇵 / 🇺🇸 トグルへ

### 2026-08-02 — 販売 LP の作成と GitHub Pages 公開 ✅
- `lp/index.html` の 1 枚もの。**外部 CSS / JS を読み込まない**（審査で壊れて見えないように）
- 公開は専用 `gh-pages` ブランチ。**`/docs` 公開にしなかったのは、
  `docs/` に署名鍵の置き場所を含む内部メモがあるため**
- 公開リポジトリに実アドレスを残さないよう、過去 80 コミットの著者を noreply へ書き換え

### 2026-08-02 — GitHub Releases を配信元とした自動更新 ✅
- `tauri-plugin-updater` ＋ `createUpdaterArtifacts` で署名付き成果物
- **`latest.json` は Tauri が作らない。** `npm run release:manifest` で組み立てる
- 署名鍵はリポジトリ外（`~/.stoq-updater/stoq.key`）。**失うと更新を届けられなくなる**
- ビルド時は `TAURI_SIGNING_PRIVATE_KEY` に**鍵の中身**を渡す
  （`..._PATH` だけだと署名が黙って飛ばされ、`.sig` の無い成果物ができてしまう）

### 2026-08-02 — i18n A〜F グループ（既定を英語へ反転）✅
- `DEFAULT_LOCALE = "en"`。**未訳のときに日本語へ落ちない**
- **`export const X = t("...")` は禁止**（読み込み時に値が焼き付き、切替に追従しない）
- C グループは**内部キーと表示ラベルを分離**。`criterion.id` などは保存データの鍵なので訳さない。
  Yahoo の指標引き当て文字列も日本語のまま
- F グループで `AppError::msg` を全廃し `Coded{code, detail}` へ（41 コード / 197 か所）。
  **文面を Rust に持たせない**ので、翻訳がフロント側だけで完結する
- 直書き検出テスト `localized.test.ts` を新設（許可リストが古びていないかも検査する）
- **注意**: 言語を切り替えるテストは `setLocale("ja")` を**トップレベル**に置く。
  `beforeAll` ではモジュール読み込み時の定数生成に間に合わない

### 2026-08-02 — 3週間・10銘柄トライアル ＋ カスタム指示 ＋ APIキーガイド ✅
- 体験期間の**起点は初回起動日**。`settings::load` の時点で焼き付ける
- **未来の記録は「いま」に直す**（時計を進めて期限を先送りさせない）
- 銘柄上限 3 → 10。**期限切れを枠数より先に見る**
- **期限切れでも既存銘柄の再分析・過去データの閲覧は止めない**
- 分析ボタンは `aria-disabled`。本物の `disabled` だと押下を拾えず、
  指示にある購入ダイアログを出せない
- ライセンス有効化で触るのは `license_key` のみ（DB・使用済み銘柄はそのまま）
- カスタム指示は**出力フォーマットの後ろ**に結合（2000 文字で切り詰め）
- APIキーガイドは辞書化し、**キー未設定時はそのタブを初期表示**
- テスト 60 件追加
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 577 件 / Rust 235 件）
- **ビルド**: `npm run tauri build` ✅

### 2026-08-02 — アプリ名「株究」の多言語切替 ＋ 免責同意（EULA）ブロック ✅
- **名称の出どころを定数から辞書へ移した。** 言語で変わる以上、定数では持てない
  - ja: 「株究 - StoQ AI Analyzer」 / en: 「StoQ AI Analyzer」
- **読み込み時に一度だけ組み立てていた文字列を関数化した。**
  定数のままだと、言語を切り替えても免責文やロゴが古い名前を名乗り続ける
- ウィンドウタイトルは Rust の `window_set_title` 経由で差し替える
- OS 言語判定は `detectLocale`。**日本語環境だけ ja、それ以外は en**
- **初回起動時の免責同意は閉じる手段を持たない**（Esc・✕・背景クリックのいずれも無効）
- **状態が分からないうちもブロックする。** 読み込み中に素通りさせると、
  未同意のまま一瞬でも操作できてしまう
- 時刻の無い同意フラグは信用しない（設定ファイルの手書きで素通りさせない）
- 設定「一般」タブに同意内容の確認と撤回。
  **撤回してもライセンスキーには触れない**（買ったものを取り上げる理由にはならない）
- テスト 36 件追加（名称の多言語 17 / EULA 19）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 535 件 / Rust 214 件）
- **ビルド**: `npm run tauri build` ✅（exe 6.7MB / msi 4.0MB / setup 3.0MB）
- **制約**: インストーラー名は `StoQ AI Analyzer`。日本語を入れると
  WiX の MSI 生成が `LGHT0311`（コードページ 1252）で失敗するため

### 2026-08-02 — 提携導線の撤去 ＋ 表記の一元化 ＋ テスト追加 ✅
- **アフィリエイト構想を全面破棄。** 無料版上限モーダルから
  「🎁 無料でライセンスを取得（IBKR等）」を削除し、`onOpenGuide` の
  受け口ごと閉じた（残すと呼び出し側で復活するため）
- `src/` 全ファイルを走査して提携文言が 1 つも無いことをテストで固定
- **アプリ名は `appMeta.ts` の 1 行が唯一の出どころ**になるよう書き換え。
  ロゴ・免責事項・ヘルプ AI・ツアー・メール件名すべてが `APP_NAME` を参照する
- 権利表記のテストは文字列の直書きから**形の検証**へ変更（改名に耐える）
- テスト 40 件追加（提携導線 8 / 表記の一貫性 11 / クラウド同期ストア 21）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 499 件 / Rust 209 件）
- **ビルド**: `npm run tauri build` ✅（exe 6.7MB / msi 4.0MB / setup 3.0MB）
- **未了**: アプリ名・コピーライトの新文言は未受領のため現行のまま。
  受領後は `appMeta.ts` と辞書 2 か所の変更だけで全画面に反映される

### 2026-08-02 — Google Drive クラウドバックアップ／復元 ＋ マスターキー ✅
- OAuth 2.0 **PKCE**（クライアントシークレット無し）で Google と連携。
  配布バイナリに秘密を埋めても誰でも取り出せるため、PKCE を使う
- 要求するスコープは **`drive.appdata` ひとつだけ**。アプリ専用の隠し領域に限られ、
  ユーザーの写真・書類は一覧すらできない。**テストで固定**している
- `access_type=offline` ＋ `prompt=consent` を必ず付ける
  （無いとリフレッシュトークンが返らず毎回ログインになる）
- 受け口は **`127.0.0.1` にだけ**立て、ポートは OS に選ばせる。`state` 不一致は中止
- バックアップは 3 つの DB を**まとめて 1 つの束**にする。1 つだけ戻すと
  「ポートフォリオには居るのに分析結果が無い」という食い違いが起きる
- **`settings.json`（APIキー・ライセンスキー）は束に入れない**
- 復元先は既知の 3 ファイル名だけを通す（束は外から来たデータなので `../` を弾く）。
  全部読めてから書き始め、上書き前の現物を同じフォルダへ退避する
- バックアップ名は毎回変える。1 つを上書きし続けると、
  壊れたデータを同期した瞬間に戻せなくなる。世代は 10 件を残す
- 設定に「クラウド同期」タブを新設。**触れる範囲を画面上でも明示**
- `license.rs` に開発者用マスターキー `STOQ-DEV-MASTER-2026` を追加
- テスト 84 件（Rust 66 / フロント 18）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 459 件 / Rust 209 件）
- **未了**: Google Cloud の OAuth クライアント ID はユーザー側で発行し、
  設定画面から入力する必要がある（こちらでは作成できないため）

### 2026-08-02 — フリーミアム（無料 3 銘柄まで）と制限ダイアログ ✅
- **「銘柄数」で数える。** 実行回数だと、同じ銘柄を決算のたびに見直す
  使い方が引っかかり、このアプリ本来の価値（期をまたいだ追跡）が損なわれる
- 未認証で 4 銘柄目を分析（データ取得も含む）しようとすると処理を止め、
  `🔒 無料版の分析上限（3銘柄）に達しました` の案内を出す
- **すでに使った 3 銘柄は再分析・対話・過去ログ閲覧が何度でもできる**
- ボタンは「ライセンスキーを入力」（設定のライセンスタブを直接開く）と
  「🎁 無料でライセンスを取得（IBKR等）」（ヘルプを開く）の 2 つ
- ライセンスを有効化すると**その場で無制限**になる（判定が `activated` を見るだけ）
- 使用済み銘柄は Rust の設定ファイルに保存。localStorage だと
  ブラウザデータの消去で制限が外れてしまう
- **上限を超えては積まない。** 制限中に何度も押したぶんまで記録すると、
  解除後に「使った覚えのない銘柄」が並ぶ
- テスト 32 件（純粋ロジック 21 / ストア 11）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 441 件 / Rust 143 件）

### 2026-08-02 — i18n 基盤（日本語 / English）＋ チャット添付の分離 ✅

1. **対話の添付をグローバル状態から分離**（不具合修正）
   - 📎 で読み込んだファイルが上部ドロップゾーンの「一時保存中の資料」に
     混ざっていたのを、**会話ごとの使い捨て**として別管理にした
   - `chatAttachments.ts` は抽出だけを行い、**Rust 側へ保存しない**
     （`documents_stage` を呼ばない）。上部の件数にも入らない
   - **混ぜると、1 度だけ見せたプレスリリースが以降すべての分析に混入する**
   - 送信すると破棄。会話を切り替えても持ち越さない
2. **i18n 基盤**
   - `src/locales/ja.json` / `en.json` と `lib/i18n/`（自前の軽量実装）
   - **言語を増やす手順は「JSON を置いて登録簿に 1 行足す」だけ**
   - 訳が無ければ日本語へ、それも無ければキーを返す（画面から文字が消えない）
   - `{{var}}` の差し込みに対応
3. **言語切替 UI ＆ AI 出力言語の連動**
   - 設定の「一般」タブ（旧「表示」）に「表示言語（Language）」を追加
   - **英語選択時は Rust 側でシステムプロンプトに指示を付与**
     （`Respond all analysis, block scores evaluation, and dialogue outputs in English.`）
   - 分析・対話・ヘルプのすべてに効く。**見出しは訳さないよう釘を刺している**
     （訳されるとパーサが結果を読めなくなるため）
4. **テスト 43 件**（i18n 22 / チャット添付 14 / 言語指示 7）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 409 件 / Rust 143 件）

### 2026-08-02 — ステータスバーへのコピーライト表記 ＋ 本番ビルド ✅
- 画面最下部の中央に `© 2026 StoQ AI Analyzer. All Rights Reserved.` を配置
  - **左・中央・右の 3 分割（`grid-cols-[1fr_auto_1fr]`）**にした。
    `mx-auto` で寄せるだけだと、左右の情報の長さ（銘柄名や件数）で中心がずれるため
  - 文字サイズはバー全体の `t-label` に揃え、中央だけ別指定はしない
  - 狭い画面では「読み込み済み資料」から先に隠し、中央の表記を潰さない
- 表記は `src/lib/ui/appMeta.ts` に集約（ステータスバー・About・レポートで
  年や名称がずれないように）。版は `package.json` / `tauri.conf.json` と一致
- 右端の版表記を「Phase 1 / v0.1.0」から `v0.1.0` に整理
- 表示テスト 9 件を追加（文面・3 分割・中央寄せ・版の一致）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 373 件 / Rust 136 件）
- `npm run tauri build` を実行

### 2026-08-02 — Phase 3 Step 10: 保存構造の標準化・エクスポート・ヒートマップ・ライセンス ✅

1. **分析データ保存構造の標準化**
   - `AnalysisRecord`（`fiscalQuarter` / `summary` / `blockScores` / `keyMetrics` /
     `evaluations` / `rawMarkdownOutput`）を定義し、`analyses` と
     `analysis_history` に `record` 列（JSON）として保存
   - 決算期は `FY26-Q3` 形式に統一。総合スコアは 100 点換算＋🟢🟡🔴
   - **壊れた JSON でも例外を投げない**（1 件の破損で一覧が消えないように）
2. **ワンクリック・エクスポート（CSV / MD / JSON）**
   - 分析結果パネルとマイポートフォリオに「📥 エクスポート」を配置
   - **どの形式も同じ `AnalysisRecord` から作る**ので数字が食い違わない
   - CSV は引用符エスケープ＋BOM 付き。列は全レコードの和集合で作る
   - Markdown には**免責を必ず付ける**（配布される可能性があるため）
   - 書き出し先はダウンロードフォルダ。同名があれば連番を付ける
3. **ヒートマップ → タイムライン**
   - マイポートフォリオの既定を「銘柄 × 四半期」のヒートマップに変更
   - 銘柄名／セルのクリックでタイムラインへ。各四半期カードを開くと
     ブロック別スコア・主要数値・強み/リスク・生成テキスト全文が読める
4. **全期の一括転送 ＆ 一次資料の添付**
   - 「📋 全期の分析データを対話へ転送」で過去全四半期を構造化テキストとして注入
   - 対話入力欄に「📎」を新設し、単発のプレスリリース等をその場で読み込める
5. **応答スタイルの最適化**: 「私は投資助言は行いません」等の定型的なお断りを
   出さず、プロのアナリストとして客観的分析と仮説を述べるよう秘匿プロンプトに追記
6. **ライセンス認証タブ**: キー入力＋有効化ボタン。形式チェックのみの骨組みで、
   キーはマスク済み文字列しかフロントへ返さない
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 364 件 / Rust 136 件）

### 2026-08-02 — Phase 3 Step 9: UI 不具合修正 ＋ 専有レイアウト ＋ 保存導線 ✅

1. **ドロップダウンの切れを解消**
   - `PortalMenu` を新設し、メニューを `document.body` 直下へ出す（`z-index: 9999`）。
     **親パネルの `overflow: hidden` に切られなくなった**
   - 位置は `menuPosition.ts` が計算（右端そろえ→はみ出すなら左端、
     下に入らなければ上へ、どちらにも入らなければ高さを制限）
   - 役割選択・AI設定・対話の役割の 3 つを移行
2. **ホバー被りの抑制と OFF 案内**
   - `overlayStore` でメニューの開閉を数え、**開いている間はツールチップを出さない**
   - 表示回数を数え、**5 回に 1 回**「💡 ツールチップは [設定] ➔ [表示] タブから
     オフにできます」を本文の後ろに添える
3. **モーダル背景のぼかしを撤廃**
   - `backdrop-blur` を全廃し、`bg-black/35` の薄い半透明に統一。
     **背後の株価や分析テキストが読めないと、何を消そうとしているのか確認できない**
4. **サイドバーのタブと幅**
   - **選択中のタブを先頭（左）へ寄せ、ラベルを省略させない**。
     いま見ているモード名が読めないのがいちばん困るため
   - 既定幅を 288px に広げ、右端のドラッグで 220〜480px に変更できるようにした
5. **マイポートフォリオ専有レイアウト**
   - 「💼 マイポートフォリオ」選択中は市場データ・分析結果を出さず、
     ①分析履歴（アーカイブ）と ②AI対話 の 2 ペインに切り替わる
   - 右上のトグルで縦分割（左右）/ 横分割（上下）を切替。選択は保存される
   - 過去ログは「コピー」と「対話へ引用」ができ、そのまま AI に聞ける
6. **保存導線**: 分析結果パネルの「保存」から
   「マイポートフォリオのどこに保存しますか？」を開き、リストをチェックで選ぶ。
   **すでに入っているリストは最初からチェック済み**にし、変わった分だけ書き込む
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 309 件 / Rust 125 件）

### 2026-08-02 — Phase 3 Step 8: サイドバーのモード切替 ＋ 複数ポートフォリオ ✅

1. **サイドバーのモード切替**
   - 「＋ 新規チャット」の上に `💬 対話履歴` / `💼 マイポートフォリオ` のタブを新設
   - 選択は localStorage に保存。**同じモードを選び直しても再描画を起こさない**
2. **フォルダ型のポートフォリオ管理**（`portfolios` / `portfolio_tickers`）
   - 「＋ 新規リスト作成」でリストを追加。右クリック（または ✏️）でリネーム、削除は確認つき
   - リストを削除しても**分析結果そのものは消えない**（未分類として残す）。
     リストは「見方の整理」であってデータの持ち主ではないため
   - 初回だけ「メインポートフォリオ」を作る。消したら復活させない
   - 「検討中銘柄」の右クリックメニューから任意のリストへ追加できる
3. **分析アーカイブ（時系列）**
   - `analysis_history` テーブルを新設。`analyses` は銘柄ごとの最新 1 件しか持たないため、
     **決算期をまたいだ推移を追うには上書きされない置き場が要る**
   - 既存の `analyses` はそのまま残すので、復元機能は影響を受けない
   - 銘柄を開くと「FY2026 Q3 / 4.1 / 日付」のタイムラインが並ぶ。
     直近と 1 つ前のスコア差を ▲▼ で表示する
4. **比較との連動**: ポートフォリオ内でもチェックボックスから横並び比較を起動できる
5. **自動テスト**: フロント 21 件（モード切替・グループ化・並び順の保持）＋
   Rust 16 件（作成・リネーム・削除・重複追加・CASCADE・複数リストへの所属）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 266 件 / Rust 125 件）

### 2026-08-02 — Phase 3 Step 7: ティッカー入力案内 ＋ 横並び比較ダッシュボード ✅

1. **ティッカー入力のフォーマット案内**
   - 入力欄にホバーで「米国株は `AAPL`、日本株は `7203.T` のように `.T` を付ける」を表示
   - ヘルプ AI のナレッジにも「ティッカーの入力形式」節を追加。
     日本株 `.T` に加え、`.AS` / `.DE` / `.HK` / `.KS` などのサフィックスと、
     **SEC EDGAR は米国上場のみ**であることも案内する
   - **ツールチップとナレッジで例が食い違わないことをテストで固定**した
2. **検討中銘柄の複数選択**
   - 各行にチェックボックスを追加。2 銘柄以上で
     「📊 選択した銘柄を横並び比較（最大5社）」ボタンが出る
   - 上限を超えたら**押しても無反応にせず、古い選択から外す**
3. **比較ダッシュボード**（`ComparePanel`・比較タブ）
   - サマリー比較テーブル: 株価 / 時価総額 / 売上成長率 / 粗利率 / 営業利益率 /
     ROE / Cash Runway / EV・粗利 / PER / D/E の 10 行
   - **Cash Runway と EV/粗利は指標から導出**（現金 ÷ 月間バーン、
     EV/売上高 ÷ 粗利率）。営業CF が黒字なら「営業CF黒字」と出し、
     0 か月と誤読させない
   - 各行でいちばん良い値を緑で強調（PER・D/E・EV/粗利 は低いほど良い扱い）
   - 5ブロック AI スコア比較（成長性 / 財務生存性 / 経済性 / 競争優位性 /
     バリュエーション）を横棒グラフ付きカードで並べる。
     **20項目すべてをどれかのブロックに割り当てている**
   - 未分析の銘柄も列として出し、「〜の分析データが未作成です。
     先に単体分析を実行してください」と案内 ＋ その場で分析へ飛べるボタンを置いた
4. **自動テスト 33 件**（比較 28 / ティッカー案内 5）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 245 件 / Rust 109 件）

---

**これより古い履歴は [`docs/archive/PROGRESS_履歴_2026-08.md`](docs/archive/PROGRESS_履歴_2026-08.md) に繰り下げ。**


---

### 2026-08-02 — Phase 3 Step 6: 免責事項の全文モーダル ＋ 常時テロップ ✅

1. **免責事項の全文モーダル**（`LegalDisclaimerModal`）
   - 4 条項（投資助言の否定 / ハルシネーションリスク / 投資リスクと自己責任 /
     データソースの仕様変更）を指定どおりの文面で表示
   - メニューバー「ヘルプ > 免責事項（Legal Disclaimer）」からいつでも読み直せる
   - 文面は `src/lib/legal/disclaimer.ts` に集約。
     **表示箇所ごとに書き分けると「どちらが本当の免責か」が争点になりうる**ため
2. **画面下部の無限ループテロップ**（`DisclaimerTicker`）
   - 橙色の警告アイコン付きで、指定文面が右から左へ流れ続ける
   - **同じ文面を 2 枚並べて `-50%` まで動かし、継ぎ目なくループ**させている
   - クリックで全文モーダルが開く
   - ホバー / フォーカス中はアニメーションを停止（読ませるため）
   - `prefers-reduced-motion` のときは流さない
3. **自動テスト 23 件**
   - 文面: 4 条項がそろっている / 金融商品取引法への言及 / 出力が変動しうる旨 /
     一切の法的責任を負わない旨 / 外部データソースの名指し
   - 開閉: 初期状態 / 二重オープン / 二重クローズ / トグル /
     **変化したときだけ購読者へ通知する**
   - メニュー: ヘルプ配下に項目がある / アクション ID が一致する /
     別アクションでは開かない
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 212 件 / Rust 109 件）

---

---

### 2026-08-02 — Phase 3 Step 5: プロンプトのバイナリ秘匿・4大分析パターン・表示設定 ✅

1. **核心プロンプトを Rust バイナリへ移設**
   - `src-tauri/src/prompts/` に `.md` として置き、`include_str!` でコンパイル時に埋め込む。
     **配布物の JS は開けば読めるが、バイナリに入れれば通常の手段では取り出せない**
   - フロントが送るのは **役割 ID と閾値だけ**。結合は `llm::send` の入口で行い、
     **組み立てた文字列を返すコマンドは用意しない**
   - `systemPrompt.ts` を削除。`criteria.ts` は描画とパースに要る
     「番号・分類・項目名」だけを残し、AI への指示（hint）は core.md へ移した
   - 予算計算のため、長さだけを返す `analysis_prompt_tokens` を追加
   - **フロントに核心文言が残っていないことをテストで見張る**（`promptSecrecy.test.ts`）
2. **4大分析パターン**
   - 汎用 / 高成長・中小型グロース / メガスケーラー大型優良株 / 成熟・高配当バリュー
   - 分析結果パネルのヘッダーから切り替える（`AnalystRoleMenu`）。
     **UI に出るのは役割名・一行説明・重点項目だけ**で、本文は届かない
   - 役割ごとに Cash Runway / FCF マージン・ROIC / FCF 利回り・減配リスク等の
     重点指示が入り、該当項目のスコア下限も指定している
3. **閾値との動的連動**
   - 閾値項目を 10 個に拡張（キャッシュランウェイ・FCFマージン・配当性向を追加）
   - 文字列化は Rust 側が担当し、選択中の役割プロンプトへ動的に注入される
   - TS 側は入力欄のメタ情報だけを持つ。項目 ID の一致は両側のテストで固定
4. **追加のご要望 3 点**
   - 設定「表示」タブを新設し、**ツールチップの ON/OFF トグル**（既定 ON）を追加。
     OFF なら `Tooltip` が子要素を素通しする
   - 機能リクエストを `mailto:`（件名「【StoQ AI】機能リクエスト」）に変更
   - 閾値タブに「💡 迷ったら既定値のままで問題ありません」の案内と
     **「既定値に戻す（デフォルト復元）」ボタン**を上部に配置
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 189 件 / Rust 109 件）

---

---

### 2026-08-02 — Phase 3 Step 4: ツールチップ・初回チュートリアル・可変閾値 ✅
3 つの新機能を実装した。

1. **ツールチップ ＆ 初回チュートリアル**
   - `Tooltip` を新設。`title` 属性だと表示まで 1〜2 秒待たされ改行も効かないため、
     **すぐ出て読みやすい**吹き出しを自前で持つ。画面端では自動で反対側へ寄る
   - 案内文は `tooltipText.ts` に集約（同じ機能の説明が画面ごとに食い違わないように）
   - `?` ヘルプ / ショートカット設定 / 役割選択 / 検討中銘柄パネルの 4 か所に配置
   - `WelcomeTour`（5 ステップ）を新設。初回だけ自動表示し、
     閉じたら localStorage に印を残して二度と自動では出さない。
     ヘルプの「はじめかたを見る」から開き直せる
   - ヘルプ窓に「欲しい機能がありませんか？開発者へリクエストを送る」を追加
2. **可変式 閾値設定**
   - `thresholds.ts` に 7 項目（売上成長率 / 営業利益率 / ROE / PER / PBR / D/E / 配当利回り）を定義。
     **定義と既定値はここが唯一の出所**で、Rust 側には変更した項目だけを保存する
   - 設定モーダルに「分析ルール・閾値」タブを追加。スライダーと数値入力の両方で調整でき、
     変更は即座に保存される。**AI に送る文面をその場で確認できる**ボタンも付けた
   - `buildSystemPrompt(thresholds)` が閾値セクションを差し込み、
     各項目の根拠欄に「基準に対して合格か」を書かせる
   - **データが取れないことを不合格として扱わない**よう明記した。
     資料不足と実力不足の区別がつかなくなるため
3. **自動テスト**
   - フロント 26 件（丸め / 保存値の復元 / 知らない ID の無視 / 文字列埋め込み /
     例示の数値が設定値に追従すること / システムプロンプトへの反映）
   - Rust 3 件（既定では空 / 保存と読み込みの往復 / 閾値を持たない旧設定ファイルの読み込み）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 190 件 / Rust 90 件）

---

---

### 2026-08-02 — Phase 3 Step 3: データ取得元の切替・ドラッグ可能モーダル・ヘルプ AI ✅
3 つの大型機能を実装した。

1. **データ取得元の選択（利用規約リスクの回避）**
   - `MarketDataProvider` トレイトを新設し、Yahoo / FMP / Alpha Vantage を差し替え可能にした。
     **どれを選んでも同じ `Fundamentals` を返す**ので UI 側は取得元を意識しない
   - 設定モーダルに「データ取得元」タブを追加。3 つをラジオで選び、
     それぞれに指定どおりの注記（非公式/推奨/無料枠）を必ず併記する
   - FMP / Alpha Vantage はキー入力欄と「接続テスト」を表示。
     キーは `market:<id>` として LLM のキーと同じ仕組みで保存する（生の値はフロントに出ない）
   - **キー未設定のときは通信する前に理由つきで止める**（`status_of` で判定）
   - 四半期推移は SEC XBRL との突き合わせが要るため Yahoo + SEC 固定。UI にも明記した
2. **モーダルの操作性**
   - `ModalShell` を新設し、設定 / 役割ライブラリ / 銘柄インポート / 資料プレビューを統一
   - **ヘッダーをドラッグして自由な位置へ動かせる。** 位置は「中央からのずれ」で持つので、
     ウィンドウをリサイズしても画面外に取り残されない（`modalDrag.ts` + `ResizeObserver` 相当）
   - **背景クリックでは閉じない。** 入力途中の内容が消える事故を防ぐ。
     閉じるのは ✕ / キャンセル / Esc だけ
3. **ヘルプ AI アシスタント**
   - 最下部ステータスバー右側（バージョン表記の左）に「? ヘルプ」ボタンを新設
   - 押すと右下にドロワーが立ち上がる。銘柄分析の対話とは履歴を分ける（保存もしない）
   - ナレッジは `helpKnowledge.ts` が組み立てる。画面の見方・使い方の流れ・分析のコツ・
     APIキーの設定手順・取得元の違いに加え、**いま実際に割り当てられているショートカット**を
     埋め込む。既定値を書き込むと、変更後に誤案内してしまうため
   - 投資助言はせず操作案内に徹する、無い機能をでっち上げない、という制約も明示
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 164 件 / Rust 87 件）

---

### 2026-08-01 — Phase 3 Step 2: UI/UX 仕上げ・コンプライアンス表現・ショートカット基盤 ✅
7 件の改善を一括で実装した。

1. **既定役割のコンプライアンス表現**
   「テンバガー発掘アナリスト」→「**高成長・グロース株アナリスト**」に改称し、
   本文も「売上・利益成長率や競争優位性を分析する」客観的な表現へ。
   **すでに配布済みの DB に入っている行も `migrate` で差し替える**。
   ユーザーが自分で作った役割（`builtin = 0`）には触れない
2. **確認ダイアログの刷新** — `.ui-fixed` で文字サイズを固定し、
   アイコン付きヘッダー・角丸 `rounded-xl`・`backdrop-blur`・
   ホバー/フォーカスリングを追加。本文の可変フォントに引きずられて崩れなくなった
3. **チャット入力欄** — 入力量に応じて自動で伸び（上限 `max-h-64` でスクロール）、
   `resize-y` で手動調整もできる
4. **「最小化中」の視認性** — 黄色背景（`bg-amber-500`）＋白の太字＋リング。
   パネルが隠れていることを見落とさない
5. **検討中銘柄の小窓**
   - 上の履歴との境界線をドラッグして高さを変更（`clampFirstSize` で丸め、localStorage に保存）
   - ヘッダークリック / ⌄ ボタンで折りたたみ
   - `overflow-auto` ＋ `w-max` で縦横スクロール。✕ は `sticky right-0` で常に押せる
   - **ティッカーの改行羅列（`AAPL⏎NVDA`）だけでも取り込めるようパースを強化**。
     カンマ・読点・タブ区切りにも対応
6. **Ctrl+Enter で送信 / Enter で改行**（mac は ⌘+Enter）。
   長文プロンプトの途中で誤送信しないための割り当て。入力欄の下に常時表示する
7. **ショートカット基盤**
   - `shortcuts` テーブルを新設。**既定値は保存せず、変更した割り当てだけを持つ**
     （既定を両方に置くと、既定を変えたときに古い値が残るため）
   - アクション一覧と既定キーは `src/lib/ui/shortcutStore.ts`
   - キーの正規化・照合・衝突検出は `shortcutKeys.ts`（純粋関数、テスト 21 件）
   - 設定モーダルに「ショートカット」タブを追加。押したキーをそのまま割り当てられる
   - 入力欄にフォーカスがあるときは文字入力を優先し、`allowInInput` のものだけ発火する
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 135 件 / Rust 65 件）

---

---

### 2026-08-01 — Phase 3 Step 1: 検討中銘柄・アーカイブ・役割ライブラリ ✅
4 件の特注要件を一括で実装した。

1. **検討中銘柄（ミニウィンドウ ＋ インポート）**
   - `library.db` を新設し `candidate_stocks` を作成。**既存の `analyses.db` /
     `chats.db` には触れない**ので、新テーブルを増やしても投資データが壊れない
   - パースは `src/lib/candidates/parseCandidates.ts`（純粋関数）。
     全角パイプ `｜`・全角スペース・CRLF・`#` メモ行・重複（後勝ち）を吸収し、
     **取り込めなかった行は捨てずに行番号つきで返す**。
     黙って落とすと「貼ったのに入っていない」に気づけないため
   - モーダルは保存前に解析結果（取り込む銘柄 / 重複 / エラー）を必ず見せる
   - 小窓は 1 行 1 銘柄。件数が増えたら縦、社名が長ければ横にスクロールする
   - 削除は **✕ ボタン**と**右クリックメニュー**の 2 系統（どちらも `removeCandidate`）
   - クリックすると上部のティッカー入力欄にセットし、そのまま分析を開始する
     （同じ銘柄を続けて選べるよう `preset` に連番を添えている）
2. **チャット履歴の独立スクロール ＋ アーカイブ**
   - 履歴 `<nav>` に `min-h-0` + `overflow-y-auto`。
     **`min-h-0` が無いと中身の高さを主張して「検討中銘柄」を押し出す**
   - `chat_sessions` に `is_archived` を `ALTER TABLE` で追加。
     既存行は既定値 0 になるため、**過去の会話は 1 件も失われない**（テスト済み）
   - サイドバー右上の 📁 で「履歴 / アーカイブ」を切り替え。各行の 📁 で移動・復元
   - 削除ダイアログに「残すならアーカイブを」の案内を追加
3. **AI の役割ライブラリ**
   - `prompts` テーブルを新設し、既定 3 種（プロの株式ジャーナリスト /
     テンバガー発掘アナリスト / 慎重な高配当アナリスト）を**初回だけ**投入。
     投入済みの印を `prompt_seed` に残すので、**削除した既定役割が再起動で復活しない**
   - 対話パネルのヘッダーからワンタップで切替（`PromptLibraryMenu`）。
     選択中の役割は端末ごとの好みなので localStorage に保持する
   - 追加・編集・削除は `PromptLibraryModal` から
4. **自動テストとデータ保護**
   - パース 18 件（正常系 / エラー検知 / 重複）、削除ストア 7 件
   - Rust: 旧スキーマ（`is_archived` 無し）の DB を再現し、
     **移行後も会話・メッセージ・ティッカーが残ること**を検証。
     マイグレーションを 3 回流してもアーカイブ状態が初期化されないことも確認
   - `candidate_stocks` と `prompts` が同じ DB に同居しても互いを壊さないことも検証
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 101 件 / Rust 56 件）

---

---

### 2026-08-01 — Phase 2 Step 5.6: メニューバー移設・折りたたみの余白ゼロ化・パネル移動の根本改修 ✅
スクリーンショットで判明した 5 件を一括で実装した。

1. **フォント調整 UI を最上位メニューバーへ完全移設**
   - `CommandBar` から撤去し、`MenuBar` の右端（ファイル / 表示 / 分析 / ヘルプ の並びの右）に配置
   - **`MenuBar` 自体を `.ui-fixed` ＋ 高さ `h-9` 固定にした。**
     ここに調整 UI を置く以上、バーの高さが変わるとスライダーがカーソルの下から動いてしまうため。
     結果として最上部バーだけは文字サイズ連動から外れる（本文・パネル・サイドバーは従来どおり連動）
2. **設定モーダルの「未設定」縦割れを修復**
   - `flex` の子は既定で縮むため、ラベル側に `min-w-0 truncate`、
     状態表示側に `shrink-0 whitespace-nowrap` を明示的に振り分けた
3. **折りたたみ時のデッドスペースを完全に排除**
   - **畳んだ枠は分割そのものから外す**方式に変更。`ResizableSplit` の `collapsed` を廃止し、
     `App.renderWorkspace()` が表示中の枠だけで分割を組み立てる。
     1 枚だけになったら分割せずそのまま 100% を占有する
   - 畳んだパネルは `PanelRestoreBar`（メニューバー右端の小さな復元ボタン）へ退避
   - 最後の 1 枚は畳めないようにした（画面が空になるため）
4. **パネル移動を Pointer Events で根本改修**
   - HTML5 DnD をやめ、`src/lib/ui/panelDrag.ts` を新設。
     `setPointerCapture` でポインタを捕捉し、`document.elementFromPoint` ＋
     `data-panel-slot` 属性でドロップ先を判定する
   - **`draggable` との併用は不可能**。ネイティブドラッグが pointermove を奪うため、
     同じ要素で両立させると必ずどちらかが死ぬ。Pointer Events に一本化した
   - ドロップ先は各パネル全体（ヘッダーだけでなく本文も可）。Esc で中断できる
5. **APIキー削除に確認ダイアログ**（「登録されているAPIキーを削除しますか？」/ [キャンセル] [削除する]）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 76 件 / Rust 35 件）
  パネル移動は状態遷移として 10 件のテストで検証（入れ替え / 自枠 / 枠外 / 中断 / 二重終了）

---

### 2026-08-01 — Phase 2 Step 5.5: コントロール類のサイズ固定 ＆ ドロップ拒否（🚫）の解消 ✅
実機で見つかった Web 標準仕様の考慮漏れ 2 件を修正した。

1. **スライダー操作中のガタつきを解消**
   - 真因は `:root { font-size: var(--fs-base) }`。**`:root` の font-size は `rem` の基準**であり、
     Tailwind の余白・幅・高さはすべて `rem` で表現されている。
     そのため文字を大きくするとボタンやスライダーまで太り、
     **カーソルの下から要素が逃げて操作不能**になっていた
   - 指定を `:root` から `body` へ移動。これで `rem` の基準は 16px に固定されたまま、
     文字だけが継承でアプリ全域に連動する
   - さらに `.ui-fixed` を新設し、右上のコントロール群（AI設定ボタン ＋ フォント調整）を
     まとめて 12px 固定に。`FontSizeControl` の各部品も
     `h-[22px]` / `w-[72px]` / `h-[4px]` など**絶対値 px で完全固定**した
     （上の AI設定ボタンが太るとスライダーが下へずれるため、群ごと固定する必要がある）
2. **パネル D&D の進入禁止マーク（🚫）を解消**
   - HTML5 Drag and Drop は、ドロップ先が **`dragenter` と `dragover` の両方で
     `preventDefault()` を呼ばない限りブラウザ既定の拒否が働く**。
     従来は `dragover` のみ、しかも MIME 種別の判定を通ったときだけ打ち消していた
   - 判定を挟まず必ず打ち消すように変更し、`onDragEnter` も追加
   - `dragstart` で `text/plain` にも同じ値を入れ、カスタム MIME を落とす環境でも成立させた
   - `dragleave` は `relatedTarget` が子要素なら無視し、枠のちらつきを止めた
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 64 件 / Rust 35 件）

### 2026-08-01 — Phase 2 Step 5.4: IBKR 風の操作感へ修正（リサイズ・パネル移動・フォント全域） ✅
IBKR のトレーディングツールを参考にした 5 要件を実装した。

1. **ドラッグリサイズの方向反転と非固定化（最優先）**
   - 原因は符号ではなく**どちらのペインに実サイズを持たせるか**だった。
     従来は 2 番目を固定していたため 1 番目が `flex-1` になり、
     **中身が空（ティッカー未入力）のとき 1 番目が 0 まで潰れて仕切り線を掴めなかった**
   - `splitMath.ts` を `clampSecondSize` → `clampFirstSize` + `desiredFirstSize` に作り替え、
     **1 番目に px を与えて 2 番目に残りを割り当てる**方式へ変更。
     仕切り線がポインタ位置そのものになるため、
     「下へ引く＝上の市場データが広がる / 上へ引く＝下の対話が広がる」が直感どおりになった
   - `ResizeObserver` を追加。**ウィンドウを縮めても固定 px が残らず**、
     常に両ペインの最小サイズを満たす範囲へ入れ直す
   - 当たり判定を 1px → 5px（`-top-2 h-5`）に拡大し、`pointercancel` でも確実に離す
   - `.panel` の `height: 100%` を `flex: 1 1 0%` に変更し、CSS の固定高を撤廃。
     コンポーネント側の固定高 24 箇所も `min-h-*` に置換した
2. **グリップアイコンによるパネル移動の実効化**
   - ヘッダー全体ではなく**グリップ（サイコロの 6）だけを `draggable`** にした。
     ヘッダー全体だと中のボタンやスライダーが掴めなくなるため
   - ドロップ先はヘッダー全体。`application/x-stoq-panel` を持つドラッグのときだけ
     枠が光り、離すと `layoutStore.movePanel` で配置が入れ替わる（localStorage に保存）
3. **フォントコントロールの移設**: 分析結果パネルのヘッダーから、
   画面右上「AI設定」ボタンの直下（`CommandBar`）へ移動
4. **フォント上限 28px ＋ アプリ全域適用**
   - `MAX_FONT_SIZE` を 20 → 28 に拡張
   - `:root` 自体に `font-size: var(--fs-base)` を設定し、**個別指定の無い要素も連動**させた
   - 固定 px クラス 84 箇所（メニューバー・タブ・サイドバー・資料チップ・トースト・
     モデル入力欄など）を `.t-body` / `.t-label` / `.t-heading` / `.t-mono` に置換
5. **検証**: `npm run verify`（typecheck → test → build）と `cargo test --lib` が全通過。
   フロント 64 件（うちドラッグ方向の意味を検証する新規テストを追加）/ Rust 35 件

---

---

### 2026-08-01 — Phase 2 Step 5.3: レイアウト修復・フォント可変・パネル移動・自動テスト ✅
- **着手前の確認**: ロゴ変更・分析根拠表示・チャット履歴の SQLite 化は Step 5.2 で実装済み。
  新規に必要だったのはレイアウト修復・パネル移動・フォント可変・自動テストの 4 点
- **レイアウト崩れの修復**
  - `styles.css` に `.panel` / `.panel-scroll` を定義し全パネルで共通化。
    **`min-height: 0` が無いと Flex 子要素が内容の高さを主張してスクロールせず枠外へ突き抜ける**
  - 対話パネルの入力欄を固定高（`min-h-11`）にし、textarea に `max-h-40` と独立スクロールを付与。
    **フォントを 20px にしても送信ボタンが埋もれない**
  - スプリッターの当たり判定を 1px → 4px に広げ、`setPointerCapture` を追加
- **パネルのドラッグ配置**: ヘッダーをドラッグして他パネルへ落とすと入れ替わる。
  配置は `layoutStore`（localStorage）で管理し、壊れた保存値も自動で正規化する
- **フォント 10〜20px の 1px 刻み**: `:root` の CSS 変数 `--fs-base` 方式に変更し、
  `.t-body` / `.t-label` / `.t-heading` で全パネルへ連動。スライダーと px 表示を追加
- **自動テストを整備**（`vitest` 導入、`npm run verify` で通し実行）
  - フロント 57 件 / Rust 35 件。DB ロジックを `*_in(conn, …)` に分離してテスト可能にした
- **テストが 2 件の不具合を検出し修正**
  1. スマート圧縮でセグメント明細が 1 行落ちていた（優先確保の閾値を 0.6 → 0.75）
  2. HTML 抽出が日本語の途中に空白を入れていた（「売上高は 512億 ドル」）
- **改修記録を `docs/archive/改修記録_2026-08.md` へ繰り下げ**（11 件をアーカイブ、直近 3 件を本体に保持）

### 2026-08-01 — Phase 2 Step 5.2: 2 カラム化・分析根拠表示・チャット履歴の実機能化 ✅
6 件の要件を一括で実装した。
1. **ロゴを「StoQ AI Analyzer」に変更**（`MenuBar`）
2. **分析根拠バッジ**を分析結果パネルのヘッダー直下に追加。
   実行時に組み立てて `AnalysisRun.basis` に持ち、SQLite にも保存するため、
   **復元した過去の分析でも当時のデータ元が分かる**
   （`analyses` テーブルに `basis` 列を追加。既存 DB は `ALTER TABLE` で移行）
3. **一次資料が 0 件のときの確認ダイアログ**。指定文面と [戻る] / [このまま分析する]。
   1 件以上あれば確認なしで即実行
4. **2 カラム構造に組み替え**。外側を左右分割、左カラムの内側を上下分割にすることで、
   **左右の仕切りが画面最下部まで一直線に伸びる**。
   市場データは縦スクロール（`overflow-y: auto`）
5. **文字サイズを 4 段階（13 / 15 / 17 / 19px）に拡張**し、
   市場データ・分析結果・対話の**全パネルに適用**。旧保存値は読み替える
6. **チャット履歴を SQLite で実機能化**（`chats.db`）
   - 送信のたびに自動保存。ユーザー発言は応答を待たずに先に保存する
   - 未命名セッションは最初のユーザー発言の先頭 40 文字を自動タイトルにする
   - サイドバーから切り替え・リネーム（✏️ / ダブルクリック）・削除
   - 削除は確認ダイアログを挟み、メッセージも CASCADE で一緒に消える
   - ダミーデータ（`sampleData.ts` の `SAMPLE_SESSIONS`）は削除
- **検証**: チャット履歴の SQLite ロジックを直接実行し、
  作成 / 自動命名（40 文字超の省略含む）/ 一覧（更新順・件数）/ リネーム /
  メッセージ復元 / 削除（CASCADE）がすべて期待どおりであることを確認

---

### 2026-08-01 — Phase 2 Step 5.1: 3 ペイン構成への再構築 ✅
- **レイアウトを 3 ペイン構成へ作り直した**
  - 上段左＝市場データ（四半期モメンタム / 指標カード / SEC ステータス）
  - 上段右＝20項目の分析結果（**縦長**にしてスクロールしやすくした）
  - 下段＝対話ウィンドウ（**左右いっぱいに横長**、高さ 1/3）
  - すべての境界がドラッグでリサイズ可能。`ResizableSplit` を入れ子で使い回している
  - 各ペインヘッダー右端の **`_`** で最小化、**`□`** で復元。
    左右は同時に畳めないようにした
  - 配置切替は不要になったため `BottomDock` を削除
- **一括クリアの 2 段階確認**を追加（`ConfirmDialog`）
  - 一時保存資料は AI に渡すコンテキストそのものなので、即座に消さず確認を挟む
  - 「事前に AI に『引き継ぎ書』を書かせてから」という案内と、削除件数・トークン数を表示
  - 既定フォーカスを「もどる」に置き、Enter 連打で誤って消えないようにした
- **プロバイダ切替を `ProviderMenu` に集約**
  - 横並び 3 個 →「⚙️ AI設定 ｜ ● 稼働中: OpenAI ▾」の 1 ボタン + ドロップダウン
  - カスタムプロバイダも一覧に出る。未設定の項目には理由（APIキー未設定 / Base URL 未設定）を表示
  - `ApiKeyIndicator` は役目を終えたため削除
- 文字サイズ（A− / A+）は右ペインにそのまま維持

### 2026-08-01 — Phase 2 Step 5: フレキシブル UI ＆ 分析結果の永続化 ✅
- **レイアウト**
  - `ResizableSplit` を新設。**縦並び / 横並びの両方**に対応する汎用スプリット。
    旧 `SplitPane`（縦専用）は削除
  - `BottomDock` で分析結果と対話をまとめ、右上のボタンで配置を切替。
    **既定は縦並び**（20項目テーブルは横幅を使うほうが読みやすいため）。
    選択は localStorage に保存
  - `PanelHeader` に折りたたみボタン。畳むとヘッダーだけが残りタブのように機能する。
    両方同時には畳めないようにした
- **可読性**
  - `textScale.ts` に 3 段階（小 12px / 標準 13px / 大 15px）を定義し、
    分析結果パネルの `A− / A+` で切替。行間も 1.75〜1.95 に拡大
  - 評価テーブルの列幅を広げ、スコアドットを 2px 拡大
- **分析結果の永続化（SQLite）**
  - `rusqlite`（`bundled`）を採用。実行環境に SQLite が無くても動く
  - `<app_data_dir>/analyses.db` に 1 銘柄 1 件（最新）で保存
  - 生成完了と同時に自動保存。**中断した場合も途中結果を保存する**
  - 銘柄タブを開くと自動復元。ヘッダーに「〜に保存した結果を復元」と表示
  - 削除は「クリア」ボタンを押したときだけ
- **検証**: SQLite のロジックを Tauri なしで直接実行し、
  保存 / 同一銘柄の上書き / 復元 / 一覧（新しい順）/ 個別削除がすべて期待どおりであることを確認

### 2026-08-01 — Phase 2 Step 4.1: スマート圧縮 ＆ 4Q モメンタム評価 ✅
- **中間カット（head-tail）を完全撤去。** 決算資料では中盤にセグメント業績・
  ガイダンス・Q&A が集中するため、中間削除は分析上 NG という指摘を受けての対応
- `src/lib/prompts/condense.ts` を新設。構造分割 → 価値採点 → 予算配分 →
  文単位の抽出、という流れに変更。**要約ではなく原文抽出**なので事実の改変が起きない
  - 定型文（免責事項・将来見通しの注意書き）は −5 点で真っ先に除外
  - **数値明細セクション（セグメント別業績など）は丸ごと優先確保**。
    一部だけ残すと「最も伸びている事業だけ欠けた表」になり誤読を招くため
  - Q&A は質問と回答を 1 単位に結合。片方だけ残らないようにした
  - 落とした箇所には `…（N文を省略）…` を必ず挿入
- **四半期推移（4Q）とモメンタム**を追加
  - `src-tauri/src/quarterly.rs` を新設。Yahoo（売上・純利益・EPS）と
    SEC XBRL（YoY 比較対象）を統合
  - UI に `QuarterlyTrend`（TradingView 風）。売上高 / 純利益 / 純利益率 / EPS を切替、
    「⏫ 成長が加速 / ⏬ 成長が減速」バッジを表示
  - プロンプトに四半期テーブルを追加し、system で
    「YoY を主軸」「減速ならスコア 3 以下」「QoQ だけで加速と結論づけない」を厳守させた
- **実 API で確認した制約**（`docs/設計.md` 6.8 に記載）
  - Yahoo は四半期粒度で粗利・営業利益・キャッシュフローを返さない（0 / null）
  - Yahoo の四半期履歴は 4 期分のみ → そのままでは YoY を計算できない
  - SEC XBRL なら 30 四半期分あり、**期末日の突き合わせ**で YoY を算出。
    AAPL で +16.4% となり Yahoo の公表値と一致
  - 10-Q には第4四半期が含まれないため、インデックスで 4 つ前を見る方法は誤り
- **検証中に 3 件の欠陥を発見して修正**
  - 連続する見出しでセクション名が上書きされ、「質疑応答」の章題が消えていた
  - トークン数で割る評価だと短い文が優先され、情報量の多い行
    （「サービス部門の売上高は287億ドル…」）が落ちていた → 0.35 乗に緩和
  - Q&A を 1 問ずつ別セクションに割ると、Q&A だけで予算を食い尽くしていた

### 2026-08-01 — Phase 2 Step 4: Prompt Engine ＆ 20項目ファンダメンタル評価 ✅
- **Step 3 の微修正**
  - PPTX 対応（`ppt/slides/slideN.xml` の `<a:t>` を抽出。発表者ノートも取り込む）
  - チップに ✏️ リネームアイコンを追加（ダブルクリックのみだった操作を明示的に）
  - ドロップゾーンに「※ URL ではなくローカルのファイル」の注記を追加
- **プロンプトエンジン**（`src/lib/prompts/`）
  - `criteria.ts` — 20項目を 1 ファイルに集約。ここだけ差し替えれば評価軸を変えられる
  - `systemPrompt.ts` — 出力フォーマットを厳密に指定。幻覚対策として
    「資料に無いことを断定しない」「判断不能はスコア 0」「数値の出所を明記」を厳守事項に
  - `buildPrompt.ts` — 財務指標 → 添付資料 → SEC の優先度で予算配分。
    SEC は Risk Factors / MD&A 等の重要節を優先抽出
  - `parseAnalysis.ts` — Markdown テーブルを構造化。**ストリーミング途中でも解釈できる**
  - `analysisRunner.ts` — 資料収集 → プロンプト構築 → 生成 のオーケストレーション
- **UI**
  - `AnalysisPanel` を全面刷新。20項目のスコアバー、強み / リスク / バリュエーション所見 /
    総合投資判断を逐次描画。平均スコアをヘッダに表示
  - 「AI分析を実行」ボタンと、生成中に切り替わる「中断」ボタン
- **中断機能**（Rust）
  - `requestId` で識別し、`llm_cancel` で SSE 読み取りループを抜ける。
    それまでの生成テキストは破棄しない
- **バグを 1 件検出して修正**
  - `headTail` がトークン→文字数の逆算に固定係数 2.5 を使っており、
    日本語資料で **上限を 21% 超過**していた（26,633 tok / 上限 22,000）。
    文章自身の「文字あたりトークン数」から逆算する方式に変更し、
    さらに全体での最終検算（`enforceLimit`）を追加
  - 資料ごとの配分も「均等割り → 余りを再配分」に改め、予算利用率を 74% → 98% に改善
- **Node 上で実データ検証**
  - PPTX: スライド 2 枚 + 発表者ノートを正しく抽出
  - パーサ: 20/20 行、平均スコア、強み・リスク・所見・判断すべて正しく構造化。
    途中経過（5 行時点）でも破綻せず解釈
  - 予算: 上限 30,000（出力用 8,000 確保）に対し 21,616 tok で収まり、
    重要 3 節すべてがプロンプトに含まれることを確認

### 2026-08-01 — Phase 2 Step 3: 一次資料の解析 ＆ ステージングキャッシュ管理 ✅
- **テキスト抽出**（すべてフロント側・ローカル完結）
  - PDF: `pdfjs-dist`。1MB 超あるため**動的 import** にし、メインバンドルを 258KB に維持
    （静的 import だと 690KB まで膨らんだ）
  - DOCX: `fflate` で ZIP を展開し `word/document.xml` からタグ除去。専用ライブラリ不要
  - HTML / TXT / MD / CSV / JSON にも対応
  - PDF は `--- p.N ---` のページ区切りを入れ、出典を追えるようにした
- **ステージング**（`src-tauri/src/documents.rs`）
  - `<app_data_dir>/temp_documents/` に抽出テキストと `index.json` を保存。再起動後も保持
  - **元のバイナリは複製しない**（理由は `docs/設計.md` 6.6 に記載）
- **UI**（ドロップゾーンの直下に常設トレイ）
  - 🟢 バッジ付きチップ / クリックでプレビュー / ダブルクリックでリネーム / ✕ で削除 / 一括クリア
  - 取り込み中は「抽出中 → 保存中」の 2 段階を表示
  - トークンメーター（🟢 <75% / 🟡 75-100% / 🔴 ≧100%）
- **トークン概算**は `CJK文字数 + ceil(その他/4)`。Rust と TS の両方に同じ規則を置いた
- **Node 上で実データ検証**
  - PDF: 生成した検証用 PDF から 3 行を正しく抽出、概算 25 トークンを算出
  - DOCX: 段落・タブ・改行・実体参照・日本語すべて正しく抽出

### 2026-08-01 — Phase 2 Step 2: Yahoo Finance ＆ SEC EDGAR のデータ取得と UI 接続 ✅
- **Yahoo Finance クライアント**（`src-tauri/src/yahoo.rs`）を実装
  - Cookie → crumb → quoteSummary の 3 段認証。crumb はプロセス内にキャッシュし、
    401 のときだけ 1 回取り直す
  - 6 グループ 36 項目（時価総額 / PER / PBR / PSR / 売上・EPS成長率 / 営業利益率 /
    ROE / フリーCF / D/E ほか）を取得し、表示用に整形
  - Yahoo は割合を小数で返すものと百分率で返すものが混在するため整形関数を分けた
    （`returnOnEquity` は 0.4523、`debtToEquity` は 78.445）
  - `quoteSummary` が失敗しても、crumb 不要の `chart` から株価・銘柄名・通貨は返す
- **SEC 提出状況の軽量照会**（`edgar::fetch_status`）を追加。本文をダウンロードせず、
  10-K / 10-Q の最新 1 件（提出日・対象期間・URL）だけを返す
  - 「EDGAR に無い」「User-Agent 未設定」はエラーではなく `status` として返し、
    非米国上場銘柄でも Yahoo の指標表示を止めない
- フロントエンド
  - `src/lib/api/` に `yahoo.ts` / `sec.ts` / `analysisStore.ts` を追加。
    YF と SEC は並行実行し、片方の失敗が他方を止めない
  - `WorkspacePanel` を実データ描画に置き換え（銘柄名・株価・前日比・取得時刻）
  - `MetricCard` / `MetricCardSkeleton` / `FilingStatusBadge` を追加
  - `ToastHost` + `toastStore` でエラー通知（種類ごとに自動消去時間を変える）
  - 取得失敗時は本文中にもエラーと「再試行」ボタンを表示
- **実 API で検証済み**
  - Yahoo: crumb 取得成功、使用している 32 フィールドすべてが AAPL で実値を返す
  - SEC: ticker マップ 10,432 銘柄、AAPL の 10-K (2025-10-31) / 10-Q (2026-07-31) の
    URL 生成まで確認。7203.T が EDGAR 未登録（🔴 相当）であることも確認

### 2026-08-01 — 改良: モデル名入力欄をコンボボックス化 ✅
- **LLM 疎通確認がユーザー側で成功**（OpenAI / gpt-5.6）。方言の自動フォールバックも機能
- モデル名の入力欄を `<datalist>` によるコンボボックスに変更（`ModelCombo.tsx`）
  - ドロップダウンから代表的なモデルを選択でき、任意の文字列の手入力も可能
  - 新しいモデルが出てもアプリを更新せずに使える
- `modelCatalog.ts` に候補を集約。OpenAI互換プロバイダは **Base URL から提供元を推測**して
  候補を切り替える（deepseek / moonshot / openrouter / groq / localhost）
- ユーザー例示の `claude-3-5-sonnet-latest` / `claude-3-opus-latest` は提供終了済みのため、
  現行の `claude-opus-5` / `claude-sonnet-5` / `claude-opus-4-8` / `claude-haiku-4-5` を候補にした

### 2026-08-01 — 修正: GPT-5 系で送信が 400 になる問題 ✅
- OpenAI のキー投入後、`Unsupported parameter: 'max_tokens' ... Use 'max_completion_tokens' instead`
  で送信が失敗する不具合を修正
- 原因: OpenAI互換 API 間でパラメータの方言が揃っていない
  - GPT-5 系は `max_completion_tokens` のみ受け付け、`temperature` も既定値以外を拒否
  - DeepSeek 等の他社互換は `max_tokens` のみ
- 対応: `llm/openai.rs` に方言吸収を実装。400 のエラーメッセージを読んで
  パラメータを調整し自動で再送する（最大 3 回、出力上限の切り替えは 1 回のみ）
- 対話パネルに、選択中プロバイダの設定不足を常時知らせる警告バーを追加

### 2026-08-01 — Phase 2 Step 1.1: OpenAI互換プロバイダの可変長リスト化 ✅
- 固定 1 枠だった OpenAI互換プロバイダを、**任意個追加・削除できるリスト**に変更
  - `CustomProvider { id, label, base_url, model }` を導入し、`Settings.custom_providers: Vec<_>` で保持
  - APIキーは組み込み・カスタムを区別せず同一辞書（プロバイダ ID → キー）で管理。
    マスク表示・保存・削除のロジックが 1 本で済む
  - 枠を削除すると対応する APIキーも同時に破棄。選択中のものを消した場合は組み込みへ戻す
  - 旧形式（`custom_base_url` + `keys["custom"]`）を読み込み時に自動でリストへ移行
- Rust コマンドを追加: `settings_add_custom_provider` / `settings_update_custom_provider` /
  `settings_remove_custom_provider`
- `llm/mod.rs` のディスパッチを ID ベースに変更。組み込み 3 種以外は
  カスタム定義を引いて OpenAI 互換実装を再利用する
- フロントエンド
  - `providers.ts` を組み込み定義 + ラベル解決 + `providerReadiness()`（キー / Base URL /
    モデル名が揃っているかの判定）に再編
  - `SettingsModal` に「＋ プロバイダーを追加」と各枠の「✕」を実装。ラベルはインライン編集
  - `ApiKeyIndicator` を全プロバイダ表示に対応（横スクロール、ラベル truncate）
- **将来の複数 LLM 直列/並列検証**に備え、カスタムプロバイダを配列として保持している
- **次**: Step 2（Yahoo Finance クライアントと財務データの UI 接続）

### 2026-08-01 — Phase 2 Step 1: 設定モーダルと LLM 接続基盤 ✅
- `docs/設計.md` に Phase 2 の全体設計図を記載（モジュール構成・データフロー・
  アーキテクチャ判断・Step 1 詳細設計・20項目の初期案）
- Rust 側を実装
  - `error.rs` — 共通エラー型（日本語メッセージでフロントへ返す）
  - `http.rs` — Cookie ストア共有の HTTP クライアント
  - `settings.rs` — 設定の永続化、APIキーのマスク処理
  - `llm/mod.rs` — 共通インターフェース、SSE ポンプ、Channel 送出
  - `llm/openai.rs` / `llm/anthropic.rs` / `llm/gemini.rs` — 3 実装（Custom は openai を再利用）
  - `commands.rs` — `settings_load` / `settings_save` / `settings_set_key` / `llm_send`
- フロントエンドを実装
  - `src/lib/tauri.ts` — invoke / Channel のラッパと Tauri 環境判定
  - `src/lib/config/providers.ts` — プロバイダのメタ情報
  - `src/lib/config/settingsStore.ts` — `useSyncExternalStore` による設定ストア
  - `src/lib/llm/client.ts` — `streamChat`（Channel を Promise + コールバックに変換）
  - `src/components/SettingsModal.tsx` — 設定モーダル
  - `ChatPanel` を実接続（ストリーミング表示、Enter 送信、エラー表示）
  - `ApiKeyIndicator` / `CommandBar` / `MenuBar` / `App` を設定ストアに接続
- `.env` ベースのキー読み取り（`apiKeyStatus.ts` / `maskSecret.ts`）を削除
- `CLAUDE.md` に「秘密情報をフロントに渡さない」「HTTP は Rust 側」のルールを追記
- **Step 2 の先行実装**: `edgar.rs`（SEC EDGAR）と `html.rs`（HTML→テキスト）を作成。
  コマンド `sec_fetch_latest_filing` として登録済みだが **UI には未接続**
- **次**: Step 2（Yahoo Finance クライアントと財務データの UI 接続）

### 2026-08-01 — Phase 1: スケルトンUI の構築と起動確認 ✅
- 開発環境を調査。Rust 1.95 / VS Build Tools 2022 / WebView2 は導入済み、Node.js のみ未導入と判明
- **Node.js 24.18.1 を winget でインストール**（ユーザー承認のうえ実行）
- Tauri 2.0 プロジェクトを構成（`Cargo.toml` / `tauri.conf.json` / `main.rs` / `lib.rs` / `commands.rs`）
- アプリアイコンを生成（Python の自作スクリプトで PNG / ICO を出力）
- フロントエンドを構成（Vite 7 / React 19 / TypeScript strict / Tailwind CSS v4 / `@/` エイリアス）
- スケルトン UI を 1 機能 1 ファイルで実装（`src/components/` 配下 13 ファイル）
- **動作確認**: typecheck ✅ / build ✅ / Vite dev ✅ / Tauri ウィンドウ起動 ✅

### 2026-07-31 — プロジェクト初期セットアップ
- Git リポジトリを初期化（ブランチ `main`）
- `.gitignore` / `.gitattributes` / `README.md` / `PROGRESS.md` / `改修記録.md` / `CLAUDE.md` を作成
- `docs/` ディレクトリと設計・API 調査のプレースホルダを作成
