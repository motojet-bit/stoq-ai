# i18n 未対応文字列の一覧

`src/locales/{ja,en}.json` に載っていない日本語文字列を機械的に洗い出したもの。
コメントは除外し、**文字列リテラルと JSX テキストだけ**を対象にしている。

生成元: `npm run test` の対象外（走査スクリプトは使い捨て）。
再生成が要るときは、コメントを除いたうえで `[ぁ-んァ-ヶ一-龠]` を含むリテラルを拾えばよい。

| 区分 | ファイル数 | 文字列数 |
| --- | ---: | ---: |
| フロントエンド（`src/`） | 87 | 867 |
| Rust（`src-tauri/src/`、テスト除く） | 26 | 288 |
| **合計** | **113** | **1155** |

---

## A. 画面に直接出る UI 文言（46 ファイル / 417 件）

最優先。辞書キーを足して `t()` に置き換えるだけで済むものが大半。

| ファイル | 件数 | 内容 |
| --- | ---: | --- |
| `src/components/SettingsModal.tsx` | 33 | 設定モーダルの見出し・入力欄ラベル・注記（「使用するプロバイダ」「APIキーとモデル」「SEC EDGAR」など） |
| `src/components/CloudSyncSettings.tsx` | 30 | クラウド同期タブ全体。連携状態・アクセス範囲の説明文・ボタン・確認ダイアログ |
| `src/components/WelcomeTour.tsx` | 29 | 初回チュートリアル 5 ステップの本文とボタン（「スキップ」「次へ」「はじめる」） |
| `src/components/PortfolioPanel.tsx` | 24 | マイポートフォリオ画面のラベル・空状態・操作ボタン |
| `src/components/MarketProviderSettings.tsx` | 23 | データ取得元タブ。各プロバイダの位置づけ説明・接続テストの結果表示 |
| `src/components/PortfolioHistoryPanel.tsx` | 21 | ポートフォリオ分析履歴（タイムライン）の見出し・空状態・操作 |
| `src/components/CandidateStocksPanel.tsx` | 19 | 検討中銘柄の小窓。列見出し・右クリックメニュー・空状態 |
| `src/components/ChatHistoryItem.tsx` | 15 | チャット履歴 1 件のメニュー（改名・アーカイブ・削除）と確認文 |
| `src/components/PromptLibraryModal.tsx` | 14 | AI 役割ライブラリの編集モーダル |
| `src/components/Sidebar.tsx` | 14 | サイドバーの見出し・空状態・ボタン |
| `src/App.tsx` | 12 | アプリ全体の確認ダイアログ文言・タブ名・最小化の禁止理由 |
| `src/components/AnalysisPanel.tsx` | 12 | 分析結果パネルの見出し・状態表示・空状態 |
| `src/components/ChatPanel.tsx` | 12 | 対話パネルの入力欄プレースホルダ・添付の注記・エラー表示 |
| `src/components/ComparePanel.tsx` | 12 | 横並び比較ダッシュボードの見出し・空状態 |
| `src/components/ThresholdSettings.tsx` | 12 | 可変閾値タブの初心者向け案内と操作 |
| `src/components/HelpAssistant.tsx` | 11 | ヘルプ AI 画面の案内文・質問例・機能リクエストの導線 |
| `src/components/DocumentTray.tsx` | 9 | 一時保存中の資料トレイの見出し・件数・一括操作 |
| `src/components/SaveToPortfolioModal.tsx` | 9 | 分析結果の保存先リスト選択ダイアログ |
| `src/components/ShortcutSettings.tsx` | 9 | ショートカット設定の操作（「変更」「既定に戻す」など） |
| `src/components/LicenseSettings.tsx` | 8 | ライセンス認証タブの説明とボタン |
| `src/components/QuarterlyTrend.tsx` | 8 | 四半期推移グラフの軸ラベル・凡例 |
| `src/components/CandidateImportModal.tsx` | 7 | 検討中銘柄の一括インポート。貼り付け例と書式の説明 |
| `src/components/ProviderMenu.tsx` | 7 | プロバイダ切替ドロップダウン |
| `src/components/PortfolioHeatmap.tsx` | 6 | ヒートマップの凡例・空状態 |
| `src/components/StagedFileChip.tsx` | 6 | 資料チップの操作（改名・削除）ツールチップ |
| `src/components/WorkspacePanel.tsx` | 6 | ワークスペースの空状態・操作案内 |
| `src/components/ExportMenu.tsx` | 4 | エクスポートのドロップダウン項目 |
| `src/components/PdfDropZone.tsx` | 4 | ドロップゾーンの案内文と対応形式 |
| `src/components/PromptLibraryMenu.tsx` | 4 | 役割切替ドロップダウン |
| `src/components/AnalystRoleMenu.tsx` | 3 | 分析役割（4 パターン）の選択メニュー |
| `src/components/DisplaySettings.tsx` | 3 | 一般タブの残り文言（レイアウト初期化の説明など） |
| `src/components/FontSizeControl.tsx` | 3 | 文字サイズ調整のツールチップ |
| `src/components/FreeTierLimitModal.tsx` | 3 | 無料版上限モーダルの本文（ボタンは辞書化済み） |
| `src/components/LegalDisclaimerModal.tsx` | 3 | 免責事項モーダルのボタン・見出し |
| `src/components/ModalShell.tsx` | 3 | モーダル共通枠の「ドラッグして位置を移動」「中央に戻す」「〜を閉じる」 |
| `src/lib/i18n/locales.ts` | 3 | 言語選択の表示名（「日本語」「英語」）。en 側は英語表記に要検討 |
| `src/components/CriterionScoreRow.tsx` | 2 | 評価 1 行の補助表示 |
| `src/components/DisclaimerTicker.tsx` | 2 | テロップのクリック案内 |
| `src/components/FilingStatusBadge.tsx` | 2 | SEC 資料準備バッジ（🟢/🟡/🔴）の説明 |
| `src/components/PanelHeader.tsx` | 2 | パネル見出しの操作ツールチップ |
| `src/components/TabBar.tsx` | 2 | タブの操作（閉じる等） |
| `src/components/TokenMeter.tsx` | 2 | トークン概算メーターのラベル |
| `src/components/PanelRestoreBar.tsx` | 1 | 最小化パネルの復元ボタン |
| `src/components/ToastHost.tsx` | 1 | トーストの閉じるボタン |
| `src/lib/sampleData.ts` | 1 | 初期タブ名「ワークスペース」 |
| `src/lib/ui/tooltipHint.ts` | 1 | ツールチップの追加ヒント |

## B. 通知・エラーメッセージ（29 ファイル / 159 件）

トーストや状態表示。数が多く、変数を差し込むものは `{{var}}` 補間が要る。

| ファイル | 件数 | 内容 |
| --- | ---: | --- |
| `src/lib/prompts/analysisRunner.ts` | 18 | 分析実行の進捗・成功・失敗トースト、および「分析根拠」のラベル |
| `src/lib/ui/shortcutStore.ts` | 15 | ショートカット 6 種の表示名と説明文＋保存失敗トースト |
| `src/lib/api/sec.ts` | 14 | SEC 資料の準備状況（「準備完了」「書類なし」等）と理由文 |
| `src/lib/cloud/cloudStore.ts` | 12 | クラウド同期の成功・失敗トースト |
| `src/lib/cloud/cloudBackup.ts` | 11 | クライアント ID の書式エラー、「◯分前」などの経過時間表示 |
| `src/lib/parser/documentStore.ts` | 9 | 資料の取り込み・改名・削除の結果トースト |
| `src/lib/config/providers.ts` | 8 | プロバイダが使えない理由（「APIキーが未設定です」等）とモデル名の入力例 |
| `src/lib/chat/chatStore.ts` | 7 | チャット履歴操作のトースト |
| `src/lib/portfolio/portfolioStore.ts` | 7 | ポートフォリオ操作のトースト |
| `src/lib/export/exportStore.ts` | 6 | 書き出しの成功・失敗トースト |
| `src/lib/chat/chatAttachments.ts` | 5 | チャット添付の上限・失敗メッセージ |
| `src/lib/license/licenseStore.ts` | 5 | ライセンス認証のトースト |
| `src/lib/prompts/promptLibrary.ts` | 5 | 役割ライブラリ操作のトースト |
| `src/lib/candidates/candidateStore.ts` | 4 | 検討中銘柄操作のトースト |
| `src/lib/portfolio/saveTarget.ts` | 4 | 保存先の既定名・重複時の文言 |
| `src/lib/api/analysisStore.ts` | 3 | 分析結果の読み込み失敗トースト |
| `src/lib/candidates/parseCandidates.ts` | 3 | 一括インポートの書式エラー |
| `src/lib/legal/eulaStore.ts` | 3 | 免責同意の保存・撤回の失敗トースト |
| `src/lib/parser/tokenCount.ts` | 3 | トークン概算の区分ラベル |
| `src/lib/portfolio/heatmap.ts` | 3 | ヒートマップの区分ラベル |
| `src/lib/license/freeTier.ts` | 2 | 「無料版であと N 銘柄」の残枠表示 |
| `src/lib/llm/client.ts` | 2 | LLM 送信の失敗メッセージ |
| `src/lib/parser/extractText.ts` | 2 | 未対応形式のエラー |
| `src/lib/parser/pptx.ts` | 2 | PPTX 解析の失敗メッセージ |
| `src/lib/prompts/analystRoleStore.ts` | 2 | 役割一覧の取得失敗トースト |
| `src/lib/api/yahoo.ts` | 1 | 株価取得の失敗メッセージ |
| `src/lib/config/settingsStore.ts` | 1 | ブラウザ実行時の案内 |
| `src/lib/license/freeTierStore.ts` | 1 | 無料版カウントの保存失敗トースト |
| `src/lib/parser/docx.ts` | 1 | DOCX 解析の失敗メッセージ |

## C. 分析ドメインのラベル（6 ファイル / 142 件）

画面表示とデータ構造の両方で使う。**訳すと保存済みデータや AI 出力との突き合わせが壊れる箇所がある**ので、表示名と内部キーを分ける必要がある。

| ファイル | 件数 | 内容 |
| --- | ---: | --- |
| `src/lib/prompts/criteria.ts` | 42 | 20 項目の評価基準名と 6 カテゴリ名。**AI の出力テーブルと突き合わせる鍵**でもある |
| `src/lib/compare/compareData.ts` | 27 | 横並び比較の指標名（「売上成長率（YoY）」等）。Yahoo の指標名との対応表を兼ねる |
| `src/lib/prompts/thresholds.ts` | 24 | 可変閾値 20 項目の表示名・単位・初心者向け説明。プロンプトにも埋め込まれる |
| `src/lib/config/modelCatalog.ts` | 21 | モデル候補の一行説明（「最新世代」「低コスト・高速」等） |
| `src/lib/export/analysisRecord.ts` | 17 | 保存 JSON の指標名と判定ラベル（良好/中立/懸念）。**過去データとの互換に影響** |
| `src/lib/export/exportAnalysis.ts` | 11 | CSV/MD/JSON の列見出しと書き出し形式の説明 |

## D. AI プロンプト・パーサ用の日本語（4 ファイル / 104 件）

**そのまま残すべきものが混ざっている。** 見出しやキーワードは機械的な突き合わせに使っているため、訳すと機能が壊れる。

| ファイル | 件数 | 内容 |
| --- | ---: | --- |
| `src/lib/prompts/condense.ts` | 60 | **大半は翻訳してはいけない。** 日本語の決算資料を解析するためのキーワード辞書（「セグメント」「質疑応答」「免責事項」等）。末尾の 5 件だけが画面に出る圧縮結果の説明 |
| `src/lib/prompts/helpKnowledge.ts` | 21 | ヘルプ AI に渡すナレッジベース本文。回答言語に合わせて訳すか、英語版を別に持つかの判断が要る |
| `src/lib/prompts/buildPrompt.ts` | 18 | プロンプト組み立て時の見出しと、上限超過時の警告文（警告文は画面に出る） |
| `src/lib/prompts/sections.ts` | 5 | **翻訳してはいけない。** AI に出力させる Markdown 見出し（`## 評価テーブル` 等）で、パーサがこの文字列で結果を読む |

## E. 法務・案内文（2 ファイル / 45 件）

翻訳の可否に法的な判断が要る。

| ファイル | 件数 | 内容 |
| --- | ---: | --- |
| `src/lib/legal/disclaimer.ts` | 29 | 免責事項 4 条項の全文とテロップ。**翻訳には法務レビューが要る**（EULA 3 条項は辞書化済み） |
| `src/lib/ui/tooltipText.ts` | 16 | ツールチップの初心者向け案内。投資判断に触れる文言を含むため訳し方に注意 |

## F. Rust 側のメッセージ（26 ファイル / 288 件）

**現状フロントへ日本語のまま返る。** `AppError` は文字列を返すだけで、
言語を切り替えても英語にならない。対応するなら次のどちらかを決める必要がある。

1. Rust はエラー**コード**を返し、フロントの辞書で文言を組み立てる（推奨。訳が 1 か所に集まる）
2. Rust 側にも辞書を持ち、`locale` を毎回渡す（Rust とフロントで訳が二重管理になる）

| ファイル | 件数 | 内容 |
| --- | ---: | --- |
| `src-tauri/src/yahoo.rs` | 50 | Yahoo Finance の指標名 36 項目（「時価総額」「PER」等）と取得失敗メッセージ |
| `src-tauri/src/prompts/mod.rs` | 39 | **秘匿プロンプトの日本語**。英語出力時は `language_directive` で指示を足す設計なので、原則そのまま |
| `src-tauri/src/chats.rs` | 22 | チャット履歴の既定タイトル・DB エラーメッセージ |
| `src-tauri/src/personas.rs` | 21 | 既定の AI 役割（システムプロンプト）のシード本文 |
| `src-tauri/src/portfolios.rs` | 21 | 既定ポートフォリオ名と DB エラーメッセージ |
| `src-tauri/src/analyses.rs` | 15 | 分析結果の保存・読み出しのエラーメッセージ |
| `src-tauri/src/cloud/oauth.rs` | 15 | Google 連携のエラーと、ブラウザに返す完了ページの文言 |
| `src-tauri/src/cloud/drive.rs` | 14 | Drive API のエラーメッセージ（認証切れ・アクセス拒否等） |
| `src-tauri/src/quarterly.rs` | 12 | 四半期推移のラベルと取得エラー |
| `src-tauri/src/cloud/mod.rs` | 12 | バックアップ・復元のエラーメッセージ |
| `src-tauri/src/commands.rs` | 9 | コマンド層の入力チェックエラー |
| `src-tauri/src/candidates.rs` | 7 | 検討中銘柄の DB エラー |
| `src-tauri/src/shortcuts.rs` | 7 | ショートカット保存の DB エラー |
| `src-tauri/src/documents.rs` | 6 | 資料ステージングのファイル入出力エラー |
| `src-tauri/src/cloud/bundle.rs` | 6 | バックアップ束の検証エラー（形式違い・サイズ不一致等） |
| `src-tauri/src/llm/openai.rs` | 6 | OpenAI 系 API のエラー整形 |
| `src-tauri/src/license.rs` | 5 | ライセンス形式エラーと状態メッセージ |
| `src-tauri/src/settings.rs` | 5 | 設定の読み書きエラー・未設定プロバイダの案内 |
| `src-tauri/src/market/mod.rs` | 4 | データ取得元の表示名と未設定理由 |
| `src-tauri/src/edgar.rs` | 3 | SEC EDGAR の取得エラー |
| `src-tauri/src/error.rs` | 3 | 共通エラーの接頭辞（「通信に失敗しました」等） |
| `src-tauri/src/library.rs` | 2 | DB オープンのエラー |
| `src-tauri/src/exports.rs` | 1 | ファイル書き出しのエラー |
| `src-tauri/src/http.rs` | 1 | HTTP クライアント初期化のエラー |
| `src-tauri/src/lib.rs` | 1 | 起動失敗時の panic メッセージ（画面には出ない） |
| `src-tauri/src/llm/mod.rs` | 1 | LLM 共通層のエラー |

---

## 付録: 全文字列

行番号は生成時点のもの。`literal` は文字列リテラル、`jsx` は JSX のテキストノード。

### `src/App.tsx` — A（12 件）

```
  116 literal 最後のパネルは最小化できません
  142 literal 設定を読み込めていません。
  277 literal マイポートフォリオ画面では最小化できません
  286 literal マイポートフォリオ画面では最小化できません
  474 literal 比較: ${tickers.join(" / ")}
  487 literal ワークスペース
  630 literal 一次資料が添付されていません
  632 literal 現在分析のベースとなる情報は、1. 財務指標 2. SEC資料 のみです。\n
  633 literal 他に一次資料（決算説明会資料など）があれば、読み込ませてから分析すると
  634 literal より詳細な分析結果が得られます。
  636 literal このまま分析する
  637 literal 戻る
```

### `src/components/AnalysisPanel.tsx` — A（12 件）

```
   40 literal 未実行
   41 literal 資料を収集中…
   42 literal 分析中…
   43 literal 完了
   44 literal 失敗
   83 literal （${new Date(run.savedAtMs).toLocaleString("ja-JP")} に保存した結果を復元）
  128 literal この分析をマイポートフォリオのリストに残す
  140 literal この銘柄の分析結果を削除（保存済みキャッシュも消えます）
  162 literal 先に銘柄を分析してください
  208 jsx     設定を開く
  234 jsx     SEC 提出書類と一時保存資料を読み込んでいます…
  241 jsx     評価テーブル
```

### `src/components/AnalystRoleMenu.tsx` — A（3 件）

```
   35 literal 分析の役割を選ぶ
   39 literal 役割
   49 jsx     分析の役割（企業タイプに合わせて選ぶ）
```

### `src/components/CandidateImportModal.tsx` — A（7 件）

```
   12 literal AAPL|Apple|Phone
NVDA|NVIDIA|AI Chip
7203.T|トヨタ自動車|自動車
# 先頭が # の行はメモとして無視されます
   49 literal 検討中銘柄の追加
   68 literal ${result.items.length} 件を追加
   84 literal 検討中銘柄の入力
   75 jsx     パイプ区切りで ティッカー|社名|ジャンル を入力してください（複数行可）
   89 jsx     社名・ジャンルは省略できます（`NVDA` だけでも可）。全角の｜でも区切れます。 すでに登録済みのティッカーは社名・ジャンルが上書きされます。
  101 jsx     取り込める行がありません。
```

### `src/components/CandidateStocksPanel.tsx` — A（19 件）

```
   87 literal この銘柄を分析
   93 literal 「${portfolio.name}」に追加
   97 literal ${candidate.ticker} を「${portfolio.name}」に追加しました
  102 literal 削除
  116 literal ドラッグして高さを変更
  145 literal パイプ区切り、またはティッカーの羅列からまとめて追加
  154 literal 検討中銘柄を開く
  154 literal 検討中銘柄を折りたたむ
  155 literal 開く
  155 literal 折りたたむ
  195 literal ${candidate.ticker} を比較対象にする
  196 literal チェックした銘柄を横並びで比較できます
  202 literal : ""}\nクリックで分析 / 右クリックでメニュー
  221 literal ${candidate.ticker} を削除
  222 literal この銘柄を削除
  174 jsx     まだ登録がありません。
  176 jsx     「＋ 追加」から
  178 jsx     ティッカー|社名|ジャンル
  180 jsx     を貼り付けてください。
```

### `src/components/ChatHistoryItem.tsx` — A（15 件）

```
   25 literal たった今
   26 literal ${minutes} 分前
   29 literal ${hours} 時間前
   32 literal 昨日
   33 literal ${days} 日前
   97 literal チャットのタイトル
  105 literal ${session.title}\nダブルクリックで名前を変更
  129 literal ${session.title} の名前を変更
  130 literal 名前を変更
  140 literal ${session.title} をアーカイブから戻す
  141 literal ${session.title} をアーカイブ
  145 literal アーカイブから戻す
  146 literal アーカイブへ移動（削除はされません）
  159 literal ${session.title} を削除
  160 literal このチャットを削除
```

### `src/components/ChatPanel.tsx` — A（12 件）

```
  270 literal 設定が不足しています
  299 literal ${t("chat.attach")}（この会話でのみ使う使い捨ての資料です）
  316 literal ${sendKeyLabel} で送信 / Enter で改行（手動で縦にも広げられます）
  319 literal 長文もどうぞ。${sendKeyLabel} で送信 / Enter で改行
  320 literal APIキーを設定すると送信できます
  332 literal ${sendKeyLabel} でも送信できます
  348 literal ${item.name}
${item.charCount.toLocaleString()} 文字 / 概算 ${item.tokenEstimate.toLocaleString()} トークン
  357 literal ${item.name} を外す
  215 jsx     銘柄や決算資料について質問すると、ここに会話が表示されます。 会話は自動で保存され、左のサイドバーから開き直せます。
  220 jsx     APIキーが未設定です。
  226 jsx     設定を開く
  275 jsx     設定を開く
```

### `src/components/CloudSyncSettings.tsx` — A（30 件）

```
   62 literal ${result.included.join(" / ")} を ${formatBytes(result.sizeBytes)} でアップロードしました。
   72 literal ${result.restored.join(" / ")} を復元しました。反映するにはアプリを再起動してください。
   95 literal Google Drive と連携中
   95 literal 未連携
  155 literal 変更する場合のみ入力
  198 literal ブラウザで許可してください…
  198 literal 🌐 Google アカウントで連携
  208 literal アップロード中…
  208 literal ☁️ クラウドへバックアップ
  217 literal 復元中…
  217 literal 🔄 クラウドから復元
  248 literal クラウドから復元
  249 literal クラウド上のいちばん新しいバックアップで、このパソコンのデータを置き換えます。現在のデータは復元前に同じフォルダへ退避されます。実行しますか？
  250 literal 復元する
  106 jsx     （しばらく更新されていません）
  116 jsx     🔒 アクセスする範囲
  119 jsx     このアプリが要求するのは
  121 jsx     のみです。これは Google Drive 内の
  123 jsx     アプリ専用の隠し領域
  123 jsx     に限った権限で、あなたの写真・書類・他のファイルは
  125 jsx     一覧することすらできません
  126 jsx     バックアップに含めるのは分析結果・対話履歴・ポートフォリオのデータベースだけです。
  128 jsx     APIキーとライセンスキーは送信しません。
  135 jsx     Google OAuth クライアント ID（デスクトップアプリ）
  140 jsx     設定済み
  142 jsx     未設定
  172 jsx     Google Cloud Console →「APIとサービス」→「認証情報」→ OAuth クライアント ID を作成（種類は「デスクトップアプリ」）し、 Google Drive API を有効化してください。 クラ…
  188 jsx     連携を解除
  231 jsx     起動時に自動バックアップ
  232 jsx     アプリを起動したときに、その時点のデータを自動でアップロードします。 失敗しても起動は妨げません。古い世代は 10 件を超えたぶんから順に整理されます。
```

### `src/components/ComparePanel.tsx` — A（12 件）

```
  119 literal / 読み込み中…
  158 literal ${column.ticker} の分析タブを開く
  196 literal この項目でいちばん良い値
  103 jsx     左サイドバーの「検討中銘柄」で 2 銘柄以上にチェックを入れると、 ここで横並び比較できます。
  115 jsx     横並び比較
  127 jsx     分析データが未作成の銘柄があります
  142 jsx     サマリー比較
  150 jsx     指標
  209 jsx     緑色はその項目でいちばん良い値です（PER・D/E・EV/粗利 は低いほど良い扱い）。
  216 jsx     AI スコア比較（5 ブロック）
  245 jsx     平均 / 5
  248 jsx     未分析
```

### `src/components/CriterionScoreRow.tsx` — A（2 件）

```
   33 literal スコア ${row.score ?? "未判定"}
   45 literal 判定不能
```

### `src/components/DisclaimerTicker.tsx` — A（2 件）

```
   17 literal 免責事項の全文を表示
   18 literal クリックすると免責事項の全文が開きます
```

### `src/components/DisplaySettings.tsx` — A（3 件）

```
   54 literal 本文・パネル・サイドバーの文字サイズ（${MIN_FONT_SIZE}〜${MAX_FONT_SIZE}px）。メニューバー右端でも変更できます。
   75 literal 市場データ・分析結果・対話の配置を、既定の並びに戻します。
   21 jsx     表示に関する設定です。変更は即座に画面へ反映され、この端末に保存されます。
```

### `src/components/DocumentTray.tsx` — A（9 件）

```
   64 literal 抽出中
   64 literal 保存中
   88 literal 一時保存中の資料をすべて削除しますか？
   90 literal ⚠️ コンテキスト（一時保存資料）がリセットされます。\n
   91 literal 必要な場合は、事前に対話ウィンドウで AI に「引き継ぎ書」を書かせてから操作してください。\n\n
   92 literal 削除される資料: ${documents.length} 件（概算 ${totalTokens(documents).toLocaleString()} トークン）
   94 literal このままリセット
   37 jsx     一時保存中の資料
   77 jsx     一括クリア
```

### `src/components/ExportMenu.tsx` — A（4 件）

```
   46 literal CSV / Markdown / JSON で書き出す
   50 literal 書き出し中…
   60 jsx     書き出す形式
   78 jsx     ダウンロードフォルダに保存します。 同名のファイルがあれば連番を付けます。
```

### `src/components/FilingStatusBadge.tsx` — A（2 件）

```
   21 jsx     SEC 提出状況を確認中…
   33 jsx     SEC 資料: 確認失敗
```

### `src/components/FontSizeControl.tsx` — A（3 件）

```
   24 literal 文字サイズ ${size}px（${MIN_FONT_SIZE}〜${MAX_FONT_SIZE}px）
   31 literal 文字を小さく
   40 literal 文字を大きく
```

### `src/components/FreeTierLimitModal.tsx` — A（3 件）

```
   33 literal 🔒 無料版の分析上限（${FREE_TICKER_LIMIT}銘柄）に達しました
   43 jsx     あとで
   51 jsx     ライセンスキーを入力
```

### `src/components/HelpAssistant.tsx` — A（11 件）

```
  116 literal ヘルプアシスタント
  139 literal ヘルプを閉じる
  169 literal APIキーが未設定です。
  189 literal ヘルプAI
  236 literal 使い方を質問（${sendKeyLabel} で送信）
  236 literal APIキーを設定すると使えます
  122 jsx     ヘルプアシスタント
  150 jsx     このアプリの使い方について何でも聞いてください。 画面の見方、APIキーの設定、ショートカット、データ取得元の違いなどをご案内します。
  174 jsx     設定を開く
  215 jsx     はじめかたを見る
  223 jsx     欲しい機能がありませんか？開発者へリクエストを送る
```

### `src/components/LegalDisclaimerModal.tsx` — A（3 件）

```
   28 jsx     本ツールは投資助言を行うものではありません。投資判断は自己責任でお願いします。
   35 jsx     理解しました
   44 jsx     本ツールが表示するすべての情報は情報提供・リサーチ支援を目的としたものです。 投資助言ではなく、内容の正確性も保証されません。
```

### `src/components/LicenseSettings.tsx` — A（8 件）

```
   52 literal ライセンス有効
   52 literal 未認証
   89 literal 確認中…
   89 literal ライセンス有効化
   67 jsx     ライセンスキー（購入時にメールで届きます）
   99 jsx     このパソコンのライセンスを解除
  105 jsx     キーは OS のアプリ設定ディレクトリに保存され、画面にはマスク済みの文字列しか出ません。
  107 jsx     いまは形式の確認のみを行っています（オフライン）。 発行元サーバーでの照会はこの後の対応です。
```

### `src/components/MarketProviderSettings.tsx` — A（23 件）

```
   28 literal ※個人利用・デモ用。非公式取得のため将来の動作保証はありません
   32 literal ※安定・商用利用に推奨。無料枠あり/有料。別途 FMP 公式サイトにて API キーの取得が必要です
   34 literal FMP の APIキー
   39 literal ※無料枠あり/有料。別途 Alpha Vantage 公式サイトにて API キーの取得が必要です
   40 literal Alpha Vantage の APIキー
   67 literal データ取得元を切り替えられませんでした
   77 literal APIキーを保存しました
   79 literal APIキーを保存できませんでした
   88 literal 接続を確認できませんでした
  145 literal キー設定済み（${keyStatus.masked}）
  146 literal キー未設定
  172 literal 変更する場合のみ入力
  192 literal AAPL を 1 件引いて確認します
  195 literal 確認中…
  195 literal 接続テスト
  226 literal APIキーの削除
  227 literal 登録されているAPIキーを削除しますか？
  234 literal 削除できませんでした
   57 jsx     設定を読み込んでいます…
   96 jsx     株価・財務指標をどこから取得するかを選びます。 選んだ取得元は「分析」実行時の指標取得に使われます。
   99 jsx     四半期推移（4Q モメンタム）は SEC XBRL との突き合わせが必要なため、 取得元の選択にかかわらず Yahoo Finance ＋ SEC EDGAR を使います。
  132 jsx     推奨
  202 jsx     キーを削除
```

### `src/components/ModalShell.tsx` — A（3 件）

```
  143 literal ドラッグして位置を移動
  170 literal ${title}を閉じる
  161 jsx     中央に戻す
```

### `src/components/PanelHeader.tsx` — A（2 件）

```
   93 literal ${title} パネルを移動
   94 literal ドラッグして他のパネルと位置を入れ替え
```

### `src/components/PanelRestoreBar.tsx` — A（1 件）

```
   32 literal ${PANEL_TITLES[slots[slot]]} を元に戻す
```

### `src/components/PdfDropZone.tsx` — A（4 件）

```
   33 literal 対応形式: ${SUPPORTED_EXTENSIONS.join(" / ")}\n\n※ WebサイトのURLではなく、ローカルのファイルをドロップしてください。\n　 クリックするとファイル選択ダイアログが開きます…
   42 literal ここにファイルをドロップ
   42 literal 決算PDF・IR資料をドロップ
   46 jsx     ※ URL ではなくローカルのファイル
```

### `src/components/PortfolioHeatmap.tsx` — A（6 件）

```
   74 literal ${row.ticker} の四半期タイムラインを開く
   92 literal この期の分析はありません
   33 jsx     まだ分析の履歴がありません。
   35 jsx     銘柄を分析すると、実行のたびにここへ蓄積されます。
   45 jsx     スコア推移（銘柄 × 四半期）
  117 jsx     銘柄名またはセルをクリックすると、その銘柄の四半期タイムラインを開きます。
```

### `src/components/PortfolioHistoryPanel.tsx` — A（21 件）

```
   87 literal ${ticker} の ${sections.length} 期分を対話へ転送しました
  125 literal 本文を取得できませんでした。
  127 literal 分析ログを開けませんでした
  137 literal 分析ログをコピーしました
  139 literal コピーできませんでした
  145 literal 以下は ${ticker} の過去の分析（${label}）です。この内容について質問します。\n\n---\n${text}\n---\n\n
  147 literal 対話ウィンドウに引用しました
  155 literal ${archives.length} 銘柄
  165 literal 縦分割（左右に並べる）
  178 literal 横分割（上下に並べる）
  228 literal 過去全四半期の分析を構造化テキストとして対話へ送る
  231 literal 転送中…
  231 literal 全期の分析データを対話へ転送
  278 literal 開く
  202 jsx     ← 一覧へ戻る
  288 jsx     ブロック別スコア
  306 jsx     主要数値
  333 jsx     適合・強み
  348 jsx     基準未達・リスク
  375 jsx     コピー
  386 jsx     対話へ引用
```

### `src/components/PortfolioPanel.tsx` — A（24 件）

```
  111 literal 名前を変更
  116 literal リストを削除
  144 literal ${ticker} を比較対象にする
  154 literal ${ticker} の分析アーカイブ（${entries.entries.length} 件）
  177 literal ${entries.entries.length}件
  184 literal ${ticker} を分析
  185 literal この銘柄を分析する
  195 literal ${ticker} をリストから外す
  196 literal このリストから外す（分析結果は消えません）
  244 literal 新しいリストを作る
  283 literal リスト名
  292 literal クリックで開閉 / 右クリックでメニュー
  310 literal ${portfolio.name} の名前を変更
  311 literal 名前を変更
  374 literal このリストを削除しますか？
  376 literal 「${deleting?.name ?? ""}」と、その中の ${deleting?.tickers.length ?? 0} 銘柄の登録が消えます。\n
  377 literal 分析結果そのものは消えません（未分類として残ります）。
  165 jsx     未分析
  208 jsx     分析の履歴がありません。
  254 jsx     リストがありません。
  256 jsx     「＋ 新規リスト作成」から
  258 jsx     作ってください。
  323 jsx     銘柄がありません。「検討中銘柄」や分析結果から追加できます。
  348 jsx     アーカイブを読み込み中…
```

### `src/components/PromptLibraryMenu.tsx` — A（4 件）

```
   44 literal 役割: 既定
   55 literal 既定（汎用アナリスト）
   50 jsx     AI の役割を選ぶ
   82 jsx     役割を追加・編集…
```

### `src/components/PromptLibraryModal.tsx` — A（14 件）

```
   54 literal AI の役割ライブラリ
   95 literal ${prompt.title} を削除
   96 literal この役割を削除
  110 literal 役割を編集
  110 literal 新しい役割
  120 literal 例: 高成長・グロース株アナリスト
  134 literal あなたは〜のアナリストです。〜を重視して評価してください。
  155 literal 更新
  163 literal この役割を削除しますか？
  164 literal 「${deleting?.title ?? ""}」を削除します。\nこの操作は取り消せません。
   70 jsx     新規
  114 jsx     名前
  126 jsx     内容（システムプロンプト。AI の立場・重視する観点・禁止事項を書く）
  145 jsx     編集をやめる
```

### `src/components/ProviderMenu.tsx` — A（7 件）

```
   35 literal （名称未設定）
   45 literal プロバイダを切り替えられませんでした
   55 literal 送信可能
   92 literal 送信可能
   27 jsx     設定を読み込み中…
   79 jsx     プロバイダを選択
  124 jsx     APIキー・モデルの設定を開く…
```

### `src/components/QuarterlyTrend.tsx` — A（8 件）

```
   25 literal 売上高
   31 literal 純利益
   37 literal 純利益率
  187 literal 売上 YoY: ${previousYoy.toFixed(1)}% → ${latestYoy.toFixed(1)}%
  194 literal ⏫ 成長が加速
  194 literal ⏬ 成長が減速
   92 jsx     四半期推移（直近4Q）
  179 jsx     モメンタム判定不能
```

### `src/components/SaveToPortfolioModal.tsx` — A（9 件）

```
   70 literal マイポートフォリオのどこに保存しますか？
   78 literal 追加 ${plan.add.length} / 削除 ${plan.remove.length}
   79 literal 変更はありません
  106 jsx     どのリストに並べるか
  106 jsx     だけです。
  111 jsx     リストがまだありません。
  117 jsx     新しいリストを作る
  137 jsx     登録済み
  151 jsx     新しいリストを作る
```

### `src/components/SettingsModal.tsx` — A（33 件）

```
  171 literal 変更する場合のみ入力
  188 literal ライセンスキーは OS のアプリ設定ディレクトリに保存されます。
  190 literal アクセスするのは Google Drive のアプリ専用領域のみです。個人ファイルには触れません。
  194 literal 閾値の変更は即座に保存され、次回の分析から反映されます。
  196 literal データ取得元の変更とキーは即座に保存されます。
  198 literal 保存しました（${savedAt}）
  199 literal APIキーは OS のアプリ設定ディレクトリに保存され、画面には表示されません。
  276 literal （名称未設定）
  285 literal 送信可能
  326 literal ${p.label} のモデル名
  390 literal 識別ラベル（例: DeepSeek）
  391 literal 識別ラベル
  397 literal ${c.label} を削除
  398 literal このプロバイダーを削除（APIキーも破棄されます）
  413 literal ${draft.label} のモデル名
  418 literal 例: deepseek-chat
  477 literal APIキーの削除
  478 literal 登録されているAPIキーを削除しますか？
  157 jsx     削除
  162 jsx     未設定
  263 jsx     設定を読み込んでいます…
  268 jsx     使用するプロバイダ
  307 jsx     APIキーとモデル
  322 jsx     モデル名（候補から選択、または手入力）
  338 jsx     Claude Opus 5 では
  341 jsx     で制御しています。
  351 jsx     OpenAI互換 API（DeepSeek / Moonshot など）
  359 jsx     プロバイダーを追加
  365 jsx     OpenAI互換の API はまだ登録されていません。
  367 jsx     「プロバイダーを追加」から Base URL とキーを設定してください。
  409 jsx     モデル名（候補から選択、または手入力）
  424 jsx     Base URL（末尾の /chat/completions は不要）
  450 jsx     User-Agent（「アプリ名 メールアドレス」形式。SEC が必須としています）
```

### `src/components/ShortcutSettings.tsx` — A（9 件）

```
  115 literal キーを押す…
  124 literal 中止
  124 literal 変更
  130 literal ${def.label} を既定に戻す
  131 literal 既定に戻す
   63 jsx     「変更」を押してから、割り当てたいキーを押してください（Esc で中止）。 入力欄にカーソルがあるときは、文字入力を優先するため発火しません。
   77 jsx     すべて既定に戻す
   98 jsx     入力欄で有効
  143 jsx     同じキーが複数のアクションに割り当てられています。 先に定義されたほうだけが動作します。
```

### `src/components/Sidebar.tsx` — A（14 件）

```
  181 literal サイドバーを開く (Ctrl+B)
  208 literal サイドバーの幅を変更
  214 literal ドラッグして幅を変更（${MIN_SIDEBAR_WIDTH}〜${MAX_SIDEBAR_WIDTH}px）
  255 literal サイドバーを閉じる (Ctrl+B)
  275 literal 履歴に戻る
  275 literal アーカイブを見る（${archived.length} 件）
  345 literal このチャットを削除しますか？
  347 literal 「${deleting?.title ?? ""}」と、その中の ${deleting?.messageCount ?? 0} 件のメッセージが削除されます。\n
  348 literal 残しておきたい場合は、削除ではなく 📁 アーカイブを使ってください。\n
  349 literal この操作は取り消せません。
  298 jsx     残しておきたい会話は
  300 jsx     📁 ボタンでここへ移せます。
  306 jsx     対話ウィンドウで質問すると、
  308 jsx     自動でここに保存されます。
```

### `src/components/StagedFileChip.tsx` — A（6 件）

```
   43 literal ${doc.originalName}\n${doc.charCount.toLocaleString()} 文字 / 概算 ${doc.tokenEstimate.toLocaleString()} トークン\nクリッ…
   46 literal 一時保存中
   58 literal 表示名
   79 literal ${doc.displayName} の名前を変更
   80 literal 名前を変更
   90 literal ${doc.displayName} を削除
```

### `src/components/TabBar.tsx` — A（2 件）

```
   51 literal ${tab.title} を閉じる
   71 literal 新しいタブ (Ctrl+T)
```

### `src/components/ThresholdSettings.tsx` — A（12 件）

```
   50 literal 閾値を保存できませんでした
  150 literal ${def.label} のスライダー
  161 literal ${def.label} の数値
  174 literal すべて既定値です
  175 literal ${changed.length} 項目を既定から変更しています
  182 literal AI に送る文面を見る
  182 literal 送信文を隠す
   86 jsx     💡 使い方がわからない場合や迷った場合は、デフォルト（既定値）のままで問題ありません。 プロの基準値が自動適用されます
   98 jsx     AI が合格/不合格を判定する基準です。設定した数値は
  100 jsx     そのままシステムプロンプトに埋め込まれ
  100 jsx     、 各項目の根拠欄に「基準に対して合格か」が明記されます。
  108 jsx     既定値に戻す（デフォルト復元）
```

### `src/components/ToastHost.tsx` — A（1 件）

```
   47 literal 通知を閉じる
```

### `src/components/TokenMeter.tsx` — A（2 件）

```
   22 literal ${usage.label}｜概算 ${tokens.toLocaleString()} / 上限 ${usage.limit.toLocaleString()} トークン（${(usage.ratio * 100).t…
   39 literal トークン消費量
```

### `src/components/WelcomeTour.tsx` — A（29 件）

```
   30 literal ようこそ、${appName()} へ
   32 literal 米国株・グローバル株のファンダメンタル分析を、AI と一緒に進めるためのアプリです。\n\n
   33 literal 取得した財務データと、あなたが読み込ませた決算資料をまとめて AI に渡し、\n
   34 literal 20 項目の評価を作ります。まずは 4 ステップだけ確認しましょう。
   37 literal 1. まず APIキーを登録する
   39 literal AI を動かすには、OpenAI / Anthropic / Gemini のいずれかの APIキーが必要です。\n
   40 literal 上部の「AI設定」ボタンから登録してください。\n\n
   41 literal キーは OS のアプリ設定ディレクトリに保存され、画面にはマスク済みの文字列しか出ません。\n
   42 literal SEC EDGAR を使うには「アプリ名 メールアドレス」形式の User-Agent も必要です。
   43 literal 設定を開く
   46 literal 2. 銘柄を調べる
   48 literal 上部の入力欄にティッカー（例: NVDA、7203.T）を入れて「分析」を押すと、\n
   49 literal 株価・財務指標・四半期推移・SEC の提出状況を取得します。\n\n
   50 literal 左サイドバー下部の「検討中銘柄」には、\n
   51 literal AAPL|Apple|Phone のような一覧をそのまま貼り付けて一括登録できます。
   54 literal 3. 一次資料を読み込ませる
   56 literal 決算説明会資料や 10-K の PDF / DOCX / PPTX をドロップゾーンに入れてください。\n
   57 literal 資料があるほど分析の精度が上がります。\n\n
   58 literal 長い資料は自動で圧縮されますが、要約ではなく原文抽出なので事実の改変は起きません。
   61 literal 4. 分析して、迷ったら聞く
   63 literal 分析結果パネルの「AI分析を実行」で 20 項目の評価が生成されます。\n
   64 literal 合格ラインの数値は設定の「分析ルール・閾値」タブで自由に変えられます。\n\n
   65 literal 操作で迷ったら、画面右下の「? ヘルプ」からいつでも AI に質問できます。
   66 literal ヘルプを開く
   93 literal はじめかた
  132 literal はじめる
  132 literal 次へ
  115 jsx     スキップ
  123 jsx     戻る
```

### `src/components/WorkspacePanel.tsx` — A（6 件）

```
   52 jsx     タブがありません
   64 jsx     上部のフォームにティッカーを入力して「分析」を押すと、
   66 jsx     Yahoo Finance の主要指標と SEC の提出状況を取得します。
   69 jsx     例: AAPL / NVDA / 7203.T / ASML.AS
  141 jsx     再試行
  166 jsx     詳細指標を取得できませんでした。銘柄によっては Yahoo Finance が指標を提供していません。
```

### `src/lib/api/analysisStore.ts` — B（3 件）

```
  101 literal ${ticker}: 指標を一部取得できませんでした
  106 literal ${ticker} の株価データを取得できませんでした
  117 literal ${ticker} の SEC 提出状況を確認できませんでした
```

### `src/lib/api/sec.ts` — B（14 件）

```
   13 literal ブラウザで実行中のため SEC のデータを取得できません。`npm run tauri:dev` で起動してください。
   38 literal 未取得
   38 literal まだ確認していません。
   45 literal 取得不可
   46 literal SEC EDGAR に登録がありません。
   54 literal 要設定
   55 literal SEC の User-Agent が未設定です。
   63 literal 書類なし
   64 literal 10-K / 10-Q が見つかりませんでした。
   75 literal 準備完了
   76 literal 10-K (${status.latest10k?.filed}) と 10-Q (${status.latest10q?.filed}) を取得できます。
   83 literal 一部のみ
   85 literal 10-K (${status.latest10k?.filed}) のみ取得できます。
   86 literal 10-Q (${status.latest10q?.filed}) のみ取得できます。
```

### `src/lib/api/yahoo.ts` — B（1 件）

```
   12 literal ブラウザで実行中のため財務データを取得できません。`npm run tauri:dev` で起動してください。
```

### `src/lib/candidates/candidateStore.ts` — B（4 件）

```
   60 literal 検討中銘柄を読み込めませんでした
   73 literal 検討中銘柄を保存できませんでした
   83 literal 検討中銘柄を削除できませんでした
   91 literal 検討中銘柄を削除できませんでした
```

### `src/lib/candidates/parseCandidates.ts` — B（3 件）

```
   57 literal ティッカーが空です
   61 literal ティッカーとして扱えません: ${ticker}
   95 literal 項目が多すぎます（ティッカー|社名|ジャンル の 3 つまで）
```

### `src/lib/chat/chatAttachments.ts` — B（5 件）

```
   85 literal この形式は読み込めません
   86 literal ${file.name}（PDF / DOCX / PPTX / TXT / MD / HTML / CSV / JSON に対応）
  107 literal ${file.name} を読み込めませんでした
  142 literal <資料 name="${item.name}" 文字数="${item.charCount}">\n${item.text}\n</資料>
  146 literal 以下は今回の質問のために添付した一次資料です（${items.length} 件）。\n\n${sections}\n\n
```

### `src/lib/chat/chatStore.ts` — B（7 件）

```
   76 literal チャット履歴を読み込めませんでした
   96 literal チャットを読み込めませんでした
  117 literal チャットを作成できませんでした
  128 literal タイトルを変更できませんでした
  142 literal アーカイブ状態を変更できませんでした
  160 literal チャットを削除できませんでした
  198 literal メッセージを保存できませんでした
```

### `src/lib/cloud/cloudBackup.ts` — B（11 件）

```
   29 literal クライアント ID を入力してください。
   31 literal クライアント ID は ${CLIENT_ID_SUFFIX} で終わる形式です。Google Cloud Console で「デスクトップアプリ」として発行してください。
   46 literal まだバックアップしていません
   49 literal たった今
   51 literal たった今
   52 literal ${minutes} 分前
   55 literal ${hours} 時間前
   58 literal ${days} 日前
   91 literal 日時不明
   97 literal 復元できるデータがありませんでした
   98 literal ${restored.length} 件のデータを復元しました（${restored.join(" / ")}）
```

### `src/lib/cloud/cloudStore.ts` — B（12 件）

```
   69 literal クラウド同期の状態を確認できませんでした
   77 literal クライアント ID を保存しました
   80 literal クライアント ID を保存できませんでした
   89 literal Google Drive と連携しました
   92 literal Google Drive と連携できませんでした
  101 literal 連携を解除しました
  103 literal 連携を解除できませんでした
  111 literal 自動バックアップの設定を保存できませんでした
  120 literal クラウドへバックアップしました
  123 literal バックアップできませんでした
  142 literal 復元できませんでした
  151 literal バックアップ一覧を取得できませんでした
```

### `src/lib/compare/compareData.ts` — C（27 件）

```
   57 literal 株価
   58 literal 時価総額
   59 literal 売上成長率（YoY）
   60 literal 粗利率
   61 literal 営業利益率
   64 literal EV / 粗利
   65 literal PER（実績）
   66 literal 負債比率 (D/E)
   76 literal 成長性
   77 literal 財務生存性
   78 literal 経済性
   79 literal 競争優位性
   80 literal バリュエーション
  107 literal 現金・同等物
  108 literal 営業CF
  110 literal 営業CF黒字
  116 literal ${months.toFixed(1)} か月
  126 literal EV / 売上高
  127 literal 粗利率
  131 literal ${value.toFixed(1)} 倍
  141 literal 売上成長率（YoY）
  151 literal 時価総額
  153 literal 粗利率
  154 literal 営業利益率
  158 literal PER（実績）
  159 literal 負債比率 (D/E)
  198 literal ${name} の分析データが未作成です。先に単体分析を実行してください
```

### `src/lib/config/modelCatalog.ts` — C（21 件）

```
   17 literal 最新世代（temperature は既定値のみ）
   18 literal 汎用・マルチモーダル
   19 literal 低コスト・高速
   22 literal 最上位。長期のエージェント処理に強い
   23 literal 速度と知能のバランス
   24 literal 前世代の Opus
   25 literal 最速・最安
   28 literal 高性能
   29 literal 低コスト・高速
   38 literal 汎用
   39 literal 推論特化
   45 literal コンテキスト 8K
   46 literal コンテキスト 32K
   47 literal コンテキスト 128K
   53 literal OpenRouter 経由
   54 literal OpenRouter 経由
   55 literal OpenRouter 経由
   61 literal 汎用
   62 literal 高速
   68 literal ローカル（Ollama 等）
   69 literal ローカル（Ollama 等）
```

### `src/lib/config/providers.ts` — B（8 件）

```
   20 literal 例: gpt-5.6 / gpt-4o
   27 literal 例: claude-opus-5 / claude-sonnet-5
   34 literal 例: gemini-2.5-pro / gemini-2.5-flash
   64 literal 設定を読み込めていません。
   67 literal APIキーが未設定です。
   72 literal プロバイダが見つかりません。
   73 literal Base URL が未設定です。
   74 literal モデル名が未設定です。
```

### `src/lib/config/settingsStore.ts` — B（1 件）

```
   56 literal ブラウザで実行中のため設定を読み込めません。`npm run tauri:dev` で起動してください。
```

### `src/lib/export/analysisRecord.ts` — C（17 件）

```
  107 literal 判定不能
  110 literal 良好
  113 literal 中立
  115 literal 懸念
  154 literal 希薄化率
  203 literal 株価
  207 literal 時価総額
  207 literal 時価総額
  208 literal 売上成長率（YoY）
  210 literal 粗利率
  210 literal 粗利率
  213 literal PER（実績）
  213 literal PER（実績）
  215 literal 負債比率 (D/E)
  215 literal 負債比率 (D/E)
  236 literal EV / 粗利
  261 literal 判定不能
```

### `src/lib/export/exportAnalysis.ts` — C（11 件）

```
   13 literal CSV（表計算ソフト用）
   14 literal Markdown（レポート）
   15 literal JSON（他ツール連携・AI再読込）
   29 literal ;
  }
  return text;
}
/** 行を CSV の 1 行にする。 */
export function csvRow(cells: string[]): string {
  return cel…
   80 literal ),
    "分析日時",
    "モデル",
  ];
  const rows = records.map((record) => {
    const metrics = new Map(record.ke…
   94 literal ,
      record.summary.averageScore === null ? "" : String(record.summary.averageScore),
      ...blockIds.map…
  109 literal ;
}
// ---------------------------------------------------------------- Markdown
/** レポート形式の Markdown にする。 *…
  129 literal )
    .join("\n");
  const bullets = (items: string[]) =>
    items.length === 0 ? "- （該当なし）" : items.map((i)…
  140 literal ／ モデル: ${record.model}
  170 literal ;
}
// ---------------------------------------------------------------- JSON
/** 他ツール連携・AI 再読み込み用の構造化データ。 */…
  228 literal ;
}
/** ファイル名に使えない文字を
```

### `src/lib/export/exportStore.ts` — B（6 件）

```
   21 literal エクスポートできません
   21 literal 書き出す分析結果がありません。
   29 literal エクスポートできません
   29 literal アプリ内でのみ利用できます。
   35 literal 書き出しました
   37 literal 書き出せませんでした
```

### `src/lib/i18n/locales.ts` — A（3 件）

```
   36 literal 日本語
   37 literal 日本語
   44 literal 英語
```

### `src/lib/legal/disclaimer.ts` — E（29 件）

```
   16 literal 免責事項・利用上の注意 (Legal Disclaimer)
   27 literal 投資助言の否定（非勧誘）
   29 literal 本ツール「${appName()}」（以下、本ツール）が提供・表示するすべての情報
   30 literal （数値データ、AIによる分析結果、スコアリング、生成テキスト等）は、
   31 literal 一般的な情報提供およびリサーチ支援のみを目的としており、
   32 literal 金融商品取引法に基づく投資助言、投資勧誘、
   33 literal または特定の有価証券・金融商品の売買を推奨するものではありません。
   36 literal AIの特性およびハルシネーション（誤情報生成）リスク
   38 literal 本ツールには大規模言語モデル（LLM）などのAI技術が使用されています。
   39 literal AIの性質上、事実と異なる情報や不正確な数値を生成する現象（ハルシネーション）、
   40 literal および同一の質問やプロンプトに対しても出力結果が毎回変動・矛盾する可能性があります。
   41 literal 本ツールが出力する情報の正確性、最新性、完全性、妥当性について、
   42 literal 開発者および配布者は一切保証いたしません。
   45 literal 投資リスクと自己責任の原則
   47 literal 株式投資および金融取引には、原本割れを含む重大な価格変動リスクや財務リスクが伴います。
   48 literal 本ツールの分析結果や提示されたデータに基づいて行われた投資判断、
   49 literal ポートフォリオ構築、売買注文等によって生じたあらゆる直接的・間接的損失、
   50 literal 機会損失、損害について、開発者、配布者、および権利者は
   51 literal 理由の如何を問わず一切の法的責任を負いません。
   52 literal すべての投資判断および売買決定は、ユーザーご自身の自己責任と判断において行ってください。
   55 literal データソースの仕様変更・中断
   57 literal 本ツールが参照・取得する外部データソース
   58 literal （Yahoo Finance、FMP、Alpha Vantage、SEC EDGAR等）の仕様変更、通信障害、
   59 literal データの欠落等により生じた情報の遅延や誤りについても、
   60 literal 本ツールおよび開発者は一切の責任を負いません。
   67 literal ⚠️【免責事項】本ツールが示す情報は情報提供のみを目的としており投資助言ではありません。
   68 literal AIはハルシネーション（誤情報）を起こす可能性があり、質問のたびに回答が異なる場合があります。
   69 literal 本ツールを利用した投資による損失・損害について開発者は一切の責任を負いません。
   70 literal 投資判断は必ず自己責任で行ってください。（クリックで全文を表示）
```

### `src/lib/legal/eulaStore.ts` — B（3 件）

```
   65 literal 免責事項の同意状態を確認できませんでした
   75 literal 同意を保存できませんでした
   91 literal 同意を撤回できませんでした
```

### `src/lib/license/freeTier.ts` — B（2 件）

```
  100 literal 無料版の分析枠を使い切りました
  101 literal 無料版であと ${result.remaining} 銘柄を分析できます
```

### `src/lib/license/freeTierStore.ts` — B（1 件）

```
   85 literal 利用状況を保存できませんでした
```

### `src/lib/license/licenseStore.ts` — B（5 件）

```
   13 literal ライセンス状態を確認しています…
   52 literal ライセンス状態を確認できませんでした
   60 literal ライセンスを有効化しました
   63 literal ライセンスを有効化できませんでした
   72 literal ライセンスを解除できませんでした
```

### `src/lib/llm/client.ts` — B（2 件）

```
   62 literal ブラウザで実行中のため LLM を呼び出せません。`npm run tauri:dev` で起動してください。
  110 literal LLM からの応答が完了しませんでした。
```

### `src/lib/parser/documentStore.ts` — B（9 件）

```
   61 literal 一時保存中の資料を読み込めませんでした
   76 literal ブラウザでは資料を保存できません
   77 literal `npm run tauri:dev` で起動すると一時保存が有効になります。
  101 literal 「${file.name}」を取り込めませんでした
  108 literal ${succeeded} 件の資料を一時保存しました
  116 literal 表示名を変更できませんでした
  124 literal 資料を削除できませんでした
  131 literal 一時保存中の資料をすべて破棄しました
  133 literal 資料を一括削除できませんでした
```

### `src/lib/parser/docx.ts` — B（1 件）

```
   13 literal DOCX の本文（word/document.xml）が見つかりませんでした。
```

### `src/lib/parser/extractText.ts` — B（2 件）

```
   57 literal 「${file.name}」は未対応の形式です。対応: ${SUPPORTED_EXTENSIONS.join(" / ")}
   65 literal 「${file.name}」からテキストを抽出できませんでした。画像だけの PDF や暗号化された PDF の可能性があります。
```

### `src/lib/parser/pptx.ts` — B（2 件）

```
   18 literal PPTX のスライド（ppt/slides/）が見つかりませんでした。
   33 literal \n[発表者ノート] ${notes}
```

### `src/lib/parser/tokenCount.ts` — B（3 件）

```
   50 literal 入力上限を超過
   52 literal 入力上限に接近
   53 literal 余裕あり
```

### `src/lib/portfolio/heatmap.ts` — B（3 件）

```
  143 literal ${ticker} の過去の分析データはまだありません。
  149 literal ### ${entry.label}（総合 ${
          entry.score === null ? "—" : entry.score.toFixed(1)
        } / 5・${new Dat…
  155 literal 以下は ${ticker} の過去 ${entries.length} 期分の分析データです。
時系列での変化（改善・悪化した点、想定が外れた点）を踏まえて回答してください。
<過去の分析データ ticker="${t…
```

### `src/lib/portfolio/portfolioStore.ts` — B（7 件）

```
   69 literal ポートフォリオを読み込めませんでした
   81 literal 分析アーカイブを読み込めませんでした
   92 literal リストを作成できませんでした
  100 literal リスト名を変更できませんでした
  108 literal リストを削除できませんでした
  116 literal 銘柄を追加できませんでした
  127 literal 銘柄を外せませんでした
```

### `src/lib/portfolio/saveTarget.ts` — B（4 件）

```
   74 literal ${plan.add.length} 件のリストに追加
   75 literal ${plan.remove.length} 件から削除
   76 literal 変更はありません
   77 literal ${ticker.toUpperCase()} を${parts.join("・")}しました
```

### `src/lib/prompts/analysisRunner.ts` — B（18 件）

```
  126 literal ブラウザでは分析を実行できません
  127 literal `npm run tauri:dev` で起動してください。
  174 literal SEC 提出書類を取得できませんでした
  175 literal ${e instanceof Error ? e.message : String(e)} 財務指標と添付資料のみで分析します。
  203 literal 財務指標(YF)
  205 literal 四半期推移 ${options.quarterly.quarters.length}Q
  207 literal SEC開示書類 ${filing.form} (${filing.filed})
  212 literal 添付資料: ${names[0]}
  213 literal 添付資料: ${names[0]} ほか (${documents.length}件)
  216 literal データ元なし
  286 literal 分析結果を保存できませんでした
  287 literal 画面には表示されていますが、次回起動時には復元されません。${
            e instanceof Error ? e.message : String(e)
          }
  295 literal 分析を中断しました
  295 literal 途中までの結果は保存しています。
  297 literal ${ticker} の分析が完了しました
  307 literal ${ticker} の分析に失敗しました
  372 literal ${ticker} の分析結果を削除しました
  374 literal 分析結果を削除できませんでした
```

### `src/lib/prompts/analystRoleStore.ts` — B（2 件）

```
   87 literal 分析の役割を読み込めませんでした
  114 literal （アプリ内でのみ表示できます）
```

### `src/lib/prompts/buildPrompt.ts` — D（18 件）

```
   90 literal トークン上限が小さすぎるため、財務指標のみで分析します。設定で上限を引き上げてください。
  153 literal 資料全体
  162 literal 入力トークン上限に収めるため、資料全体をさらに圧縮しました。設定で上限を引き上げると、より多くの原文を渡せます。
  170 literal # 分析対象
ティッカー: ${ticker}
以下の資料に基づいて、20項目の評価を行ってください。
  179 literal ## 財務指標（Yahoo Finance）
取得できませんでした。指標に依存する項目はスコア 0（判定不能）としてください。
  185 literal ## 財務指標（Yahoo Finance / 取得: ${new Date(f.fetchedAtMs).toLocaleString("ja-JP")}）
  187 literal 銘柄名: ${f.name}
  188 literal 取引所: ${f.exchange || "不明"}
  189 literal 現在値: ${f.priceDisplay}${
      f.changePercent !== null ?
  215 literal ## SEC 提出書類（${filing.form} / 提出 ${filing.filed} / 対象期間 ${filing.period}）
出典: ${filing.url}
  235 literal ## 添付された一次資料
  296 literal ${form} 本文
  315 literal ${form} の本文が長いため、分析価値の高い節（${kept}）を抽出しました
  317 literal 。うち ${compressed} 節は文単位で重要箇所を選別しています（中間の一括カットはしていません）。
  336 literal ## 四半期推移（直近4四半期）
  338 literal 出典: ${series.source}
  340 literal | 四半期 | 期末 | 売上高 | 前四半期比 | 前年同期比 | 純利益 | 純利益率 | EPS実績 | EPS予想 | サプライズ |
  351 literal **モメンタム判定**: ${series.momentum.summary}
```

### `src/lib/prompts/condense.ts` — D（60 件）

```
   40 literal セグメント
   40 literal 事業別
   40 literal 地域別
   41 literal 質疑応答
   41 literal 質問
   41 literal 回答
   41 literal アナリスト
   42 literal ガイダンス
   42 literal 見通し
   42 literal 通期
   42 literal 業績予想
   48 literal 売上
   49 literal 営業利益
   50 literal 純利益
   50 literal 利益率
   50 literal 粗利
   51 literal 一株当たり
   52 literal キャッシュフロー
   52 literal フリーキャッシュ
   53 literal 成長率
   53 literal 前年同期
   53 literal 前四半期
   54 literal 受注
   54 literal 解約
   55 literal 設備投資
   55 literal 配当
   55 literal 自社株買い
   56 literal 在庫
   56 literal 顧客
   56 literal シェア
   62 literal 訴訟
   62 literal 規制
   63 literal 為替
   63 literal 競合
   63 literal 戦略
   64 literal 投資
   64 literal 買収
   64 literal 人員
   71 literal 将来見通しに関する注意
   71 literal 将来に関する記述
   72 literal 免責事項
   72 literal 本資料に含まれる
   72 literal いかなる保証
   72 literal 予告なく変更
   73 literal 無断転載
   73 literal 禁じられて
   73 literal 著作権
   74 literal 本プレゼンテーション
   74 literal このプレゼンテーション
   74 literal 注記事項
  124 literal 冒頭
  135 literal 冒頭
  168 literal （本文 ${sections.length + 1}）
  284 literal …（${dropped}文を省略）…
  292 literal …（${dropped}文を省略）…
  379 literal 「${label}」が長いため、セクション（見出し・ページ・Q&A）ごとに分析価値を採点し、
  380 literal 重要な文を原文のまま残す方式で ${Math.round((estimateTokens(result) / original) * 100)}% に圧縮しました
  381 literal （中間の一括カットはしていません）。
  385 literal 「${label}」の定型文セクション ${droppedLowValue} 件（免責事項・将来見通しの注意書き等）を除外しました。
  160 jsx     0); // 細かすぎると配分が効かないので、おおよそ 20 個にまとめる const groupSize = Math.max(1, Math.ceil(blocks.length / 20)); const sect…
```

### `src/lib/prompts/criteria.ts` — C（42 件）

```
   12 literal 事業
   12 literal 成長
   12 literal 収益性
   12 literal 財務
   12 literal 経営
   12 literal 総合
   24 literal 事業
   25 literal 事業モデルの明瞭さ
   29 literal 事業
   30 literal 競争優位（モート）
   34 literal 成長
   35 literal TAM と成長余地
   39 literal 成長
   40 literal 売上成長のモメンタム
   44 literal 収益性
   45 literal 粗利率の水準と推移
   49 literal 収益性
   50 literal 営業レバレッジ・利益率の推移
   54 literal 収益性
   55 literal フリーキャッシュフロー創出力
   59 literal 財務
   60 literal バランスシートの健全性
   64 literal 財務
   65 literal ROIC / ROE の質
   69 literal 財務
   70 literal 設備投資の効率
   74 literal 経営
   75 literal 資本配分の巧拙
   79 literal 経営
   80 literal 経営陣の質・実行力
   84 literal 経営
   85 literal ガイダンスの信頼性
   89 literal 事業
   90 literal セグメント別トレンド
   95 literal 顧客・取引先の集中リスク
  100 literal 規制・訴訟リスク
  105 literal 景気感応度
  109 literal 総合
  110 literal バリュエーションの妥当性
  114 literal 総合
  115 literal 短期カタリスト
  119 literal 総合
```

### `src/lib/prompts/helpKnowledge.ts` — D（21 件）

```
   14 literal # ${appName()} とは
米国株・グローバル株のファンダメンタル分析デスクトップアプリ（Tauri + React）。
Yahoo Finance / SEC EDGAR から取得したデータと、ユーザーが読み込…
   61 literal # データ取得元（設定画面の「データ取得元」タブ）
| 取得元 | キー | 位置づけ |
| --- | --- | --- |
| Yahoo Finance | 不要 | 非公式取得。個人利用・デモ向けで、将来の動…
   74 literal # 回答のしかた
- 日本語で、丁寧かつ簡潔に答える
- 手順は「どのボタンを押すか」まで具体的に示す
- **このアプリに無い機能を、あるかのように答えない。** 分からないことは分からないと言う
- 投資判断そのもの…
   86 literal | 操作 | キー | 説明 |
   91 literal # 現在の設定\n読み込めていません。
   99 literal # 現在の設定
  100 literal - LLM プロバイダ: ${settings.provider}
  101 literal - APIキー登録済み: ${configured.length > 0 ? configured.join(", ") : "なし"}
  102 literal - データ取得元: ${market?.label ?? settings.marketProvider}
  103 literal （未設定: ${market.reason ?? ""}）
  104 literal - SEC User-Agent: ${settings.secUserAgent ? "設定済み" : "未設定（SEC の取得ができません）"}
  115 literal あなたは ${appName()} の操作案内AIアシスタントです。
  116 literal アプリの機能、ショートカットキー一覧、APIキーの設定方法、
  117 literal データプロバイダーの違いなどを丁寧に解説してください。
  120 literal # ショートカットキー（現在の割り当て）\n${shortcutTable(bindings, mac)}\n
  121 literal 変更は設定画面の「ショートカット」タブから行う。
  122 literal 「変更」を押してから割り当てたいキーを押す（Esc で中止、✕ で既定に戻す）。
  130 literal ショートカットキーはどこで変える？
  131 literal FMP のキーはどう入手する？
  132 literal 分析のコツは？
  133 literal 検討中銘柄はどう登録する？
```

### `src/lib/prompts/promptLibrary.ts` — B（5 件）

```
   16 literal あなたは米国株・グローバル株のファンダメンタル分析を支援するアシスタントです。
   17 literal 根拠を明示し、断定できない点は不確実であると述べてください。回答は日本語で行ってください。
  101 literal 役割ライブラリを読み込めませんでした
  114 literal 役割を保存できませんでした
  123 literal 役割を削除できませんでした
```

### `src/lib/prompts/sections.ts` — D（5 件）

```
   11 literal ## 評価テーブル
   12 literal ## 強み
   13 literal ## リスク
   14 literal ## バリュエーション所見
   15 literal ## 総合投資判断
```

### `src/lib/prompts/thresholds.ts` — C（24 件）

```
   35 literal 売上高成長率（YoY）
   42 literal 直近四半期の前年同期比。成長株を探すなら高め、安定株なら低めに
   46 literal 営業利益率
   53 literal 本業の稼ぐ力。業種によって水準が大きく違う
   57 literal ROE（自己資本利益率）
   64 literal 株主資本をどれだけ効率よく利益に変えているか
   68 literal キャッシュランウェイ
   69 literal か月
   75 literal 手元現金でバーンレートを何か月まかなえるか。赤字先行の中小型株で重要
   79 literal FCF マージン
   86 literal フリーCF ÷ 売上高。会計上の利益より現金の創出力を見る
   90 literal PER（株価収益率）
   91 literal 倍
   97 literal 割高さの目安。成長率とセットで見る
  101 literal PBR（株価純資産倍率）
  102 literal 倍
  108 literal 資産面からの割高さ。資産の軽い業種では高くなりやすい
  112 literal D/E（負債資本倍率）
  113 literal 倍
  119 literal 財務の安全性。高いほど借入依存
  123 literal 配当利回り
  130 literal インカム重視なら引き上げる。0 のままなら判定に使わない
  134 literal 配当性向
  141 literal 利益に対する配当の割合。高いほど減配余地が小さい
```

### `src/lib/sampleData.ts` — A（1 件）

```
   10 literal ワークスペース
```

### `src/lib/ui/shortcutStore.ts` — B（15 件）

```
   45 literal 新しい会話を作って履歴に追加する
   50 literal 検討中銘柄を追加
   51 literal パイプ区切りの一括インポートを開く
   56 literal ティッカー入力へ移動
   57 literal 上部の入力欄にカーソルを移す
   63 literal 開いている銘柄の 20 項目評価を開始する
   68 literal サイドバーの開閉
   69 literal チャット履歴と検討中銘柄の表示を切り替える
   74 literal 設定を開く
   75 literal APIキー・モデル・ショートカットの設定
   80 literal チャットを送信
   81 literal 入力欄で押すと送信する（Enter は改行）
  154 literal ショートカット設定を読み込めませんでした
  171 literal ショートカットを保存できませんでした
  179 literal ショートカットを戻せませんでした
```

### `src/lib/ui/tooltipHint.ts` — A（1 件）

```
   13 literal 💡 ツールチップは [設定] ➔ [表示] タブからオフにできます
```

### `src/lib/ui/tooltipText.ts` — E（16 件）

```
   11 literal 操作で迷ったらここをクリック！\n
   12 literal AIアシスタントが使い方を何でも案内します
   15 literal 💡 ヒント: 無理に全部覚える必要はありません。\n
   16 literal 一度設定をクリアして、よく使う操作だけを\n
   17 literal 好きなキーに割り当てるのがおすすめです
   20 literal 💡 提示される数値だけでなく、CAPEX（設備投資）過多による\n
   21 literal 業界全体の価格競争（泥沼化）リスクなど、\n
   22 literal 定性面も考慮して総合判断しましょう
   25 literal AIで出したティッカーリスト（AAPL|Apple|Phone など）を\n
   26 literal そのまま貼り付けて一括ストックできます
   29 literal 💡 米国株は `AAPL` や `NVDA`、\n
   30 literal 日本株は `7203.T`（トヨタ）や `9984.T` のように\n
   31 literal 末尾に `.T` を付けて入力してください
   34 literal AI が合格/不合格を判定する基準です。\n
   35 literal 成長株を探すなら成長率を高く、割安株なら PER を低く。\n
   36 literal 設定した数値はそのままプロンプトに埋め込まれます
```

### `src-tauri/src/analyses.rs` — F（15 件）

```
  データディレクトリを取得できません: {e}
  分析結果データベースを開けません: {e}
  分析結果テーブルを作成できません: {e}
  分析履歴テーブルを作成できません: {e}
  分析履歴を保存できません: {e}
  分析履歴を取得できません: {e}
  分析履歴を取得できません: {e}
  分析履歴を取得できません: {e}
  分析履歴を削除できません: {e}
  保存する分析結果が空です。
  分析結果を保存できません: {e}
  分析結果を読み出せません: {e}
  一覧を取得できません: {e}
  一覧を取得できません: {e}
  分析結果を削除できません: {e}
```

### `src-tauri/src/candidates.rs` — F（7 件）

```
  検討中銘柄テーブルを作成できません: {e}
  検討中銘柄を取得できません: {e}
  検討中銘柄を取得できません: {e}
  検討中銘柄を保存できません: {e}
  検討中銘柄を削除できません: {e}
  対象の銘柄が見つかりませんでした。
  検討中銘柄を削除できません: {e}
```

### `src-tauri/src/chats.rs` — F（22 件）

```
  データディレクトリを取得できません: {e}
  チャット履歴データベースを開けません: {e}
  チャット履歴テーブルを作成できません: {e}
  履歴を取得できません: {e}
  履歴を取得できません: {e}
  新しいチャット
  チャットを作成できません: {e}
  タイトルを空にはできません。
  タイトルを変更できません: {e}
  対象のチャットが見つかりませんでした。
  アーカイブ状態を変更できません: {e}
  対象のチャットが見つかりませんでした。
  チャットを削除できません: {e}
  対象のチャットが見つかりませんでした。
  メッセージを取得できません: {e}
  メッセージを取得できません: {e}
  チャットを参照できません: {e}
  対象のチャットが見つかりませんでした。
  メッセージを保存できません: {e}
  新しいチャット
  チャットを更新できません: {e}
  新しいチャット
```

### `src-tauri/src/cloud/bundle.rs` — F（6 件）

```
  バックアップを組み立てられません: {e}
  バックアップの形式が違います。StoQ が作成したバックアップか確認してください。
  このバックアップは新しい形式（v{}）です。アプリを更新してから復元してください。
  バックアップに復元できるデータがありません。
  {} の中身が壊れています。
  {} のサイズが合いません（記録 {} / 実際 {}）。転送中に壊れた可能性があります。
```

### `src-tauri/src/cloud/drive.rs` — F（14 件）

```
  Google の認証が切れています。設定の「クラウド同期」で連携し直してください。
  Google Drive へのアクセスが拒否されました。連携時にアクセスを許可したかご確認ください。
  Google Drive へ接続できません: {e}
  応答を読めません: {e}
  バックアップ一覧を取得できません（{status}）
  応答を解釈できません: {e}
  Google Drive へ接続できません: {e}
  応答を読めません: {e}
  バックアップをアップロードできません（{status}）
  応答を解釈できません: {e}
  アップロードの応答に ID がありません。
  Google Drive へ接続できません: {e}
  バックアップを取得できません（{status}）
  バックアップを読めません: {e}
```

### `src-tauri/src/cloud/mod.rs` — F（12 件）

```
  Google の OAuth クライアント ID が未設定です。設定の「クラウド同期」で登録してください。
  Google と連携していません。「🌐 Google アカウントで連携」から接続してください。
  先に Google の OAuth クライアント ID を登録してください。\
             Google Cloud Console で「デスクトップアプリ」として発行できます。
  認証の待受に失敗しました: {e}
  Google の連携が完了しませんでした（{error}）。
  認証の応答が一致しませんでした。安全のため中止しました。もう一度お試しください。
  認可コードを受け取れませんでした。
  更新用トークンを受け取れませんでした。Google アカウントの権限を一度解除してから、もう一度お試しください。
  データディレクトリを取得できません: {e}
  バックアップできるデータがまだありません。分析を保存してからお試しください。
  クラウドにバックアップがありません。先にバックアップしてください。
  バックアップに復元できるデータがありません。
```

### `src-tauri/src/cloud/oauth.rs` — F（15 件）

```
  Google の認証に失敗しました: {detail}
  Google の応答にアクセストークンがありません。
  認証の受け口を用意できません: {e}
  受け口のポートを取得できません: {e}
  ja\"><head><meta charset=\"utf-8\">\
         <title>StoQ AI Analyzer</title></head>\
         <body style=\"f…
  受け口を設定できません: {e}
  連携をキャンセルしました
  ✅ Google Drive との連携が完了しました
  認証の応答を受け取れません: {e}
  認証がタイムアウトしました。ブラウザで許可の操作を行ってから、もう一度お試しください。
  ブラウザを開けません: {e}
  Google へ接続できません: {e}
  Google の応答を読めません: {e}
  Google の応答を解釈できません。
  {e}\n連携が取り消された可能性があります。設定の「クラウド同期」で連携し直してください。
```

### `src-tauri/src/commands.rs` — F（9 件）

```
  Phase 2 Step 1 — 設定 & LLM 接続基盤
  未知のプロバイダです: {provider}
  未知のデータ取得元です: {provider}
  未知のデータ取得元です: {provider}
  未知のプロバイダです: {provider}
  プロバイダ {id} が見つかりませんでした。
  ウィンドウタイトルが空です。
  ウィンドウが見つかりません。
  ウィンドウタイトルを変更できません: {e}
```

### `src-tauri/src/documents.rs` — F（6 件）

```
  データディレクトリを取得できません: {e}
  「{original_name}」からテキストを抽出できませんでした。画像だけの PDF や暗号化された PDF の可能性があります。
  資料の本文が見つかりませんでした。削除された可能性があります。
  表示名を空にはできません。
  対象の資料が見つかりませんでした。
  対象の資料が見つかりませんでした。
```

### `src-tauri/src/edgar.rs` — F（3 件）

```
  提出書類の一覧を取得できませんでした。
  直近の提出書類に 10-K / 10-Q が見つかりませんでした。
  \n\n…（本文が長いため以降を省略しました）
```

### `src-tauri/src/error.rs` — F（3 件）

```
  通信に失敗しました: {0}
  ファイル入出力に失敗しました: {0}
  JSON の解析に失敗しました: {0}
```

### `src-tauri/src/exports.rs` — F（1 件）

```
  保存先を決められません: {e}
```

### `src-tauri/src/http.rs` — F（1 件）

```
  HTTP クライアントの初期化に失敗しました: {e}
```

### `src-tauri/src/lib.rs` — F（1 件）

```
  Tauri アプリケーションの起動に失敗しました
```

### `src-tauri/src/library.rs` — F（2 件）

```
  データディレクトリを取得できません: {e}
  ライブラリデータベースを開けません: {e}
```

### `src-tauri/src/license.rs` — F（5 件）

```
  ライセンスキーが未登録です。購入時に発行されたキーを入力してください。
  開発者用マスターキーで有効化されています。
  ライセンスは有効です。
  ライセンスキーを入力してください。
  ライセンスキーの形式が違います。\
             XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX の形で入力してください。
```

### `src-tauri/src/llm/mod.rs` — F（1 件）

```
  送信するメッセージがありません。
```

### `src-tauri/src/llm/openai.rs` — F（6 件）

```
  OpenAI互換 API のパラメータを調整しましたが、リクエストが受け付けられませんでした。
  （APIキーが正しいか確認してください）
  （APIキーの権限、またはモデルへのアクセス権を確認してください）
  （モデル名または Base URL が正しいか確認してください）
  （レート制限に達しました。しばらく待って再試行してください）
  OpenAI互換 API エラー (HTTP {status}){hint}: {detail}
```

### `src-tauri/src/market/mod.rs` — F（4 件）

```
  {} の APIキーが未設定です。設定画面の「データ取得元」で登録してください。
  {} の株価を取得できました（{}）
  取得元を利用できません。
  取得元を利用できません。
```

### `src-tauri/src/personas.rs` — F（21 件）

```
  プロの株式ジャーナリスト
  あなたは経済メディアで企業取材を重ねてきた株式ジャーナリストです。\
         事実と意見を明確に分け、数値には必ず出典（資料名・期）を添えてください。\
         推測を述べるときは「推測」と明示し、断…
  高成長・グロース株アナリスト
  あなたは中小型グロース株の売上・利益成長率や競争優位性を分析するアナリストです。\
         市場規模（TAM）、成長率の持続性、参入障壁、経営陣の実行力を客観的に評価してください。\
         赤字継続…
  慎重な高配当アナリスト
  あなたはインカム投資を専門とする慎重派のアナリストです。\
         配当性向、フリーキャッシュフローによる配当カバー率、負債水準、\
         過去の減配履歴を重視して評価してください。\
      …
  プロンプトテーブルを作成できません: {e}
  UPDATE prompts SET title = ?1, body = ?2
         WHERE builtin = 1 AND title = 'テンバガー発掘アナリスト'
  既定の役割を更新できません: {e}
  プロンプトの初期化状態を確認できません: {e}
  既定の役割を追加できません: {e}
  プロンプトの初期化を記録できません: {e}
  役割を取得できません: {e}
  役割を取得できません: {e}
  役割の名前を入力してください。
  役割の内容（システムプロンプト）を入力してください。
  役割を更新できません: {e}
  対象の役割が見つかりませんでした。
  役割を追加できません: {e}
  役割を削除できません: {e}
  対象の役割が見つかりませんでした。
```

### `src-tauri/src/portfolios.rs` — F（21 件）

```
  メインポートフォリオ
  ポートフォリオテーブルを作成できません: {e}
  初期化状態を確認できません: {e}
  既定のリストを作成できません: {e}
  初期化を記録できません: {e}
  リストを取得できません: {e}
  リストを取得できません: {e}
  銘柄を取得できません: {e}
  銘柄を取得できません: {e}
  新しいリスト
  リストを作成できません: {e}
  リスト名を空にはできません。
  リスト名を変更できません: {e}
  対象のリストが見つかりませんでした。
  リストを削除できません: {e}
  対象のリストが見つかりませんでした。
  ティッカーが空です。
  リストを確認できません: {e}
  対象のリストが見つかりませんでした。
  銘柄を追加できません: {e}
  銘柄を外せません: {e}
```

### `src-tauri/src/prompts/mod.rs` — F（39 件）

```
  汎用ファンダメンタルアナリスト
  財務健全性・収益性・成長性をバランスよく評価します（既定）
  財務健全性
  収益性
  成長性
  標準バランススコア
  高成長・中小型グロースアナリスト
  中小型株の資金余力と成長率、希薄化リスクを重視して分析します
  Cash Runway（残存月数）
  売上成長率（YoY）
  完全希薄化後株式数
  確定受注の質
  メガスケーラー・大型優良株アナリスト
  大型株の FCF や資本配分、モートの持続性を重視して分析します
  FCF マージン
  資本配分（自社株買い・配当）
  定性的なモート
  成熟・高配当バリュー株アナリスト
  配当の持続性と減配リスクを最優先で検証します
  FCF 利回り
  配当性向の安全性
  連続増配
  有利子負債カバー率
  減配リスク
  売上高成長率（YoY）
  営業利益率
  ROE（自己資本利益率）
  キャッシュランウェイ
  か月
  FCF マージン
  PER（株価収益率）
  倍
  PBR（株価純資産倍率）
  倍
  D/E（負債資本倍率）
  倍
  配当利回り
  配当性向
  # 閾値基準（ユーザー設定・厳密に適用すること）\n\n\
         以下の閾値基準を厳密に適用して合格/不合格を判定せよ。\n\n\
         {}\n\n\
         - 各項目の根拠欄には、…
```

### `src-tauri/src/quarterly.rs` — F（12 件）

```
  Yahoo Finance（売上・純利益・EPS）＋ SEC EDGAR XBRL（前年同期比）
  Yahoo Finance（売上・純利益・EPS）
  Yahoo は四半期粒度で粗利・営業利益・キャッシュフローを提供していないため、四半期の利益率は純利益率のみです。
  この銘柄は SEC EDGAR に登録がないため前年同期比を算出できません。季節性のある事業では前四半期比だけで成長の加速を判断しないでください。
  前年同期比を算出できないため、成長の加速・減速は判定していません。前四半期比は季節性の影響を受けます。
  売上の前年同期比が {b:+.1}% → {a:+.1}% と拡大しており、成長は加速しています。
  売上の前年同期比が {b:+.1}% → {a:+.1}% と縮小しており、成長は減速しています。
  直近四半期の売上前年同期比は {a:+.1}% です。比較対象が 1 期分しかないため加速・減速は判定していません。
  前年同期比のデータが不足しており、加速・減速は判定していません。
  兆
  億
  百万
```

### `src-tauri/src/settings.rs` — F（5 件）

```
  「{}」の APIキーが未設定です。設定画面から登録してください。
  カスタム {}
  プロバイダ {id} が見つかりませんでした。
  OpenAI互換
  設定ディレクトリを取得できません: {e}
```

### `src-tauri/src/shortcuts.rs` — F（7 件）

```
  ショートカットテーブルを作成できません: {e}
  ショートカットを取得できません: {e}
  ショートカットを取得できません: {e}
  アクションを指定してください。
  ショートカットを保存できません: {e}
  ショートカットを戻せません: {e}
  ショートカットを戻せません: {e}
```

### `src-tauri/src/yahoo.rs` — F（50 件）

```
  ティッカーが空です。
  株価は取得できましたが、詳細指標の取得に失敗しました: {e}
  指標データが空でした。
  Yahoo Finance の認証トークン（crumb）を取得できませんでした。
  株価・規模
  時価総額
  企業価値 (EV)
  52週安値
  52週高値
  ベータ
  発行済株式数
  バリュエーション
  PER（実績）
  PER（予想）
  EV / 売上高
  成長性
  売上成長率（YoY）
  EPS成長率（YoY）
  四半期利益成長率
  EPS（実績）
  EPS（予想）
  売上高（TTM）
  収益性
  粗利率
  営業利益率
  純利益率
  EBITDA マージン
  キャッシュ・財務健全性
  フリーCF
  営業CF
  現金・同等物
  有利子負債
  負債比率 (D/E)
  流動比率
  アナリスト予想
  目標株価（平均）
  目標株価（高）
  目標株価（低）
  レーティング
  推奨スコア
  アナリスト数
  {v:.0} 名
  兆
  億
  百万
  強い買い
  買い
  中立
  売り
  強い売り
```

