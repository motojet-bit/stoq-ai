# 改修記録 — StockAnalyzer

指示を受けて改修を行うたびに、**新しい項目を先頭に**追記する。

書式:

```
## YYYY-MM-DD — 見出し
**指示**: ユーザーからの指示（要約）
**対応**: 実際に行った変更
**変更ファイル**: 追加/変更/削除したファイル
**コミット**: <short-sha>
**備考**: 補足・積み残し（なければ省略）
```

---

> 古い記録は [docs/archive/改修記録_2026-08.md](docs/archive/改修記録_2026-08.md) に繰り下げてあります。

---

## 2026-08-03 — 推論モデルの失敗を根本から潰す ＋ 株価フィード

**指示**: GPT-5.5 等の推論モデルで「LLM からの応答が完了しませんでした（kind: unknown）」が
出る。タイムアウト延長・パラメータ適合・**エラー本文の生ログ表示**で根本解決すること。
併せて、選択中の銘柄の株価・前日比・52週・時価総額をリアルタイム表示すること。

**対応**:

**① 「kind: unknown」の原因は 2 つあり、どちらも情報が消える作りだった**
- `AppError` の `Display` は**コードしか返さない**（`#[error("{code}")]`）。
  ストリーミングの失敗は `LlmEvent::Error` の 1 本の文字列で運ばれるため、
  `err.to_string()` を使っていたこの経路で **API が返した本文が丸ごと捨てられていた**。
  `AppError::wire()`（`ERR_X: 本文`）を足し、フロントの `parseAppError` で割り直す
- `api_error` / `ensure_success` が本文を組み立てておきながら**捨てていた**
  （`detail` と `hint` が未使用のまま）。本文とステータスを載せるようにした
- `invoke` の解決がチャネル配信を追い越すと、**成功した応答を失敗として捨てていた**。
  `waitForSettle`（3 秒の猶予）を挟み、遅れているだけの応答を殺さないようにした

**② タイムアウトは「延ばす」だけでは足りなかった**
- reqwest の `timeout` は**本文の受信中も動き続ける**。5 分にすると
  「5 分以上かかる分析」が答えを返している最中に切られる。
  LLM 用は全体制限を外し、`read_timeout`（無音 300 秒）で打ち切る作りに変更。
  **届き続けている限り何分でも待つ**（＝ハートビート維持）
- `connect_timeout` は 30 秒。繋がらない相手を 5 分待っても結果は変わらない

**③ 400 系のパラメータ拒否を「出力上限」と誤診していた**
- `Unsupported parameter: 'max_tokens'` は `max_tokens` を含むため、
  診断が `truncated`（出力上限）に落ちていた。**直し方が正反対になる**ので
  `badRequest` を新設し、パラメータ判定を先に置いた

**④ 株価フィード（新規）**
- `src-tauri/src/quote.rs`: crumb 不要の `chart` から株価・前日比・52週を取り、
  時価総額だけ追加取得。**時価総額が取れなくても株価は返す**
- `QuoteTicker.tsx`: ティッカー入力の隣に表示。60 秒ごとに更新。
  上げ＝緑・下げ＝赤に**符号と ▲▼ を必ず添える**（色だけに頼らせない）。
  取得できなければ「株価取得オフライン」と出すだけで、操作は一切止めない

**変更ファイル**:
- 追加: `src-tauri/src/quote.rs`, `src/lib/llm/settleGrace.ts`(+test),
  `src/lib/market/quoteTone.ts`(+test), `src/lib/api/marketQuote.ts`,
  `src/components/QuoteTicker.tsx`
- 変更: `src-tauri/src/{error.rs,http.rs,yahoo.rs,commands.rs,lib.rs,llm/mod.rs,llm/openai.rs}`,
  `src/lib/errors/{errorMessage.ts,diagnose.ts}`(+test), `src/lib/llm/client.ts`,
  `src/components/CommandBar.tsx`, `src/App.tsx`, `src/types/index.ts`, `src/locales/{ja,en}.json`

**コミット**: `54b2f7b`

**備考**: 実機で再発した場合、生ログに **API の本文がそのまま出る**ようになった。
「Unsupported parameter」の記載があればパラメータ、無ければ通信側と切り分けられる。

---

## 2026-08-03 — LP: 購入前ダイアログの件数ずれ（4 点 → 5 点）を修正

**指示**: 購入前の確認ダイアログが「以下の 4 点」のままだが、項目は 5 つある。修正すること。

**対応**:
- 文言を 5 点へ（項目は V0 価格の回に 5 つ目「メジャーアップデートの有償化」を足していた）
- **件数は JS がチェックボックスの数から数え直すようにした**（`[data-consent-count]`）。
  同じ事故は項目を足すたびに起きる。**文言側の数字を人が直し忘れても消えるようにする**
- **HTML にも正しい数字を書いておく。** JS が動かない環境では書いたものが出る
  （このダイアログは JS が死んでいても決済へ通す作りなので、そこだけ古い数字が残ると困る）
- 冒頭のコメントも 5 点の内訳へ更新

**変更ファイル**: `lp/index.html`

**コミット**: `d77a31c`

**公開**: `git subtree push --prefix lp origin gh-pages` ✅（`64a70f6` → `b199f60`）。
配信中の HTML から `all four` / `以下の 4 点` が消えていることを確認。

## 2026-08-03 — LP: ヒーローを誠実なトーンへ差し替え ＋ 改行調整 ＋ 再デプロイ

**指示**: ①ヒーローのメインコピーと本文を、トゲのある表現を避けて自立志向を応援する誠実なトーンへ差し替える（指定文あり）。②「1つの画面で完結する、次世代の決算分析体験。」を読点・句点の位置で改行する。③GitHub Pages へ再デプロイする。

**対応（① ヒーロー）**:
- メイン: 「情報や判断を他人に依存する投資は / **卒業**」。
  **「卒業」だけを 2 行目に落として色を乗せた。** 1 行に流すと結論が地の文に埋もれる
- 前回の「スクール頼みの投資は、もう卒業。」を撤回。
  **特定の学び方を否定する形になっていた**（読み手が通っている場合、
  自分の選択を否定されたと受け取る）。今回は主語を読み手の状態に寄せている
- 本文は指定の 3 文を**それぞれ 1 行**に。学習支援としての立ち位置
  （「投資学習を徹底サポート」）が中央の文で明示される並び
- **`sm:text-5xl` は据え置き。** 1 行目が 15 文字あり、6xl にすると `max-w-4xl` を超える
- 指定文の「納得いくまで 深掘り」にあった全角空白ぶんの隙間は詰めた
  （日本語の途中の空白は画面上で不自然な穴になる）
- 英語版も同じ主張で書き直した（`data-en` / `data-ja` の二言語構成を維持）

**対応（② 見出しの改行）**:
- Core Value の見出しを「1つの画面で完結する、`<br />` 次世代の決算分析体験。」へ。
  **読点でも改行を入れる。** 入れないと幅しだいで意味の切れ目でない位置で折り返す

**対応（③ デプロイ）**:
- `git subtree push --prefix lp origin gh-pages` ✅（`4c0c95e` → `64a70f6`）
- 配信中の HTML に新コピーが載り、**旧コピーが残っていない**ことも確認

**変更ファイル**: `lp/index.html`

**コミット**: `1ebbb47`（LP 本体）/ 本記録は次のコミット

## 2026-08-03 — LP: 自立志向の投資家へ向けた文面刷新（なぜなぜ分析）

**指示**: LP のキャッチコピーと本文を、「高額なスクールやセミナーに頼らず、自室でプロ級の『なぜなぜ分析』を自分で実践したい自立志向の投資家」に刺さるトーンへ調整する。①ヒーローのメイン／サブコピー差し替え、②お悩みセクション（こんな壁を感じていませんか？）を 4 項目で追加、③AIクロスディベートの解説を「1 人でプロの深掘りを体験できる」文脈で追加し、完全ローカル・買い切りの訴求を強める。**他者を直接批判しない**こと。

**対応（① ヒーロー）**:
- メイン: 「スクール頼みの投資は、もう卒業。/ 専属 AI と『なぜ』を突き詰める、/ 自立型・決算アナライザー。」
- **`sm:text-6xl` → `sm:text-5xl` に落とした。** 文字数がほぼ倍になったので、
  6xl のままだと 4 行に割れて圧が出る
- 日本語は `<br />` で明示改行（自動折り返しは読点を無視する）。英語も同じ主張で書き下ろし

**対応（② お悩みセクション）**:
- `#pains` を**ヒーローと Core Value のあいだ**に新設。
  **「何をしなくてよくなるか」を見せる前に、読み手が自分の詰まりを言語化できる場を置く**
- 4 項目（SNS 頼み / スクールのハードル / 1 人だと思考が止まる / 推奨銘柄からの脱却）
- **警告色を使わない。** アイコンもカードも本文と同じ寒色に留める。
  赤やアラートで煽ると、狙っている知的なトーンが崩れる
- **主語を常に読み手側に置く。** 「スクールが悪い」ではなく「通うのはハードルが高い」。
  他者を名指しで批判した瞬間、比較優位ではなく党派の話になる
- 末尾に橋渡しの一文（「渡すのは答えではなく、深掘りの型そのもの」）

**対応（③ クロスディベート ＋ ローカル・買い切り）**:
- `#features` の**先頭に単独で**配置。3 枚のカードに混ぜると主張の核が埋もれる
- 強気派（Bull）/ 慎重派（Bear）の役割を 2 枠で併記。
  文面は実装（`debate_bull.md` = 応答・修正する側 / `debate_bear.md` = 検証する側）に合わせた
- 「検証役には別プロバイダを指定できる」を注記。
  **アプリ内の `debate.settingsIntro` と同じ理由**（同じモデルだと思い込みが残る）
- 機能カード ③ の見出しを「BYOK」→「完全ローカル・買い切り」へ。
  月額費用・コミュニティへの気遣いが不要である旨を追記
- 価格セクションのリード文にも「更新も退会の手続きもない」を追記

**変更ファイル**: `lp/index.html`（変更）/ `PROGRESS.md` / `改修記録.md`

**コミット**: `56bab50`（`lp/index.html`）/ 本記録は `856363e`

**公開（追記 2026-08-03）**: 指示により先行公開した。
`git subtree push --prefix lp origin gh-pages` ✅（`faccc69` → `4c0c95e`）。
https://motojet-bit.github.io/stoq-ai/ が 200 で新コピーを配信しているのを確認。
**まだ告知していない・アクセスが無い前提での、実機表示確認を兼ねた公開**。
LP に載せたクロスディベートは配信ビルド v0.1.0 に無いので、
**告知の前にこの機能を含む版をリリースすること**。

**注意**: `lp/index.html` は**別セッションのコミット `56bab50`（クロスディベート Stage 2）に
巻き込まれて入った**。ステージしたまま並行して `git commit` が走ったため。
LP の変更なのにコミットメッセージは `feat: 2分割 AIクロスディベート` になっている。
**同じリポジトリを複数セッションで触るときは、add してから commit までを空けない。**

**備考**: クロスディベートは**この時点でコミット済みだが未リリース**（配信ビルドに未反映）。
LP を公開する前に、この機能が入ったビルドを配信する必要がある。
先に LP だけ出すと、ダウンロードした版に無い機能が載っていることになる。

## 2026-08-03 — 第78章: 分析が「切れて何も残らない」問題への六重の防御 ＋ UX 追加

**指示**: 実機テストで出た問題への連続対応。①巨大資料（MSFT 等）で分割分析が発動しない → しきい値の引き下げ。②出力上限で途中終了して分析が失敗する → 自動継続。③壊れた途中経過を引きずって同じエラーを繰り返す → 破棄手段の追加。あわせて UX の追加要望（添付ヒント／折りたたみ／コンテキストゲージ／会話クリア／格安 AI 対応）。

---

### 切れ対策（本章の主題）

**分析が途中で切れると、その回の成果がまるごと失われる。**
評価テーブルが途中で止まればパーサが行を拾えず、保存も走らない。
原因が複数あるので、層を分けて塞いだ。

| 層 | 対策 |
| --- | --- |
| 入力が大きい | 1 万トークン超で自動分割（MapReduce） |
| 抜き出しが膨らむ | Map 出力を 800 字に制限 |
| 出力が長い | プロンプトで凝縮・重複禁止・「長さは評価されない」 |
| それでも切れた | 打ち切りを検知して最大 3 回まで自動継続 |
| 継続でも足りない | 途中まででも保存し、次回は続きの段から再開 |
| 段が壊れた | 「最初からやり直す」で破棄して再実行 |

**対応（巨大資料の分割 = MapReduce）**:
- 資料が 1 万トークンを超えたら分割経路へ。
  **切り詰めると、削った部分は最初から無かったことになる**
- **意味の切れ目（見出し）で分ける。** 文字数だけで切ると表や文が途中で割れ、
  どちらのチャンクからも読めなくなる
- 見出しが無い資料でも必ず割れるようにした。
  **PDF から抽出したテキストは改行が入らず 1 行になる**ことがあり、
  行単位の分割だけでは 1 つも切れない（テストで検出して修正）
- Map ではあえて**評価も点数も付けさせない**。
  他の部分を読まないまま下した判断が最後まで残る
- **しきい値だけ下げると穴が開く。** チャンクの目安（12,000）が
  しきい値（10,000）以上のままだと、「分割が要る」と判定された資料が
  1 チャンクにしか割れず、分割経路に入ったのに素通りする。
  目安を 5,000 へ下げ、両者の関係をテストで固定した
- 1 チャンク落ちても残りで続ける（全部落とすより情報が残る）

**対応（出力の自動継続）**:
- 打ち切りの終了理由を Rust 側で検知。**3 社で名前も値も違う**
  （`finish_reason=length` / `stop_reason=max_tokens` / `finishReason=MAX_TOKENS`）
  ので 1 か所で見る。正常終了（stop / STOP）と取り違えない
- 検知したら継続プロンプトを出し、**最大 3 回**まで続きを足す。
  無制限だと終わらないモデルで費用だけが増える。
  3 回で足りないなら 1 回に詰め込みすぎ（分割の対象）と考えるほうが正しい
- **継続時は末尾 2000 字だけを渡す。** 全文を毎回渡すと入力が積み上がり、
  上限に当たった原因をさらに悪くする
- 結合は重なりを落としてから足す。**指示しても直前の行を繰り返す**ことがあり、
  そのまま足すと同じ行が二重に並ぶ。
  文字単位に加えて**行単位でも見る**（短い行は文字単位では引っかからない）

**対応（出力サイズの抑制）**:
- 出力フォーマットに「出力の長さについて」を追加。
  要点の凝縮・重複の禁止・前置きと結びの禁止
- **「長さは評価されない」と明記。** 長い出力ほど切れやすく、
  切れた結果は読み手にとって価値がゼロになる
- Map の抜き出しにも 800 字の上限（統合の入力が太ると、そこでまた切れる）

**対応（チェックポイントの破棄）**:
- 失敗表示とエラー詳細に「最初からやり直す」を追加
- **再分析・クリアも必ず捨ててから走る。** 引き継ぐのは「再開する」だけ。
  壊れた段が残っていると、再開のたびにそれを土台にして同じ場所で失敗し続ける

**対応（進捗表示）**:
- 通常は 4 段の目盛り。**分割時だけパーセント表示へ切り替える**
  （段の数が資料によって変わるので、5 つの目盛りでは表せない）
- 「資料を分割分析中（n / m 完了）…」「中間結果を統合中…」
  「長文レポートを継続生成中（追記 n / 3）…」

---

### UX の追加

- **添付ヒント**: ドロップ領域の右端に「💡 効果的な添付資料とは？」。
  **自動取得している資料（財務指標・10-Q/10-K）を先に伝える**。
  これを言わないと、同じものを探して集める手間をかけさせる
- **折りたたみ**: 「エクセル・CSV・表計算ソフト用貼り付けフォーマット」へ改称し既定で畳む。
  **左右に並ぶブロックは 1 組で畳む**（片方だけでは行の高さが変わらない）。
  畳んだ中身は描画ごと外す（`hidden` だと Ctrl+A の選択に入る）
- **コンテキストゲージ**: 対話ヘッダーに使用率（70/85% で色が変わる）。
  **使い切ってから気づくのでは遅い**。システムプロンプトも数える
- **会話クリア**: 3 択（キャンセル / 新規作成 / 消去）。
  **「消す」以外の道を同じ場所に出す**。要約して移す手順も添える
  （消したあとでは実行できない手順なので、この画面で伝えないと間に合わない）
- **かんたん追加**: DeepSeek / SiliconFlow / Ollama / OpenRouter / Groq を 1 押しで登録。
  **Base URL を覚えている人はいない**。接続先とモデルまで入れておく
- **ローカル LLM**: `localhost` 等はキー未設定でも「使える」扱い。
  Ollama は認証を持たないので、未設定を理由に弾くと使えない。
  判定は `is_local_url` の 1 か所に置く
- **ボタン名**: ティッカー横の「分析」→「**指標取得**」。
  実際に走るのは指標の取得で、AI 分析ではない

**変更ファイル**: `src/lib/prompts/mapReduce.ts`（新規）,
`src/lib/llm/continuation.ts`（新規）, `src/lib/chat/contextGauge.ts`（新規）,
`src/lib/config/providerPresets.ts`（新規）, `src/lib/ui/collapsedSections.ts`（新規）,
`src/lib/ui/overflow.ts`（新規）, `src/lib/ui/selectScope.ts`（新規）,
`src/components/{AttachmentHint,CollapsibleSection,ContextGauge,ChatClearDialog,OverflowScroller}.tsx`（新規）,
`src-tauri/src/llm/*.rs`, `src-tauri/src/{settings,prompts/mod}.rs`,
`src-tauri/src/prompts/output.md`, ほか

**コミット**: `dbc6558`, `3be5839`, `1c1d3ef`, `8744fe2`, `4afa1d2`,
`713ca41`, `25ca3c6`, `ccc6c32`, `d5b86ae`, `5a1dfe8`

**備考**: 分割の発動が早くなったぶん、**中規模の資料でも呼び出し回数が増える**。
コストは消費ログで実測できる。増え方が許容できない場合は
しきい値（10,000）か目安（5,000）で調整する。

---

## 2026-08-03 — 第77章: 消費ログ ＋ 中断の即時停止 ＋ 折りたたみ ＋ 不一致は事後警告へ

**指示**: ①分析・トークン消費ログのビューア（累計・CSV 出力・全消し）。②中断ボタンの連打防止とミリ秒での即時切断。③銘柄不一致ガードの厳格化 → **のちに「事後警告」へ方針転換**。④ヘッダーの見切れ防止とオーバーフロー表示。⑤評価テーブルの改称と既定折りたたみ。⑥ブロックごとの個別折りたたみ。⑦ディベートの 1 ターン制限撤廃・注意書き・判断ヒント・質問先頭へのジャンプ。⑧添付資料のプレビューとダウンロード。

**対応（① 消費ログ）**:
- `usage_log` テーブルを新設（`analyses.db`）。**分析結果とは別に持つ。**
  結果を消してもコストの記録は残したいし、中断・エラーで終わった実行も残す
- 表示メニューから開くモーダル。累計・CSV 出力・確認付きの全消し
- **中断・エラーぶんも累計に足す。** そこまでのトークンは実際に払っている
- **単価不明のモデルは金額に足さず、件数で伝える。** 0 円として足すと安く見える

**対応（② 中断）**:
- **フラグだけでは止まらなかった。** 読み取りループはチャンクの到着を待って
  止まっているので、フラグを見るのは「次の 1 個が届いたあと」。
  `Notify` ＋ `tokio::select!` で待ちを叩き起こし、ストリームを捨てて接続を切る
- 押した瞬間に無効化して「中断中…」へ。
  **連打で「分析開始」に切り替わった瞬間を踏む事故**を防ぐ

**対応（③ 不一致 — 二転した）**:
- はじめは厳格化した（社名照合 ＋ システムプロンプト冒頭で即時遮断）
- **そのあと事後警告へ切り替えた。** 決算資料には競合・顧客・取引先の名前が
  普通に出てくるため、**誤検知で正しい分析まで止まる不利益のほうが大きい**
- いまの形: 分析は続行し、**出力の最上部に警告文**を出させる。
  他社名の言及だけでは出させない／判断が付かない場合も出させない
- 事前検知も「差し替える／このまま続行」の確認に変更。
  既定フォーカスは差し替え側（**押し間違いで進むより、止まるほうが安全**）

**対応（④ ヘッダー）**:
- 操作ボタン群に `shrink-0`。窓を狭めても文字を大きくしても押し出されない
- 補足と分析根拠タグは 1 行に収め、溢れたら横スクロール。
  端にグラデーションと ▶ を出す（2px の遊びを入れて出入りを安定させた）

**対応（⑤⑥ 折りたたみ）**:
- 「評価テーブル」→「エクセル・CSV・表計算ソフト用貼り付けフォーマット」。
  **既定で畳む**（表計算へ移すときにしか使わない）
- 各ブロックを個別に開閉。状態は端末に残す
- **畳んだ中身は描画ごと外す。** `hidden` で隠すだけだと Ctrl+A の選択に入り、
  見えていない部分までコピーされる

**対応（⑦ ディベート）**:
- **1 往復の制限を撤廃。** 批判側／反論側を選んで何度でも問い直せる。
  ただし**自動では続けない**（押したときだけ）。費用が青天井になる
- 上部に注意書きを常時表示（批判は無限に作れるので参考程度に）
- 完了後に判断ヒント（判定一致 or スコア差 ±0.3 以内なら追加分析は不要）
- 「▲ 質問の先頭へ戻る」を対話画面とディベートに設置

**対応（⑧ プレビュー）**:
- プレビューにダウンロードを追加。**取得できないときの案内を同じ場所に出す**

**変更ファイル**: `src-tauri/src/{analyses,commands,lib,prompts/mod}.rs`,
`src-tauri/src/llm/mod.rs`, `src-tauri/Cargo.toml`,
`src/lib/usage/{usageLog,usageStore}.ts`（新規）,
`src/lib/ui/{overflow,collapsedSections,selectScope}.ts`（新規）,
`src/components/{UsageLogModal,OverflowScroller,CollapsibleSection}.tsx`（新規）ほか

**コミット**: `659a177`, `3850937`, `7eec890`, `750bf75`

**備考**: **資料のダウンロードは保存ダイアログではなく書き出しフォルダへの保存。**
`dialog` プラグインが未導入で、追加すると Cargo・npm・capabilities の 3 か所を
触ることになるため見送った。保存されるのは抽出後のテキストで、元の PDF ではない。

---
