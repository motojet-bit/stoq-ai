# 改修記録 — StockAnalyzer

指示を受けて改修を行うたびに、**新しい項目を先頭に**追記する。

書式:

```
## YYYY-MM-DD — 見出し
**指示**: ユーザーからの指示（要約）
**対応**: 実際に行った変更
**変更ファイル**: 追加/変更/削除したファイル
**コミット**: <short-sha>
**備考**: 補足・積み残し（なければ省略）
```

---

> 古い記録は [docs/archive/改修記録_2026-08.md](docs/archive/改修記録_2026-08.md) に繰り下げてあります。

---

## 2026-08-03 — LP: 自立志向の投資家へ向けた文面刷新（なぜなぜ分析）

**指示**: LP のキャッチコピーと本文を、「高額なスクールやセミナーに頼らず、自室でプロ級の『なぜなぜ分析』を自分で実践したい自立志向の投資家」に刺さるトーンへ調整する。①ヒーローのメイン／サブコピー差し替え、②お悩みセクション（こんな壁を感じていませんか？）を 4 項目で追加、③AIクロスディベートの解説を「1 人でプロの深掘りを体験できる」文脈で追加し、完全ローカル・買い切りの訴求を強める。**他者を直接批判しない**こと。

**対応（① ヒーロー）**:
- メイン: 「スクール頼みの投資は、もう卒業。/ 専属 AI と『なぜ』を突き詰める、/ 自立型・決算アナライザー。」
- **`sm:text-6xl` → `sm:text-5xl` に落とした。** 文字数がほぼ倍になったので、
  6xl のままだと 4 行に割れて圧が出る
- 日本語は `<br />` で明示改行（自動折り返しは読点を無視する）。英語も同じ主張で書き下ろし

**対応（② お悩みセクション）**:
- `#pains` を**ヒーローと Core Value のあいだ**に新設。
  **「何をしなくてよくなるか」を見せる前に、読み手が自分の詰まりを言語化できる場を置く**
- 4 項目（SNS 頼み / スクールのハードル / 1 人だと思考が止まる / 推奨銘柄からの脱却）
- **警告色を使わない。** アイコンもカードも本文と同じ寒色に留める。
  赤やアラートで煽ると、狙っている知的なトーンが崩れる
- **主語を常に読み手側に置く。** 「スクールが悪い」ではなく「通うのはハードルが高い」。
  他者を名指しで批判した瞬間、比較優位ではなく党派の話になる
- 末尾に橋渡しの一文（「渡すのは答えではなく、深掘りの型そのもの」）

**対応（③ クロスディベート ＋ ローカル・買い切り）**:
- `#features` の**先頭に単独で**配置。3 枚のカードに混ぜると主張の核が埋もれる
- 強気派（Bull）/ 慎重派（Bear）の役割を 2 枠で併記。
  文面は実装（`debate_bull.md` = 応答・修正する側 / `debate_bear.md` = 検証する側）に合わせた
- 「検証役には別プロバイダを指定できる」を注記。
  **アプリ内の `debate.settingsIntro` と同じ理由**（同じモデルだと思い込みが残る）
- 機能カード ③ の見出しを「BYOK」→「完全ローカル・買い切り」へ。
  月額費用・コミュニティへの気遣いが不要である旨を追記
- 価格セクションのリード文にも「更新も退会の手続きもない」を追記

**変更ファイル**: `lp/index.html`（変更）/ `PROGRESS.md` / `改修記録.md`

**コミット**: `56bab50`（`lp/index.html`）/ 本記録は `856363e`

**公開（追記 2026-08-03）**: 指示により先行公開した。
`git subtree push --prefix lp origin gh-pages` ✅（`faccc69` → `4c0c95e`）。
https://motojet-bit.github.io/stoq-ai/ が 200 で新コピーを配信しているのを確認。
**まだ告知していない・アクセスが無い前提での、実機表示確認を兼ねた公開**。
LP に載せたクロスディベートは配信ビルド v0.1.0 に無いので、
**告知の前にこの機能を含む版をリリースすること**。

**注意**: `lp/index.html` は**別セッションのコミット `56bab50`（クロスディベート Stage 2）に
巻き込まれて入った**。ステージしたまま並行して `git commit` が走ったため。
LP の変更なのにコミットメッセージは `feat: 2分割 AIクロスディベート` になっている。
**同じリポジトリを複数セッションで触るときは、add してから commit までを空けない。**

**備考**: クロスディベートは**この時点でコミット済みだが未リリース**（配信ビルドに未反映）。
LP を公開する前に、この機能が入ったビルドを配信する必要がある。
先に LP だけ出すと、ダウンロードした版に無い機能が載っていることになる。

## 2026-08-03 — 第72章: 決算期の承認 ＋ SEC 自動取得 ＋ アドホック分析の入れ子ツリー

**指示**: 実機テスト前に、次回実装アイデアの 3 件をすべて織り込む。①自動特定した決算期を画面で確認・補正して OK を押してから保存・分析へ進む。②添付なしのときは SEC から 10-Q / 10-K を自動取得し、自動取得した旨を明示する。③`analyses` に `parent_id` を追加する Migration を行い、期中の適時開示を四半期の配下（`Q2-01` 形式）へ紐付ける。④`latest.json` を修正して署名付き再ビルド。

**対応（① 決算期の承認）**:
- `FiscalPeriodDialog.tsx` を新設。年度は数値入力、四半期は Q1〜Q4 ＋ 通期のボタン
- **判別できなかったときも同じダイアログを出す。**
  「分かりませんでした、選んでください」と聞けば期を記録できる。
  黙って期なしで進めると、あとからヒートマップの期と突き合わせられない
- 自動特定と食い違えば「手動で修正」バッジを出す（直したことが残る）
- 確定した期は `periodStore.ts` に**メモリだけ**で持つ。
  永続化すると、資料を入れ替えても古い確定が効いてしまう
- 保存時の `period_label` は**人が確定した期を最優先**。
  無ければ従来どおり直近四半期のラベルで代用する

**対応（② SEC 自動フォールバック）**:
- `secFallback.ts` に判断を切り出した（`planFilingFetch`）。
  添付 0 件なら `autoFallback`、添付があれば従来どおり `requested`
- **10-Q を先に試す。** 直近の状況を見たいので四半期が先で、無ければ年次
- 取りに行かない条件を明示的に持つ（EDGAR に無い / User-Agent 未設定 /
  提出書類なし / 状況未確認 / 10-K も 10-Q も無い）。**空振りする呼び出しをしない**
- **自動取得したことを 3 か所に出す。** トースト・根拠欄（`basis.filingAuto`）・
  プロンプト本文（`prompt.filingAutoNote`）。
  ユーザーが渡していない資料が根拠に入るので、黙って使ってはいけない。
  プロンプトにも書かないと「添付いただいた資料」と書いてしまう
- 呼び出し側（`App.tsx`）は `fetchFiling: boolean` を渡すのをやめ、
  提出状況をそのまま渡す形にした。**判断を 1 か所に寄せる**

**対応（③ 入れ子ツリー）**:
- `analysis_history` に `parent_id` と `branch_no` を `ALTER TABLE` で追加（既存 DB も安全）
- **親を消しても子は残す（`ON DELETE` を張らない）。**
  四半期の分析をやり直したくて消しただけなのに、
  期中に積み上げた開示の分析まで巻き添えで消えると取り返しがつかない。
  親を失った子は根として一覧に出す（画面から消えたように見せない）
- **枝番は保存時に採番して持たせる。** 表示のたびに数え直すと、
  途中の 1 件を消した瞬間に既存の番号がずれて、
  「Q2-02 の件」と控えていたものが別物を指す。
  採番は**いま存在する最大値の次**（件数 + 1 だと削除後に衝突する）
- **アドホックは `analyses`（銘柄ごとの最新 1 件）を上書きしない。**
  期中のプレスリリース 1 本で、四半期決算の分析が画面から消えてしまう
- 入れ子は 1 段まで（`parentCandidates` が子を親候補から外す）
- 記録の仕方の選択は決算期ダイアログに載せた。
  同じ期の本体が既にあるときだけ「期中の追加分析として記録（Q2-01）」が出る

**対応（④ latest.json）**: 生成スクリプトの修正ぶんで再生成し、URL が 200 を返すことを確認。

**変更ファイル**: `src-tauri/src/{analyses,commands}.rs`,
`src/components/FiscalPeriodDialog.tsx`（新規）,
`src/lib/parser/periodStore.ts`（新規）, `src/lib/portfolio/archiveTree.ts`（新規）,
`src/lib/prompts/secFallback.ts`（新規）,
`src/lib/prompts/{analysisRunner,buildPrompt}.ts`,
`src/components/PortfolioHistoryPanel.tsx`, `src/App.tsx`, `src/types/index.ts`,
`src/locales/{ja,en}.json`

**コミット**: `1ebaa99`

**備考**: **`latest.json` の GitHub 上のアセットはまだ壊れたまま。**
`gh` CLI が入っていないため差し替えられない。自動更新はこれが済むまで動かない。

---

## 2026-08-03 — 第69・71章: 参照付きの根拠出力 ＋ 決算期の自動特定と YF 連動（Stage 3）

**指示**: ①重要な数値・結論に【参照: p.XX "原文引用"】を必ず添えさせ、資料に無い数値の捏造を厳禁とする。②決算資料の年度・四半期を自動特定し、当時の Yahoo Finance の指標と照合させる。当時のデータが無くてもエラーにせず PDF 単体で続ける。③`PROGRESS.md` と `改修記録.md` を第65〜71章の状態へ更新する。

**対応（① 参照付きの根拠）**:
- `prompts/core.md` の冒頭に「数値の出所（最重要）」節を新設。
  添付資料からの引用は `【参照: p.12 "原文"】` の形で、
  **ページ番号と原文の短い引用を必ずセット**にさせる
- **「参照を付けられない数値は、書いてはいけない数値である」**という言い方に統一した。
  「推測で断定しない」だけだと、それらしい値を置く余地が残る
- ページが判別できないときも `p.?` と書かせる。**黙って省かせない**
- `prompts/output.md` に「参照した原文」節を追加。本文で使った数値の裏を一覧で取れる
- 根拠欄の字数を 120 → 160 字へ（引用が入るぶん枠が要る）。
  引用内のパイプ記号は全角へ置き換えさせる（表が壊れるため）
- `src/lib/prompts/citations.ts` で参照を機械的に抽出できるようにした。
  **指示が守られたかを確かめられなければ、守られていない出力に気づけない。**
  ページ欠け・引用欠けの件数も数えられる

**対応（② 決算期の自動特定）**:
- `src/lib/parser/fiscalPeriod.ts`。`FY2023 Q3` / `Q3 FY23` /
  `2023年12月期 第3四半期` / 全角の`第３四半期` /
  `third quarter of fiscal 2023` / 通期表記 に対応
- **先頭 4000 字だけを見る。** 決算資料は表紙に対象期を書き、
  本文では前年同期にも触れる。全体から拾うと比較対象の古い期を掴む
- **四半期まで読める書き方を、年度だけの記載より先に試す。**
  順序を逆にすると、Q が読める資料でも通期扱いになる
- **判別できなければ null を返す。** 「たぶん直近の四半期」で埋めると、
  別の期の株価と突き合わせた分析が正しそうな顔で出てくる
- 1990〜2100 年の外は捨てる（ページ番号や型番の誤検出）

**対応（② YF 連動とフォールバック）**:
- `src/lib/prompts/periodLink.ts` で Yahoo の四半期系列に突き合わせる。
  ラベル（`3Q2023` 等）で当て、書式が違えば**期末日**でも当てる
- 当たれば当時の売上・純利益・YoY・QoQ・EPS を表でプロンプトへ載せ、
  **資料の数値との食い違いを必ず指摘させる**
- **当たらなくてもエラーにしない。** Yahoo が返す四半期は 4〜8 期しかないので、
  少し古い資料は外れるほうが普通。
  「当時のデータは取得できなかった／推測で補うな」と明記して PDF 単体で続ける
- 複数資料があるときは**最初に読み取れたものを主資料**にする
  （どれを正とするか機械的に決める根拠が無いため）

**対応（③ ドキュメント）**: 本記録と `PROGRESS.md` を更新。

**変更ファイル**: `src-tauri/src/prompts/{core,output}.md`,
`src/lib/parser/fiscalPeriod.ts`（新規）, `src/lib/prompts/{citations,periodLink}.ts`（新規）,
`src/lib/prompts/buildPrompt.ts`, `src/locales/{ja,en}.json`

**コミット**: `84dade7`

**備考**: 参照の抽出（`citations.ts`）は作ってあるが、**画面にはまだ出していない。**
分析結果に「根拠の付き方」バッジを出すのは次の区切りで。

---

## 2026-08-03 — 2分割 AIクロスディベート（Stage 2）

**指示**: ①設定にディベート用プロバイダ／モデルの独立設定を追加する。②分析エリアを左右 2 分割し、右に批判的検証ペインを置く。「🔥 1ターンディベート実行」で「右AIが批判 → 左AIが反論」の 1 往復のみを実行する（無限ループ防止）。

**対応（① 設定）**:
- `src-tauri/src/debate.rs` を新設。`DebateConfig{provider, model}` を設定へ保存し、
  `DebateStatus` として**マスクも要らない情報だけ**をフロントへ返す
- **未設定ならメインへ落とす。** 設定していない人がボタンを押せないと、
  機能があること自体に気づけない
- ただし**メインと同じプロバイダになる場合は画面で注意する。**
  同じモデルに自分の出力を批判させても、同じ癖・同じ思い込みがそのまま残る
- 知らないプロバイダ ID は保存時に弾く（実行時にキーが引けず毎回エラーになるため）
- `LlmRequest` に `debate` と `model` を追加。モデルの上書きはここだけで効く

**対応（② 2 分割 UI と 1 ターン制御）**:
- `AnalysisWithDebate.tsx` が分割の有無を決める。
  **閉じているときは分割そのものを作らない**（幅ゼロのペインを残すと、
  掴めるのに何も動かない仕切り線が画面に残る）
- 既定は閉。追加の API 費用がかかる機能なので、使うと決めた人だけが開く
- `debateTurn.ts` に純ロジックを切り出した。
  `nextPhase` は `rebuttal → done` で打ち止めで、**自動で次の往復を始める経路が無い**
- 走行中は `debateGate` が `running` を返して再実行を拒否（二重課金の防止）
- 批判側を中断したら反論へ進まない（片側だけの指摘で終わらせる）
- **反論はメイン分析と同じプロバイダが答える。**
  「元の分析を書いた本人が答える」構図にするため

**対応（③ プロンプトの秘匿）**:
- 批判・反論のプロンプトは `prompts/debate_{bear,bull}.md` に置き `include_str!`。
  フロントが送るのは `debate: "bear" | "bull"` だけで、本文は返さない
  （20 項目分析と同じ方針）

**変更ファイル**: `src-tauri/src/debate.rs`（新規）,
`src-tauri/src/prompts/debate_{bear,bull}.md`（新規）,
`src-tauri/src/{llm/mod,settings,commands,lib}.rs`,
`src/lib/debate/{debateTurn,debateStore}.ts`（新規）, `src/lib/ui/debateLayout.ts`（新規）,
`src/components/{DebateSettings,DebatePanel,AnalysisWithDebate}.tsx`（新規）

**コミット**: `56bab50`

**備考**: このコミットには LP のキャッチコピー変更が同梱されている（利用者による編集を
`git add -A` で巻き込んだもの）。分割不要との判断を受けてそのままにしてある。

---

## 2026-08-03 — 設定モーダルの収まり ＋ SEC UA 補完 ＋ 同期ガイド ＋ 履歴削除

**指示**: ①設定モーダルのタブ名が単語の途中で折り返すのを止め（`white-space: nowrap`）、本体に `max-height: 85vh / max-width: 90vw / overflow: auto` を効かせて縦横スクロールで操作できるようにする。②SEC の User-Agent にメールだけ入力されたら `StoQ-App test@example.com` のように補完して送る。③クラウド同期タブに折り畳みの手順ガイドを追加する。④分析履歴を 1 件ずつ削除できるゴミ箱アイコンと削除処理を実装する。

**対応（① モーダル）**:
- タブに `whitespace-nowrap` ＋ `shrink-0`、タブ行に `overflow-x-auto`。
  **入りきらないときは縮めずに送る。** 単語の途中で折り返すと、
  どのタブなのか読み取れなくなる
- **`max-w-[90vw]` を直接足さなかった。** `maxWidthClass`（`max-w-3xl` 等）と
  max-width が二重になり、どちらが勝つかが CSS の並び順まかせになる。
  代わりに覆いの余白を `px-[5vw] py-[7.5vh]` にして、
  外側から 90vw × 85vh に収めた（本体には `max-h-[85vh]` も明示）
- 中身を `overflow-y-auto` → `overflow-auto` に。**横にも溢れさせる。**
  窓が細いときに圧縮して崩すより、形のまま横スクロールさせたほうが操作できる

**対応（② SEC User-Agent）**:
- `edgar::normalize_user_agent` を新設（旧 `validate_user_agent`）。
  メールだけなら `StoQ-App` を前置きする
- **空白を含むものはそのまま通す。** 名前が書かれているところへ重ねると
  `StoQ-App StoQ v1 a@b.c` になってしまう
- 空・`@` 無しは従来どおり弾く（SEC の要求を満たさないため）
- テスト 5 件（補完 / 前後の空白 / 名前入りは素通し / 弾く条件 / 補完後の形）

**対応（③ 同期ガイド）**:
- `CloudSyncGuide.tsx` を新設（1 機能 1 ファイル）。**既定は閉じておく。**
  設定を済ませた人に毎回読ませる必要はなく、
  開いたままだと肝心の接続ボタンが下へ押し出される
- 文面は指示のまま辞書へ（`cloud.guide.*`）

**対応（④ 履歴削除）**:
- Rust 側の `analysis_history_delete` は既にあったので、フロントの導線を用意した
- **開く操作とは別のボタンにする。** 行全体が「開く」ボタンなので、
  中に入れ子にすると押し分けられない
- **消す前に確認ダイアログを挟む。** 分析は作り直すのに API 費用と時間がかかる。
  本文にどの銘柄のどの期かを書いて、取り違えたまま消させない
- `removeArchiveEntry` は**画面から先に消して、失敗したら戻す**。
  読み直しを待つと消したはずの行が残り、押し損ねたと思って二度押しされる
- 開いていた本文が消した行のものなら畳む（消えた記録を読ませない）

**ついでに直したもの**:
- 直書き検出テストが **`{式} 日本語` の形を素通り**していた。
  `>...<` の走査は `{}` を含む区間を弾くので、
  `{count} 期分` のような「値のあとに単位を足す」書き方が抜けていた
- 塞いだ結果 11 ファイル・21 件の未辞書化が出てきたので、すべて辞書へ移した
  （辞書は 927 → 948 キー）

**変更ファイル**: `src-tauri/src/edgar.rs`, `src/components/CloudSyncGuide.tsx`（新規）,
`src/components/{ModalShell,SettingsModal,CloudSyncSettings,PortfolioHistoryPanel}.tsx`,
`src/lib/portfolio/portfolioStore.ts`, `src/lib/i18n/localized.test.ts`,
`src/locales/{ja,en}.json`, ほか辞書化した 11 コンポーネント

**コミット**: `c754f65`

**備考**: ③のガイド文面（「同期用フォルダを作成し本画面で選択」）は、
**いまのクラウド同期の作りと合っていない。**
実装は Google Drive の OAuth ＋ アプリ専用領域で、フォルダを選ぶ画面は無い。
指示どおりの文面で入れてあるが、要確認。

---

## 2026-08-02 — LP: ローソク足のヒーロー背景（hero.svg）＋ トライアル条件カード

**指示**: ①移動平均線の無いダークモードのローソク足チャート `lp/hero.svg` を作り（背景 #0d1117 / 格子 #1f2937 / 緑 #00e676・赤 #ff5252 / 100 本程度 / 緩やかな上昇 → 放物線的な暴騰 → 鋭い急落）、`.hero` の背景に敷いて暗いオーバーレイを重ねる。②〜⑦は既存実装の再確認（比較カードの改行と行揃え、Core Value、V0 特別価格、同意 5 項目、SmartScreen、シナリオページ）。⑧再デプロイ。（作業中に追加指示: 「数分ではじめられます」の直上へ無料トライアルの条件カードを追加）

**対応（① hero.svg）**:
- 100 本のローソク足のみ。**移動平均線・トレンドラインは入れない。**
  線を引くと「そのラインで売買せよ」という含みが出て、投資助言に寄って見える
- 値動きは 3 局面（〜58 本目: 緩やかな上昇 / 〜80 本目: 放物線で暴騰 / 以降: 急落）。
  **急落は上昇局面の帯で止めた。** 始値を割るまで落とすと縦の縮尺が急落に食われ、
  前半の緩やかな上昇が潰れて読めなくなる
- 天井付近と急落局面だけヒゲを長くしている（値幅が荒れている様子を出すため）
- 生成は固定シードのスクリプト。再生成しても同じ絵になる（差分が暴れない）
- 実体の高さに下限 3.5px を入れた。前半の小さい足が線に見えてしまうため
- **data URI をやめて外部ファイル参照に変えた。** 差し替えが URL 1 行で済み、
  HTML の差分にも巨大な base64 が出てこない
- **ぼかしを外した。** ローソク足だと分かる粒度で見せたいので、
  読みやすさは `rgba(13, 17, 23, 0.85)` のオーバーレイ側で確保する。
  下端はページ本体（slate-950）へ寄せて、セクションの境目が線に見えないようにした

**対応（②〜⑦）**: いずれも直前までの改修で実装済み。
公開中の `gh-pages` の実物と突き合わせて反映を確認した（12 項目すべて一致）。

**対応（追加 トライアル条件カード）**:
- ダウンロードボタンの**手前**に配置。落としてから制限を知るのがいちばん悪い順番になる
- 10 銘柄 / 初回起動から 3 週間 → 超過後は新規銘柄の追加がロック →
  アクティベーションキーで永続・無制限に解錠、の 3 点を緑枠のカードで明記
- **解錠後に何が起きるかまで同じ枠に書いた。**
  制限だけを読ませると「試用版の押し売り」に見える

**変更ファイル**: `lp/hero.svg`（新規）, `lp/index.html`, `lp/README.md`

**コミット**: `60cb7ed`, `9032437`

**備考**: gh-pages へ反映済み（`1b9aa5a` → `ec36cc1`）。

---
