# 改修記録 — StockAnalyzer

指示を受けて改修を行うたびに、**新しい項目を先頭に**追記する。

書式:

```
## YYYY-MM-DD — 見出し
**指示**: ユーザーからの指示（要約）
**対応**: 実際に行った変更
**変更ファイル**: 追加/変更/削除したファイル
**コミット**: <short-sha>
**備考**: 補足・積み残し（なければ省略）
```

---

> 古い記録は [docs/archive/改修記録_2026-08.md](docs/archive/改修記録_2026-08.md) に繰り下げてあります。

---

## 2026-08-02 — LP: ローソク足のヒーロー背景（hero.svg）＋ トライアル条件カード

**指示**: ①移動平均線の無いダークモードのローソク足チャート `lp/hero.svg` を作り（背景 #0d1117 / 格子 #1f2937 / 緑 #00e676・赤 #ff5252 / 100 本程度 / 緩やかな上昇 → 放物線的な暴騰 → 鋭い急落）、`.hero` の背景に敷いて暗いオーバーレイを重ねる。②〜⑦は既存実装の再確認（比較カードの改行と行揃え、Core Value、V0 特別価格、同意 5 項目、SmartScreen、シナリオページ）。⑧再デプロイ。（作業中に追加指示: 「数分ではじめられます」の直上へ無料トライアルの条件カードを追加）

**対応（① hero.svg）**:
- 100 本のローソク足のみ。**移動平均線・トレンドラインは入れない。**
  線を引くと「そのラインで売買せよ」という含みが出て、投資助言に寄って見える
- 値動きは 3 局面（〜58 本目: 緩やかな上昇 / 〜80 本目: 放物線で暴騰 / 以降: 急落）。
  **急落は上昇局面の帯で止めた。** 始値を割るまで落とすと縦の縮尺が急落に食われ、
  前半の緩やかな上昇が潰れて読めなくなる
- 天井付近と急落局面だけヒゲを長くしている（値幅が荒れている様子を出すため）
- 生成は固定シードのスクリプト。再生成しても同じ絵になる（差分が暴れない）
- 実体の高さに下限 3.5px を入れた。前半の小さい足が線に見えてしまうため
- **data URI をやめて外部ファイル参照に変えた。** 差し替えが URL 1 行で済み、
  HTML の差分にも巨大な base64 が出てこない
- **ぼかしを外した。** ローソク足だと分かる粒度で見せたいので、
  読みやすさは `rgba(13, 17, 23, 0.85)` のオーバーレイ側で確保する。
  下端はページ本体（slate-950）へ寄せて、セクションの境目が線に見えないようにした

**対応（②〜⑦）**: いずれも直前までの改修で実装済み。
公開中の `gh-pages` の実物と突き合わせて反映を確認した（12 項目すべて一致）。

**対応（追加 トライアル条件カード）**:
- ダウンロードボタンの**手前**に配置。落としてから制限を知るのがいちばん悪い順番になる
- 10 銘柄 / 初回起動から 3 週間 → 超過後は新規銘柄の追加がロック →
  アクティベーションキーで永続・無制限に解錠、の 3 点を緑枠のカードで明記
- **解錠後に何が起きるかまで同じ枠に書いた。**
  制限だけを読ませると「試用版の押し売り」に見える

**変更ファイル**: `lp/hero.svg`（新規）, `lp/index.html`, `lp/README.md`

**コミット**: `60cb7ed`, `9032437`

**備考**: gh-pages へ反映済み（`1b9aa5a` → `ec36cc1`）。

---

## 2026-08-02 — LP: Core Value セクション ＋ V0 特別価格 ＋ 価格シナリオ検討ページ

**指示**: ①比較カードの見出しを「決算書を読めるようになりたい。／でも、どこから始めれば？」で改行し、左右カードの高さと内部要素の縦位置を揃える。②ヒーロー直後に「Core Value」セクション（3 つのコアバリュー）を新設。③価格を「$228 打ち消し → $62（V0 特別価格）」にし、V1 で通常価格へ戻す注釈を入れる。④`lp/pricing-scenarios.html` を新規作成し、第 2 段階（$98 買い切り / $28 月額）と第 3 段階（$228 買い切り / $49 月額）を並べる。⑤再デプロイ。（作業中に追加指示: 購入前同意へ「メジャーアップデートは有償になる場合がある」を 5 項目目として追加）

**対応（① 比較カードの行揃え）**:
- `<article>` を `flex h-full flex-col` にし、**本文の `<p>` に `flex-1`** を持たせた。
  こうするとポイント枠が下端に落ちて、左右で同じ高さに並ぶ
- 見出しに 2 行ぶんの `md:min-h-[3.5rem]` を確保。
  **見出しの行数が違うと、その下の本文とポイント枠が互い違いにずれる**ので、
  比較して読むページとして成立しなくなる
- 左カードの見出しは指定どおり「決算書を読めるようになりたい。<br>でも、どこから始めれば？」

**対応（② Core Value）**:
- ヒーローの直後に置いた。**「何ができるか」より先に「何をしなくてよくなるか」**を
  見せたい。機能の列挙（`#features`）はそのあとのほうが効く
- 見出しは 2 行構成、緑の強調を 2 行目に。3 枚のハイライトカード
  （自動取得 / 網羅性 / 1 画面集約）は 01・02・03 の番号付き
- カードはここでも `flex-1` で高さを揃えている

**対応（③ V0 特別価格）**:
- `$228` に打ち消し線 → `$62`、その下に「V0 フィードバック期間・数量限定価格」のバッジ
- 「V1 リリースに伴い予告なく通常価格へ改定されます」を枠付きで明記。
  **予告なく上げると書いておかないと、値上げ自体が不誠実に見える**

**対応（④ 価格シナリオページ）**:
- `lp/pricing-scenarios.html` を新規作成。現在（V0）／第 2 段階／第 3 段階の 3 段構え
- **`noindex, nofollow` を入れ、`index.html` からはリンクしない。**
  gh-pages に置く以上 URL は誰でも踏めるので、
  検討中の価格が検索結果に出ると実売価格と食い違って見える
- 最上部に消えない警告帯（内部検討用である旨 ＋ 本番ページへの導線）
- **各月額カードに損益分岐を併記した。** 比較すべきは金額そのものではなく
  「買い切りに追いつくまでの月数」（第 2 段階 4 か月 / 第 3 段階 5 か月）。
  この数字を出さないと、どちらが安いのかを読み手が判断できない
- 決済ボタンは置いていない（検討中の価格で実際に買われるのを防ぐ）

**対応（⑤ 同意項目の追加）**:
- 購入前モーダルに 5 項目目「メジャーアップデートは既存ユーザー特別価格での
  有償アップデートとなる場合がある（通常の不具合修正・マイナーは無料）」を追加。
  **無料の範囲を同じ項目内に書く。** 有償化だけを読ませると、
  以後の修正まで課金されると受け取られる

**変更ファイル**: `lp/index.html`, `lp/pricing-scenarios.html`（新規）

**コミット**: `e762cbf`

**備考**: gh-pages へ反映済み（`e293683` → `a522228`）。
シナリオページは Pages 有効化後 `.../stoq-ai/pricing-scenarios.html` で開ける。

---

## 2026-08-02 — LP: BYOK 解説セクション ＋ 購入前の同意モーダル

**指示**: ①価格（`#pricing`）の上部に、API・外部データ利用に関する解説セクションをカードデザインで新設する（AI 利用料金 4 点 / 財務・市場データ 1 点）。②「ライフタイムライセンスを購入」を押したら決済ページへ直行させず、4 項目の同意を取る確認モーダルを表示する。③再デプロイ。

**対応（① BYOK セクション）**:
- **価格の「手前」に置いた。** 金額を見て納得したあとで
  「別途 API 費用が要る」と知るのが、いちばん不満の残る順番になる
- 2 枚のカード構成。左が AI 利用料金（BYOK / 事前チャージ / Auto-recharge を OFF /
  安価なプロバイダーという選択肢）、右が財務・市場データ（FMP 等の別キー）
- **「Auto-recharge を OFF にすれば予期せぬ課金は起きない」を明記した。**
  従量課金への恐怖が購入前のいちばん大きな引っかかりなので、
  回避手段を同じ場所に書かないと不安だけが残る
- アプリ内に APIキー設定ガイドを同梱している旨も併記（未設定時は初期表示になる）

**対応（② 同意モーダル）**:
- 4 項目のチェックボックス。**全部にチェックが入るまで「購入手続きへ進む」は無効**
- **開き直すたびにチェックを外す。** 前回の状態が残っていると、
  同意を取り直したことにならない
- **`href` は残したまま `preventDefault` で止める。**
  JS が動かない環境では決済ページへ直行する。
  同意を取り損ねるより、買えなくなるほうが損失が大きい
- Esc・背景クリック・キャンセルで閉じ、閉じたら元のボタンへフォーカスを戻す
- 文面は日英併記（`data-en` / `data-ja`）。同意を取る以上、
  読んでいる言語で出ていないと意味が無い

**変更ファイル**: `lp/index.html`

**コミット**: `91439a6`

**備考**: gh-pages へ反映済み（`81729f3` → `e293683`）。
チェック項目の文面は、決済代行（Lemon Squeezy）の審査で参照される想定。

---

## 2026-08-02 — LP: 対象読者セクションの刷新 ＋ SmartScreen 注意書き ＋ 改行調整

**指示**: ①「Persona」「ペルソナ」という語を一切使わず、見出しを「こんな方のためのツールです」へ変更し、カードを 左＝初心者（脱・初級者）／右＝中級者 の順に並べ替える（本文・ポイントの文面は指定どおり）。②ダウンロードボタン周辺に Windows SmartScreen の注意書きを追加。③メインキャッチコピーを「数字に基づく、」の直後で改行して 3 行にする。④`git subtree push --prefix lp origin gh-pages` で再公開。

**対応（① 対象読者セクション）**:
- 語を消すだけでなく**アンカー ID も `#personas` → `#audience` に変更**した。
  ID はアドレスバーとナビのリンクに露出するので、見出しだけ直しても
  「personas」の語が URL に残ってしまう
- カードは 左＝「脱・初級者を目指す方へ」／右＝「中級者以上の方へ」。
  **並びは読む順そのもの**なので、リード文（入口はふたつ）と合わせて固定した
- 初心者カードのポイント末尾に「デフォルトモード」の一文を強調色で追加。
  「難しそう」で離脱する層に、最初の 1 回が押せることを先に伝える
- 英日の両方を `data-en` / `data-ja` で用意（66 対、左右で欠けが無いことを確認）

**対応（② SmartScreen）**:
- 動作環境の注記直下に、枠付きの警告文を追加。「詳細情報」「実行」を太字にした
- **コード署名が無い以上この青画面は必ず出る。**
  先に伝えておかないと「危険なソフト」と受け取られて、
  ダウンロードまで来た人をそこで失う。署名を導入したら削除してよい旨を
  `lp/README.md` に残した

**対応（③ 改行）**:
- `熱狂に飛び乗らない。<br />数字に基づく、<br />確かな銘柄選び。` の 3 行へ。
  **日本語の自動折り返しは読点を無視する**ので、幅によっては
  「数字に基づく、確かな」で切れて読みにくくなる。意味の切れ目で明示的に折る

**変更ファイル**: `lp/index.html`, `lp/README.md`

**コミット**: `766170c`, `1ccb88c`（記録の追記は `90f3ced`）

**備考**: gh-pages へ反映済み（`e365563` → `81729f3`）。公開 URL は
GitHub の Settings → Pages で `gh-pages` / `(root)` を有効化すると
`https://motojet-bit.github.io/stoq-ai/` で見えるようになる。

---

## 2026-08-02 — 66章: LP のブラッシュアップ ＋ 67章: アプリの残り日本語の辞書化

**指示**: ①アプリ内に残っている日本語直書きを英語化（辞書化）する。②LP を作り込む — 言語切替を「🇯🇵 日本語」表記へ、価格を $98 → $62 へ、アップグレード案内の注記、初心者向けデフォルトモードの訴求、「こんな人のために」セクション、背景画像。③検証してデプロイ。

**対応（67章 アプリ）**:
- 走査スクリプトを掛け直して残存分を洗い出し、辞書へ移した（ja/en 各 916 キー）
- **辞書の値と一致する文字列を除外していた初版の走査に漏れがあった**（52 件）。
  「保存」「キャンセル」のような短い語は辞書にも直書きにも同じ形で現れるため、
  一致除外を外して再走査している
- `localized.test.ts`（直書き検出テスト）の対象ファイルを追加し、再発を防いだ

**対応（66章 LP）**:
- 価格を $62 に統一し、「将来的な大規模アップデートの際は差額でのアップグレードを
  ご案内する場合があります」を併記。**後から値上げする余地を最初に明示しておく**
- 背景画像は SVG のデータ URI ＋ 暗いオーバーレイ。
  外部画像を読み込むと GitHub Pages 以外への依存が増え、差し替えも面倒になる
- 言語切替は 🇯🇵 / 🇺🇸 のトグル。`html[lang]` を切り替えて CSS で出し分ける
  （JS で DOM を組み替えないので、切替時にちらつかない）

**変更ファイル**: `src/locales/{ja,en}.json`, `src/**`（直書きの置換）,
`src/lib/i18n/localized.test.ts`, `lp/index.html`, `lp/README.md`

**コミット**: `c9a58bb`（LP の決済 URL 設定は `570e2bc`、窓口アドレスは `eae65ce`）

---

## 2026-08-02 — 販売 LP の作成と GitHub Pages 公開

**指示**: Lemon Squeezy のストア審査用およびユーザー向けの販売 LP を作成し、GitHub Pages で公開する。リモートを設定して `main` と `gh-pages` を push する。

**対応**:
- `lp/index.html` の 1 枚もの。**外部 CSS / JS を一切読み込まない**
  （審査で「壊れている」と見られないよう、依存をゼロにする）
- 日英併記。`data-en` / `data-ja` を両方 HTML に置き、`html[lang]` で出し分ける
- 免責（投資助言ではない旨）とサポート窓口をフッターに明記。審査で必ず見られる
- 公開は**専用の `gh-pages` ブランチ**（`git subtree push --prefix lp`）。
  `/docs` 公開にしなかったのは、**`docs/` に署名鍵の置き場所を含む内部メモがある**ため
- 公開リポジトリで実アドレスが露出しないよう、
  過去 80 コミットの author/committer を noreply へ書き換えてから push した

**変更ファイル**: `lp/index.html`, `lp/README.md`, `lp/.nojekyll`

**コミット**: `301d588`, `eae65ce`, `570e2bc`

**備考**: ダウンロード先の `stoq-releases` にまだリリースが無いため、
ダウンロードボタンは現在 404。初回リリースを作れば解消する。

---

## 2026-08-02 — GitHub Releases を配信元とした自動更新

**指示**: 起動時の自動更新チェックと手動チェックを、GitHub Releases 配信で組み込む。エンドポイントは `https://github.com/motojet-bit/stoq-releases/releases/latest/download/latest.json`。

**対応**:
- `tauri-plugin-updater` / `tauri-plugin-process` を追加し、
  `bundle.createUpdaterArtifacts: true` で署名付き成果物（`.sig`）を出す
- **`latest.json` は Tauri が作ってくれない。**
  `scripts/make-latest-json.mjs`（`npm run release:manifest`）で
  バージョン・署名・URL を組み立てる手順にした
- 署名鍵はリポジトリ外（`~/.stoq-updater/stoq.key`）に置き、`.gitignore` に `*.key`。
  **この鍵を失うと、配布済みのアプリへ二度と更新を届けられない**
- ビルド時は `TAURI_SIGNING_PRIVATE_KEY` に**鍵の中身**を入れる。
  `..._PATH` だけだと署名が黙ってスキップされ、`.sig` が出ないまま完成してしまう
- バージョン比較は `updateVersion.ts` に切り出してテスト（文字列比較だと 1.10 < 1.9 になる）
- `cargo add` が RC 版（`2.0.0-rc.4`）を掴んだので `"2"` に固定して `cargo update`

**変更ファイル**: `src-tauri/{Cargo.toml,tauri.conf.json,src/lib.rs}`,
`src/lib/update/*`, `src/components/{UpdateModal,UpdateSettings}.tsx`,
`scripts/make-latest-json.mjs`, `docs/リリース手順.md`

**コミット**: `69a2216`

---

## 2026-08-02 — i18n A〜F グループ（英語を Source of Truth へ反転）

**指示**: 基本言語を英語（en）に反転し、A（画面 UI）→ B（通知・エラー）→ C（分析ドメイン）→ E（法務・案内）→ F（Rust エラーコード化）の順に辞書化する。C・D・E の内部キー（英語 ID）は絶対に落とさない。秘匿システムプロンプト本文は変更しない。

**対応（方針）**:
- `DEFAULT_LOCALE` を `"en"` に反転。**未訳のときに日本語へ落ちない**ようにする
- 表示言語の判定（`detectLocale`）と、保存値の正規化（`normalizeLocale`）は別物として分けた。
  前者は「ja で始まれば日本語、それ以外は英語」、後者は「知らない値なら既定へ」

**対応（各グループ）**:
- **A（61 ファイル）**: 画面の直書きを辞書へ。
  **`export const X = t("...")` の形は禁止**。モジュール読み込み時に値が焼き付き、
  言語を切り替えても変わらなくなるため、すべて関数へ変えた
- **B（29 ファイル / 165 件）**: 通知・エラー文。日付整形も併せて修正
- **C（分析ドメイン）**: **内部キーと表示ラベルを分離**した。
  `criterion.id` / `KeyMetric.key` / `threshold.id` / `SCORE_BLOCKS.id` は
  保存済みデータと突き合わせる鍵なので訳さない。
  Yahoo の指標引き当て文字列（`営業CF` など）も日本語のまま残す。
  **許可リストが古くなっていないことを検査するテスト**を足して、
  「訳し忘れ」と「訳してはいけないものを訳した」の両方を落とせるようにした
- **E（法務・案内）**: 免責・ロック案内・ツールチップを辞書へ
- **F（Rust）**: `AppError::msg` を全廃し、`AppError::Coded{code, detail}` へ。
  41 個の `ERR_*` を定義し、フロントは `errors.${code}` で辞書を引く。
  **文面を Rust に持たせない**ので、翻訳がフロント側だけで完結する（197 か所を変換）
- 直書き検出テスト（`localized.test.ts`）を新設。
  コメントと正規表現リテラルを読み飛ばす走査にしてある
  （`/[",\n\r]/` の文字クラスを文字列開始と誤検出していたため）

**変更ファイル**: `src/locales/{ja,en}.json`（各 916 キー）,
`src/lib/i18n/*`, `src/lib/errors/errorMessage.ts`,
`src-tauri/src/error.rs` ほか多数

**コミット**: `1bfc3b2`, `b59c699`, `36f446c`, `95961f4`, `ab812bc`

**備考**: 言語を切り替えるテストは `setLocale("ja")` を**トップレベル**に置くこと。
`beforeAll` では、モジュール読み込み時に組み上がる定数に間に合わない。

---
