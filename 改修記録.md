# 改修記録 — StockAnalyzer

指示を受けて改修を行うたびに、**新しい項目を先頭に**追記する。

書式:

```
## YYYY-MM-DD — 見出し
**指示**: ユーザーからの指示（要約）
**対応**: 実際に行った変更
**変更ファイル**: 追加/変更/削除したファイル
**コミット**: <short-sha>
**備考**: 補足・積み残し（なければ省略）
```

---

> 古い記録は [docs/archive/改修記録_2026-08.md](docs/archive/改修記録_2026-08.md) に繰り下げてあります。

---

## 2026-08-01 — Phase 2 Step 5.4: IBKR 風の操作感へ修正（リサイズ方向・パネル移動・フォント全域）

**指示**: 添付の IBKR トレーディングツール UI を参考に 5 件。①（最優先）ドラッグリサイズ方向の反転と非固定化 — 上に引くと下の対話が広がり、下に引くと上の市場データが広がる。空のワークスペースでもリサイズ可能に。ウィンドウ縮小時も CSS の固定値（高さ固定等）を完全解除してスクロールバーを正常化。②ヘッダーのグリップアイコン（サイコロの6）でパネルを実際に移動・入れ替えできるようにする。③フォント調整 UI を分析結果パネルから右上「AI設定」ボタン下へ移設。④フォント上限を 28px に拡張し、メニューバー・資料チップ・サイドバーを含むアプリ全域へ連動。⑤`npm run verify` 等で高さ計算とフォント保存・全域反映を検証。

**対応（① リサイズ）**:
- **原因は `deltaY` の符号ではなく、どちらのペインに実サイズを持たせるかだった。**
  従来は 2 番目のペインを固定サイズにしていたため 1 番目が `flex-1` になり、
  **中身が空（ティッカー未入力）のときに 1 番目が 0 まで潰れて仕切り線を掴めなかった**
- `splitMath.ts` を `clampSecondSize` → `clampFirstSize` + `desiredFirstSize` に作り替え、
  **1 番目に px を与えて 2 番目に残りを割り当てる**方式へ変更。
  仕切り線の位置＝ポインタ位置になり、下へ引けば上の市場データが広がり、
  上へ引けば下の対話ウィンドウが広がる、という直感どおりの挙動になった
- `ResizableSplit` に `ResizeObserver` を追加。ウィンドウを縮めても固定 px が残らず、
  常に両ペインの最小サイズを満たす範囲へ入れ直す
- 仕切り線の当たり判定を 1px → 5px に拡大し、`pointercancel` でも確実にドラッグを終了
- `.panel` の `height: 100%` を `flex: 1 1 0%` に変更して CSS の固定高を撤廃。
  コンポーネント側の固定高 24 箇所も `min-h-*` に置換した

**対応（② パネル移動）**:
- **グリップ（サイコロの 6）だけを `draggable`** にした。ヘッダー全体を対象にすると
  中のボタンやスライダーが掴めなくなるため
- ドロップ先はヘッダー全体。`application/x-stoq-panel` を持つドラッグのときだけ枠が光り、
  離すと `layoutStore.movePanel` で配置が入れ替わる（localStorage に保存）

**対応（③④ フォント）**:
- `FontSizeControl` を分析結果パネルのヘッダーから `CommandBar`（AI設定ボタンの直下）へ移設
- `MAX_FONT_SIZE` を 20 → 28 に拡張
- `:root` 自体に `font-size: var(--fs-base)` を設定し、**個別指定の無い要素も一律連動**
- 固定 px クラス 84 箇所（メニューバー・タブ・サイドバー・資料チップ・トースト・
  モデル入力欄など）を `.t-body` / `.t-label` / `.t-heading` / `.t-mono` に置換

**対応（⑤ 検証）**:
- `npm run verify`（typecheck → test → build）✅ / `cargo test --lib` ✅
- フロント 64 件 / Rust 35 件。**ドラッグ方向の意味を検証するテストを新規追加**
  （下へ引くと 1 番目が広がる / 上へ引くと 1 番目が縮む / 仕切りがポインタに追従する）

**変更ファイル**: `src/lib/ui/splitMath.ts` / `splitMath.test.ts` / `fontStore.ts` / `fontStore.test.ts`、
`src/components/ResizableSplit.tsx` / `PanelHeader.tsx` / `FontSizeControl.tsx` / `CommandBar.tsx` /
`AnalysisPanel.tsx` ほか UI コンポーネント 19 ファイル、`src/App.tsx`、`src/styles.css`、
`PROGRESS.md`（履歴を `docs/archive/PROGRESS_履歴_2026-08.md` へ繰り下げ）、`改修記録.md`

**コミット**: (次のコミットで記録)

---

## 2026-08-01 — Phase 2 Step 5.3: レイアウト修復・フォント可変・パネル移動・自動テスト整備

**指示**: マスター要件 6 件。①UIレイアウト崩れ（`min-height: 0` / `overflow-y: auto`）とスクロール・入力欄の修復、仕切り線の復元、パネルのドラッグ配置。②ロゴ変更とフォント 10〜20px の 1px 刻み調整。③分析根拠の明記と一次資料未添付時の確認モーダル。④チャット履歴の SQLite 実機能化。⑤自動テストの徹底（型チェック・ビルド・Unit Test、リサイズ境界値、フォント変更時の要素非表示、永続化のエッジケース）。

**着手前の確認**: ②のロゴ・③・④は Step 5.2 で実装済みだったため、
**新規に必要なのは①（レイアウト修復・パネル移動）／②のフォント可変／⑤（自動テスト）**と判断した。

**対応（① レイアウト）**:
- `styles.css` に `.panel` / `.panel-scroll` を定義。**`min-height: 0` を入れないと
  Flex 子要素が内容の高さを主張してスクロールせず枠外へ突き抜ける**ため、
  全パネルの骨格として共通化した
- 対話パネルの入力欄を `shrink-0` + `min-h-11` に固定し、textarea に `max-h-40` と
  独立スクロールを付与。**フォントを 20px にしても送信ボタンが埋もれない**
- スプリッターの当たり判定を 1px → 4px に広げ、`setPointerCapture` で
  ドラッグ中にポインタを取りこぼさないようにした
- `PanelHeader` をドラッグ可能にし、他パネルのヘッダーへ落とすと配置が入れ替わる。
  配置は `layoutStore`（localStorage 保存）で管理し、壊れた保存値も自動で正規化する

**対応（② フォント可変）**:
- `textScale.ts`（4 段階）を廃止し `fontStore.ts` を新設。
  10〜20px を 1px 刻みで調整でき、`:root` の CSS 変数 `--fs-base` を書き換える方式に変更
- 派生サイズ（`--fs-label` など）と行間は CSS 側で計算。
  コンポーネントは `.t-body` / `.t-label` / `.t-heading` を付けるだけでよい
- `A− / A+` に加えてスライダーと px 表示を追加（`FontSizeControl`）

**対応（⑤ 自動テスト）**:
- `vitest` + `jsdom` を導入し、`npm run test` / `npm run verify` を追加
- **フロントエンド 57 件**: リサイズ境界値 / フォント範囲 / 配置の正規化 /
  分析結果パーサのエッジケース / プロンプト予算 / トークン概算
- **Rust 35 件**: チャット履歴（作成・自動命名・一覧・リネーム・削除 CASCADE・
  存在しない ID の例外）/ 日付突き合わせ / マスク処理 / HTML 抽出 / トークン概算
- DB ロジックを `*_in(conn, …)` に分離し、Tauri なしでテストできるようにした

**テストが検出した不具合 2 件**:
1. スマート圧縮でセグメント明細が 1 行落ちていた → 数値明細セクションの
   優先確保の閾値を 0.6 → 0.75 に引き上げ
2. HTML 抽出が日本語の途中に空白を入れていた（「売上高は 512億 ドル」）
   → 直前が CJK なら空白を入れないよう修正

**変更ファイル**:
- 追加: `src/lib/ui/fontStore.ts`, `layoutStore.ts`, `splitMath.ts`, `src/components/FontSizeControl.tsx`
- 追加: `vitest.config.ts` とテスト 6 ファイル（`*.test.ts`）
- 追加: Rust テスト（`chats.rs` / `quarterly.rs` / `settings.rs` / `documents.rs` / `html.rs`）
- 削除: `src/lib/ui/textScale.ts`
- 変更: `src/styles.css`, `App.tsx`, `PanelHeader.tsx`, `ResizableSplit.tsx`,
  `AnalysisPanel.tsx`, `ChatPanel.tsx`, `WorkspacePanel.tsx`, `MetricCard.tsx`,
  `CriterionScoreRow.tsx`, `QuarterlyTrend.tsx`, `FilingStatusBadge.tsx`, `Icons.tsx`
- 変更: `src-tauri/src/chats.rs`（テスト用に分離）, `html.rs`, `package.json`
- 変更: `docs/設計.md`, `PROGRESS.md`

**コミット**: `5a6b83c`

**検証（5 工程すべて通過）**:
| 工程 | 結果 |
| --- | --- |
| `npm run typecheck` | ✅ |
| `npm run test`（フロント） | ✅ 57 件 |
| `npm run build` | ✅ |
| `cargo check` | ✅ |
| `cargo test --lib`（Rust） | ✅ 35 件 |

**備考**: 実際の描画（スクロールバーの見え方・ドラッグ操作感）はユーザー側での確認が必要。

---

## 2026-08-01 — Phase 2 Step 5.2: 2 カラム化・分析根拠表示・チャット履歴の実機能化

**指示**: 6 件を一括で実装。①ロゴを『StoQ AI Analyzer』に変更。②分析結果パネルのヘッダー下部に分析根拠（使用データ元）を表示。③一次資料が無い状態で「AI分析を実行」を押したら確認ダイアログ（指定文面 / [戻る] / [このまま分析する]）。④左カラム（上: 市場データ / 下: 対話）と右カラム（分析結果）の 2 カラムにし、仕切り線を画面最下部まで一直線に。市場データに縦スクロール。⑤文字サイズを 4 段階（13/15/17/19px）に拡張し全パネルへ適用。⑥左サイドバーのチャット履歴を SQLite と連携し、保存・切替・削除・リネームを実装。

**対応**:
1. `MenuBar` のロゴを「StoQ AI Analyzer」に変更
2. `AnalysisRun.basis` を追加し、実行時に「財務指標(YF) / 四半期推移 4Q /
   SEC開示書類 10-Q (日付) / 添付資料: ファイル名 ほか (N件)」を組み立ててバッジ表示。
   `analyses` テーブルに `basis` 列を追加（既存 DB は `ALTER TABLE` で移行）し、
   **復元した過去の分析でも当時のデータ元が分かる**ようにした
3. 添付 0 件のときだけ `ConfirmDialog` を挟む。1 件以上なら即実行
4. **外側を左右分割・左カラムの内側を上下分割**に組み替え。
   この順序にすることで左右の仕切りが最下部まで途切れずに伸びる
5. `textScale.ts` を 4 段階に拡張し、`MetricCard` / `ChatPanel` にも適用。
   旧保存値（small/normal/large）は読み込み時に読み替える
6. `chats.rs`（`chat_sessions` / `chat_messages`、CASCADE 付き）と
   `chatStore.ts` を新設。`ChatHistoryItem` でリネーム・削除の UI を実装。
   ダミーの `SAMPLE_SESSIONS` は削除

**設計上の判断**:
- 仕切り線を最下部まで伸ばすため、**分割の入れ子の順序を逆にした**
  （従来: 上下 → 上段を左右 ／ 変更後: 左右 → 左を上下）
- ユーザー発言は LLM の応答を待たずに先に保存する。応答が失敗しても質問が残るため
- 未命名セッションは最初のユーザー発言の先頭 40 文字を自動タイトルにする

**変更ファイル**:
- 追加: `src-tauri/src/chats.rs`, `src/lib/chat/chatStore.ts`, `src/components/ChatHistoryItem.tsx`
- 変更: `src-tauri/src/analyses.rs`（basis 列 + 移行）, `commands.rs`, `lib.rs`
- 変更: `src/App.tsx`, `MenuBar.tsx`, `Sidebar.tsx`, `ChatPanel.tsx`, `AnalysisPanel.tsx`, `MetricCard.tsx`
- 変更: `src/lib/ui/textScale.ts`, `src/lib/prompts/analysisRunner.ts`, `src/lib/sampleData.ts`, `src/types/index.ts`
- 変更: `docs/設計.md`, `PROGRESS.md`

**コミット**: `4d04077`

**検証**: `cargo check` / `npm run typecheck` / `npm run build` 通過。
チャット履歴の SQLite ロジックを直接実行し、
**作成 / 自動命名（40 文字超の省略含む）/ 一覧（更新順・件数）/ リネーム /
メッセージ復元 / 削除（CASCADE でメッセージも消える）**が
すべて期待どおりであることを確認した。

---

## 2026-08-01 — Phase 2 Step 5.1: 3 ペイン構成への再構築

**指示**: ①現在のレイアウトを破棄し、左半分＝YF指標・四半期モメンタム・SEC情報、右半分＝20項目の分析結果（縦長）、下部1/3＝AIとの対話（横長）の 3 ペイン構成にする。境界はドラッグでリサイズ可能、不要なパネルは「_」ボタンで折りたたみ可能に。文字サイズ変更は右ペインに保持。②「一括クリア」に確認モーダルを出す（警告文とボタン [このままリセット] / [もどる] を指定）。③右上のプロバイダ切替を 1 ボタン + ドロップダウンにまとめる。

**対応**:
- レイアウトを 3 ペイン構成へ再構築。`ResizableSplit` を入れ子にして
  「上段（左右分割）＋下段（対話）」を組んだ
  - 上段左: `WorkspacePanel`（`PanelHeader` を追加）
  - 上段右: `AnalysisPanel`（縦長。A− / A+ はそのまま維持）
  - 下段: `ChatPanel`（全幅・高さ 1/3）
  - 初期サイズは `window.innerWidth / 2` と `window.innerHeight / 3` から算出
- `PanelHeader` の折りたたみボタンをシェブロンから **`_`（最小化）/ `□`（復元）** に変更
- 左右は同時に畳めないようにした（片方を畳むともう片方が展開される）
- 配置切替が不要になったため `BottomDock` を削除
- `ConfirmDialog` を新設し、一括クリアに 2 段階確認を追加。
  指定どおりの警告文と [このままリセット] / [もどる] を配置し、
  削除件数と概算トークン数も表示。既定フォーカスは「もどる」
- `ProviderMenu` を新設し、`ApiKeyIndicator`（横並び 3 個）を置き換え。
  カスタムプロバイダも一覧に含め、未設定の理由を各項目に表示

**変更ファイル**:
- 追加: `src/components/ConfirmDialog.tsx`, `ProviderMenu.tsx`
- 削除: `src/components/BottomDock.tsx`, `ApiKeyIndicator.tsx`
- 変更: `src/App.tsx`, `WorkspacePanel.tsx`, `PanelHeader.tsx`, `CommandBar.tsx`, `DocumentTray.tsx`, `Icons.tsx`
- 変更: `docs/設計.md`, `PROGRESS.md`

**コミット**: `37ccf29`

**検証**: `npm run typecheck` / `npm run build` 通過。

**備考**: レイアウトの操作感（リサイズ・最小化・ドロップダウン）はユーザー側での確認が必要。

---

## 2026-08-01 — Phase 2 Step 5: フレキシブル UI ＆ 分析結果の永続化キャッシュ

**指示**: ①分析結果と対話の並びを横並びだけでなく縦並びにも切替可能（または縦並びをデフォルト）にし、境界ドラッグでのリサイズを導入。各パネルに折りたたみボタン。20項目評価の文字サイズ・行間を調整して可読性を高める。②生成した分析結果を SQLite へ自動保存し、再起動やタブを閉じても銘柄を開けば復元されるようにする。「分析結果をクリア」ボタンを置き、明示操作時のみ削除できるようにする。

**対応（①レイアウト・可読性）**:
- `ResizableSplit.tsx` を新設。縦並び / 横並びの両方に対応する汎用スプリット。
  旧 `SplitPane.tsx`（縦専用）は削除
- `BottomDock.tsx` を新設。配置切替ボタン、リサイズ、折りたたみを統括。
  **既定は縦並び**（20項目テーブルは横幅を使うほうが読みやすい）。選択は localStorage 保存
- `PanelHeader.tsx` を新設。折りたたみボタンを共通化し、畳むとヘッダーだけ残る
- `textScale.ts` を新設。3 段階（小 12px / 標準 13px / 大 15px）と行間を定義し、
  分析結果パネルの `A− / A+` で切替。localStorage に保存
- 評価テーブルの列幅を広げ、スコアドットを拡大。対話パネルも 13px / 行間 1.85 に

**対応（②永続化）**:
- `src-tauri/src/analyses.rs` を新設。`rusqlite`（`bundled`）で
  `<app_data_dir>/analyses.db` に保存
- コマンド 4 種を追加（save / load / list / delete）
- 生成完了と同時に自動保存。中断時も途中結果を保存する
- 銘柄タブを開くと自動復元。復元した結果はヘッダーに保存日時を表示
- 「クリア」ボタンは画面表示と DB の両方を削除

**設計上の判断**:
- 1 銘柄につき最新 1 件のみ保持（`ticker` を主キーに UPSERT）。
  再分析は上書きとし、履歴は持たない
- `rusqlite` は `bundled` 機能で SQLite 本体を同梱。実行環境への依存をなくすため
- 復元は「実行中・表示中なら何もしない」条件付き。
  ストリーミング中に古い結果で上書きされる事故を防ぐ

**変更ファイル**:
- 追加: `src-tauri/src/analyses.rs`
- 追加: `src/components/ResizableSplit.tsx`, `BottomDock.tsx`, `PanelHeader.tsx`
- 追加: `src/lib/ui/textScale.ts`
- 削除: `src/components/SplitPane.tsx`
- 変更: `src-tauri/Cargo.toml`（rusqlite 追加）, `commands.rs`, `lib.rs`
- 変更: `src/components/AnalysisPanel.tsx`, `ChatPanel.tsx`, `CriterionScoreRow.tsx`, `Icons.tsx`
- 変更: `src/lib/prompts/analysisRunner.ts`, `src/App.tsx`, `src/types/index.ts`, `src/styles.css`
- 変更: `docs/設計.md`, `PROGRESS.md`

**コミット**: `213ab17`

**検証**: `cargo check` / `npm run typecheck` / `npm run build` 通過。
SQLite のロジックを Tauri なしで直接実行し、
**保存 / 同一銘柄の上書き / 復元 / 一覧（新しい順）/ 個別削除**が
すべて期待どおりであることを確認した。

**備考**: UI 操作（配置切替・折りたたみ・文字サイズ・クリア）はユーザー側での確認が必要。

---

