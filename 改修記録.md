# 改修記録 — StockAnalyzer

指示を受けて改修を行うたびに、**新しい項目を先頭に**追記する。

書式:

```
## YYYY-MM-DD — 見出し
**指示**: ユーザーからの指示（要約）
**対応**: 実際に行った変更
**変更ファイル**: 追加/変更/削除したファイル
**コミット**: <short-sha>
**備考**: 補足・積み残し（なければ省略）
```

---

> 古い記録は [docs/archive/改修記録_2026-08.md](docs/archive/改修記録_2026-08.md) に繰り下げてあります。

---

## 2026-08-03 — LP: 購入前ダイアログの件数ずれ（4 点 → 5 点）を修正

**指示**: 購入前の確認ダイアログが「以下の 4 点」のままだが、項目は 5 つある。修正すること。

**対応**:
- 文言を 5 点へ（項目は V0 価格の回に 5 つ目「メジャーアップデートの有償化」を足していた）
- **件数は JS がチェックボックスの数から数え直すようにした**（`[data-consent-count]`）。
  同じ事故は項目を足すたびに起きる。**文言側の数字を人が直し忘れても消えるようにする**
- **HTML にも正しい数字を書いておく。** JS が動かない環境では書いたものが出る
  （このダイアログは JS が死んでいても決済へ通す作りなので、そこだけ古い数字が残ると困る）
- 冒頭のコメントも 5 点の内訳へ更新

**変更ファイル**: `lp/index.html`

**コミット**: `d77a31c`

**公開**: `git subtree push --prefix lp origin gh-pages` ✅（`64a70f6` → `b199f60`）。
配信中の HTML から `all four` / `以下の 4 点` が消えていることを確認。

## 2026-08-03 — LP: ヒーローを誠実なトーンへ差し替え ＋ 改行調整 ＋ 再デプロイ

**指示**: ①ヒーローのメインコピーと本文を、トゲのある表現を避けて自立志向を応援する誠実なトーンへ差し替える（指定文あり）。②「1つの画面で完結する、次世代の決算分析体験。」を読点・句点の位置で改行する。③GitHub Pages へ再デプロイする。

**対応（① ヒーロー）**:
- メイン: 「情報や判断を他人に依存する投資は / **卒業**」。
  **「卒業」だけを 2 行目に落として色を乗せた。** 1 行に流すと結論が地の文に埋もれる
- 前回の「スクール頼みの投資は、もう卒業。」を撤回。
  **特定の学び方を否定する形になっていた**（読み手が通っている場合、
  自分の選択を否定されたと受け取る）。今回は主語を読み手の状態に寄せている
- 本文は指定の 3 文を**それぞれ 1 行**に。学習支援としての立ち位置
  （「投資学習を徹底サポート」）が中央の文で明示される並び
- **`sm:text-5xl` は据え置き。** 1 行目が 15 文字あり、6xl にすると `max-w-4xl` を超える
- 指定文の「納得いくまで 深掘り」にあった全角空白ぶんの隙間は詰めた
  （日本語の途中の空白は画面上で不自然な穴になる）
- 英語版も同じ主張で書き直した（`data-en` / `data-ja` の二言語構成を維持）

**対応（② 見出しの改行）**:
- Core Value の見出しを「1つの画面で完結する、`<br />` 次世代の決算分析体験。」へ。
  **読点でも改行を入れる。** 入れないと幅しだいで意味の切れ目でない位置で折り返す

**対応（③ デプロイ）**:
- `git subtree push --prefix lp origin gh-pages` ✅（`4c0c95e` → `64a70f6`）
- 配信中の HTML に新コピーが載り、**旧コピーが残っていない**ことも確認

**変更ファイル**: `lp/index.html`

**コミット**: `1ebbb47`（LP 本体）/ 本記録は次のコミット

## 2026-08-03 — LP: 自立志向の投資家へ向けた文面刷新（なぜなぜ分析）

**指示**: LP のキャッチコピーと本文を、「高額なスクールやセミナーに頼らず、自室でプロ級の『なぜなぜ分析』を自分で実践したい自立志向の投資家」に刺さるトーンへ調整する。①ヒーローのメイン／サブコピー差し替え、②お悩みセクション（こんな壁を感じていませんか？）を 4 項目で追加、③AIクロスディベートの解説を「1 人でプロの深掘りを体験できる」文脈で追加し、完全ローカル・買い切りの訴求を強める。**他者を直接批判しない**こと。

**対応（① ヒーロー）**:
- メイン: 「スクール頼みの投資は、もう卒業。/ 専属 AI と『なぜ』を突き詰める、/ 自立型・決算アナライザー。」
- **`sm:text-6xl` → `sm:text-5xl` に落とした。** 文字数がほぼ倍になったので、
  6xl のままだと 4 行に割れて圧が出る
- 日本語は `<br />` で明示改行（自動折り返しは読点を無視する）。英語も同じ主張で書き下ろし

**対応（② お悩みセクション）**:
- `#pains` を**ヒーローと Core Value のあいだ**に新設。
  **「何をしなくてよくなるか」を見せる前に、読み手が自分の詰まりを言語化できる場を置く**
- 4 項目（SNS 頼み / スクールのハードル / 1 人だと思考が止まる / 推奨銘柄からの脱却）
- **警告色を使わない。** アイコンもカードも本文と同じ寒色に留める。
  赤やアラートで煽ると、狙っている知的なトーンが崩れる
- **主語を常に読み手側に置く。** 「スクールが悪い」ではなく「通うのはハードルが高い」。
  他者を名指しで批判した瞬間、比較優位ではなく党派の話になる
- 末尾に橋渡しの一文（「渡すのは答えではなく、深掘りの型そのもの」）

**対応（③ クロスディベート ＋ ローカル・買い切り）**:
- `#features` の**先頭に単独で**配置。3 枚のカードに混ぜると主張の核が埋もれる
- 強気派（Bull）/ 慎重派（Bear）の役割を 2 枠で併記。
  文面は実装（`debate_bull.md` = 応答・修正する側 / `debate_bear.md` = 検証する側）に合わせた
- 「検証役には別プロバイダを指定できる」を注記。
  **アプリ内の `debate.settingsIntro` と同じ理由**（同じモデルだと思い込みが残る）
- 機能カード ③ の見出しを「BYOK」→「完全ローカル・買い切り」へ。
  月額費用・コミュニティへの気遣いが不要である旨を追記
- 価格セクションのリード文にも「更新も退会の手続きもない」を追記

**変更ファイル**: `lp/index.html`（変更）/ `PROGRESS.md` / `改修記録.md`

**コミット**: `56bab50`（`lp/index.html`）/ 本記録は `856363e`

**公開（追記 2026-08-03）**: 指示により先行公開した。
`git subtree push --prefix lp origin gh-pages` ✅（`faccc69` → `4c0c95e`）。
https://motojet-bit.github.io/stoq-ai/ が 200 で新コピーを配信しているのを確認。
**まだ告知していない・アクセスが無い前提での、実機表示確認を兼ねた公開**。
LP に載せたクロスディベートは配信ビルド v0.1.0 に無いので、
**告知の前にこの機能を含む版をリリースすること**。

**注意**: `lp/index.html` は**別セッションのコミット `56bab50`（クロスディベート Stage 2）に
巻き込まれて入った**。ステージしたまま並行して `git commit` が走ったため。
LP の変更なのにコミットメッセージは `feat: 2分割 AIクロスディベート` になっている。
**同じリポジトリを複数セッションで触るときは、add してから commit までを空けない。**

**備考**: クロスディベートは**この時点でコミット済みだが未リリース**（配信ビルドに未反映）。
LP を公開する前に、この機能が入ったビルドを配信する必要がある。
先に LP だけ出すと、ダウンロードした版に無い機能が載っていることになる。

## 2026-08-03 — 第76章: ティッカーと添付資料の不一致検知 ＋ 安全中止

**指示**: ①PDF 読み込み時に資料内の企業名・ティッカーを判定し、選択中の銘柄と一致しなければ警告して進ませない。②万一処理が始まっても、Step 1 の解析時に不一致を検出したら `MISMATCH_TICKER_ERROR` を発行し、トークンを無駄にせずその場で安全に中止する。

**なぜ要るか**: **別会社の資料で分析すると、結果は最後まで成立してしまう。**
TSLA を選んで Apple の決算資料を入れても、AI は Apple の数字で 20 項目を埋め、
TSLA の分析として保存される。スコアも根拠も揃っているので、
**出力を読んでも間違いに気づけない。**

**対応（① 事前検知）**:
- `tickerMatch.ts` に判定を切り出した。資料の先頭とファイル名から、
  取引所つき表記（`NASDAQ: AAPL`）・`Ticker:` 表記・証券コードを拾う
- 食い違えば警告ダイアログを出して**進ませない**。
  **「無視して続行」は置かない** — 誤って続けたときの被害が、
  止められた不便より大きい。資料を入れ替えるか銘柄を選び直せば済む
- **裸の大文字 3〜5 文字は拾わない。** `EPS` `FCF` `USD` `ROIC` を
  識別子と取り違えると、正しい資料まで弾いてしまう
- **手がかりが無ければ止めない（`unknown`）。** ティッカーを書かない資料は
  珍しくないので、分からないだけで弾くと使えなくなる
- **社名が違うだけでも止めない。** 子会社名・ブランド名・監査法人名が
  先に出てくる資料があり、「一致しなかった」を「別会社だ」と
  読み替えると誤検知が増える。社名は**一致の確認にだけ**使う
- 先頭 3000 字だけを見る（本文中の競合他社への言及を発行元と取り違えない）
- 期を聞く前に確かめる。期だけ確認して走らせても、中身が別会社なら意味が無い

**対応（② 実行時検知）**:
- **第 1 段の指示にだけ**ガードを置き、別会社と判断したら
  `MISMATCH_TICKER_ERROR: <企業名>` の 1 行だけを出させて終わらせる。
  **ここで止まれば以降の段は走らない**ので、無駄なトークンを使わない
- **判断が付かないときは通常どおり進めさせる**（誤検知で止めない）
- 合図を**ストリーム中に検出した時点で `llm_cancel` を呼んで通信を切る。**
  受け切ってから捨てるのでは、その分のトークンを払うことになる
- 生成済みの段は保存せず、**チェックポイントも消す。**
  残すと次回に別会社の採点から再開してしまう
- 合図は本文の**先頭 200 字だけ**を見る（注意書きの引用で誤爆しない）
- Rust 側に `ERR_TICKER_MISMATCH` を定義

**変更ファイル**: `src/lib/parser/tickerMatch.ts`（新規）,
`src/components/TickerMismatchDialog.tsx`（新規）,
`src/lib/prompts/{analysisSteps,analysisRunner}.ts`, `src/App.tsx`,
`src-tauri/src/error.rs`, `src/locales/{ja,en}.json`

**コミット**: `e3822e6`

**備考**: 実行時検知は**モデルの判断に依存する**。資料に企業名が書かれていない場合や、
モデルが指示を守らない場合はすり抜ける。事前検知と合わせた二段構えで確度を上げている。

---

## 2026-08-03 — 第75章: 投資思想の再定義 ＋ 期の絞り込み ＋ UI/UX 総合改修

**指示**: ①メガスケーラー／中位株／マイクロキャップの評価思想を定義し直す。②複数期の資料が来たら 1 つに絞らせる。③期のパースとフォールバックを確定させる。④UI/UX 7 件（既定ウィンドウ・Ctrl+A・対話のリッチテーブル・引用の赤字・四半期比較・PDF 出力・Google 無料明記）。

**対応（① 投資思想）**:
- **メガスケーラー**: 判定の目的を「QQQ / S&P500 を買うより、サテライトとして
  組み込んでポートフォリオ全体をかさ上げできるか」に固定。
  **二段階評価を厳守させた** — 20 項目は結論に偏らず客観採点し、
  指数との比較は最後のまとめでのみ行う。
  途中で「サテライト向きだから加点」をやると採点が歪む。
  PBR/PER の単独不合格判定を禁止（**自社株買いで純資産が縮むのは高 ROE の裏返し**で、
  割高さの証拠ではない）。FCF マージン / Rule of 40 / EV・Gross Profit / モート持続性で見る
- **中位株（新規 `midcap`）**: トレンドに道具を売る「ツルハシ企業」として評価。
  **誰が勝っても売れる構図か**（顧客集中度）、受注残の質（キャンセル条項・集中）、
  リカーリング比率、取れる比率の伸びしろ。
  ピーク利益に対する低 PER を割安と書かせない（**ピークで安く見えるのは循環の常**）
- **マイクロキャップ（`role_growth.md` → `role_microcap.md`）**: ロマン枠として、
  Cash Runway・希薄化構造・単一プロダクト依存と特許モート・黒字化の道筋を厳格査定。
  **下方修正条項つきワラント**（株価が下がるほど希薄化が進む構造）を必ず指摘させる。
  赤字期の PER/PBR 判定はやめ、**成長率 vs バーンレート**を軸にする
- **id は保存データの鍵なので変えない**（`growth` のまま中身を差し替えた）

**対応（② 複数期の絞り込み）**:
- `collectFiscalPeriods` で資料を期ごとにまとめ、2 種類以上なら選択させる
- **混ぜて分析させない。** どの数字がどの期のものか分からない結果ができる
- 選ばなかった期の資料は使わない旨も画面に出す

**対応（③ 期のフォールバック）**:
- 初期選択は「資料から読めた期 → ファイル名 → SEC に出ていそうな最新期」の順
- **見積もりは 1 四半期前。** 四半期報告は期末から 40〜45 日遅れるので、
  8 月なら Q2（4〜6 月期）が直近の提出分。年初は前年 Q4 へ戻す
- 見積もり由来であることを画面に出す（確度が違うため）
- **添付資料があるなら、SEC が取れなくても中断しない。**
  PDF と財務データだけで分析は成立する。
  期の指定は「その期の資料を入れた」という意思表示なので、
  SEC 側の未提出を理由に止めると手元の資料を使えないまま終わる

**対応（④ UI/UX）**:
- 既定ウィンドウを 1440x900 → **1680x1000**（分析ボタンが最初から画面内に収まる）
- **Ctrl+A を分析本文だけに限定。** 既定のままだとサイドバーやボタンのラベルまで
  選択され、コピーしても使えない。**入力欄の中では既定の挙動に任せる**
- 対話の本文で Markdown テーブルを表として描画し、**RAW 切り替え**を付けた
  （コピーして他所へ貼るときは素のテキストが要る）
- 引用行に目印（`> `）を付けて赤字表示。自分が書いた文と貼り込んだ引用を見分けられる
- ポートフォリオ履歴に**四半期比較**を追加。期を 1 つ選ぶと、
  銘柄横断でブロック別スコアまで並ぶ。同じ期に複数あれば最新を採る
- エクスポートに **PDF** を追加。**PDF ライブラリは足さない**
  （日本語の埋め込みフォントで数 MB 増え、「超軽量」の方針に反する）。
  印刷ダイアログの「PDF として保存」に載せ、OS のフォントで描かせる。
  行がページをまたがないよう `page-break-inside: avoid` を効かせた
- Google 認証ガイドに「クライアント ID の発行・利用は完全無料です」を明記
  （費用がかかると誤解されると、ここで止まってしまう）
- ディベートの 🔥 スイッチは削除されておらず、分析ヘッダーに残っていることを確認した

**変更ファイル**: `src-tauri/src/prompts/{mod.rs,role_megacap.md,role_midcap.md（新規）,
role_microcap.md（改名）}`, `src-tauri/tauri.conf.json`,
`src/lib/parser/fiscalPeriod.ts`, `src/lib/ui/selectScope.ts`（新規）,
`src/lib/chat/markdownTable.ts`（新規）, `src/lib/export/exportPdf.ts`（新規）,
`src/lib/portfolio/quarterCompare.ts`（新規）,
`src/components/{MessageBody,QuarterCompareTable}.tsx`（新規）,
`src/components/{FiscalPeriodDialog,AnalysisPanel,PortfolioHistoryPanel,CloudSyncSettings}.tsx`,
`src/App.tsx`, `src/locales/{ja,en}.json`

**コミット**: `a2c976d`, `cc43490`

**備考**: PDF は印刷ダイアログ経由なので、**保存先の選択は OS 側の画面**になる。
他の形式（CSV / MD / JSON）だけが `exports.rs` を通る。

---

## 2026-08-03 — 第74章: コンテキスト分離 ＋ 進捗の固定 ＋ スコア演出 ＋ 詳細ログ

**指示**: ①各ステップに直前ステップの出力を含めず、「一次資料 ＋ YF 財務データ」だけを独立して送る（雪だるま式のトークン膨張を止める）。②進捗メーターと消費トークンをスクロールしても消えないよう固定。③平均スコアを分析中は隠し、完了時にカウントアップで確定表示。④決算期ダイアログでファイル名からの期パースと「本決算／アドホック」選択を追加。⑤エラー詳細をモーダルで表示・コピーできるようにする。

**対応（① コンテキスト分離）**:
- **採点段（1〜3）に直前の出力を渡すのをやめた。** 前回の分割実装では
  各段に前段の本文を渡していたため、段を追うごとにプロンプトが積み上がり、
  資料と合わせて 10 万トークンを超えて切断されていた。
  分割したのに切れる、という状態の原因はここ
- 各段は「一次資料 ＋ 財務データ」だけを見て担当範囲を独立に採点する。
  通しの体裁（表のヘッダー、節の順序）は最後にフロント側で組み立てる
- **最終段だけは評価テーブルの「行」を渡す（`scoreRowsOnly`）。**
  矛盾の指摘と死角チェックは、何が書かれたかを読まないと成立しない。
  ただし前置きや強み・リスクの節は落とし、行だけにする
- 指示文にも「担当範囲の行だけを書く。他の行は別途評価している」を足した

**対応（② 進捗の固定）**:
- スクロール領域の**外**へ出した。sticky を効かせるより確実で、そもそも流れない
- 消費トークンと概算コストを同じ枠へ入れ、**走行中も見える**ようにした
  （終わってから合計だけ見せても、途中で止めるかの判断に使えない）

**対応（③ スコアのカウントアップ）**:
- **生成中は出さない。** 段ごとに採点しているので、途中の平均は
  「まだ採点していない項目を除いた平均」でしかなく、確定値と取り違える
- 全段完了後に 0 から回して確定させる。終盤で減速させて確定感を出す
- 計算は `countUp.ts` に切り出した（描画から離せばテストできる）
- 同じスコアで何度も回さない（再描画のたびに演出が走ると鬱陶しい）

**対応（④ 決算期ダイアログ）**:
- 本文から読めなければ**ファイル名**を見る（`FY26_Q3.pdf` 等）。
  拡張子を落とし、`_ - .` を空白へ均してから本文と同じ規則に乗せる
- 判定元がファイル名だったことを画面に出す（本文より確度が落ちるため）
- 「本決算（実績）／追加資料・見通し（アドホック）」の選択を**常に出す**。
  既存の有無で出し分けていたが、**見通し資料を本決算として記録すると、
  あとから実績と見分けが付かなくなる**

**対応（⑤ エラー詳細モーダル）**:
- 生ログを、銘柄・プロバイダ・モデル・完了段と一緒にモーダルで表示
- **そのままコピーできる形にする。** 問い合わせを受ける側が最初に聞くのは
  「実際のメッセージ」と「どのモデルで起きたか」で、画面の文言では切り分けられない
- 折りたたみをやめてモーダルにしたのは、その場に広げると本文が押し出されるため

**変更ファイル**: `src/lib/prompts/analysisSteps.ts`, `src/lib/prompts/analysisRunner.ts`,
`src/lib/parser/fiscalPeriod.ts`, `src/lib/ui/countUp.ts`（新規）,
`src/components/{ScoreCountUp,ErrorDetailModal}.tsx`（新規）,
`src/components/{AnalysisPanel,AnalysisProgress,AnalysisFailure,FiscalPeriodDialog}.tsx`,
`src/App.tsx`, `src/locales/{ja,en}.json`

**コミット**: `1a8eefe`

**備考**: 分離により入力トークンは段ごとにほぼ一定になるが、
**資料を毎回送るので合計の入力トークンは以前より増える**。
1 回あたりが上限に当たらなくなるのと引き換え。実測値が出るので確認できる。

---
