# 改修記録 — StockAnalyzer

指示を受けて改修を行うたびに、**新しい項目を先頭に**追記する。

書式:

```
## YYYY-MM-DD — 見出し
**指示**: ユーザーからの指示（要約）
**対応**: 実際に行った変更
**変更ファイル**: 追加/変更/削除したファイル
**コミット**: <short-sha>
**備考**: 補足・積み残し（なければ省略）
```

---

> 古い記録は [docs/archive/改修記録_2026-08.md](docs/archive/改修記録_2026-08.md) に繰り下げてあります。

---

## 2026-08-02 — Phase 3 Step 4: ツールチップ・初回チュートリアル・可変閾値

**指示**: 3 件。①主要 UI（`?` ヘルプ / ショートカット設定 / 役割選択 / 検討中銘柄パネル）にホバー説明を追加し、ヘルプ画面に「機能リクエスト」案内を配置。初回チュートリアルも表示する。②設定画面に「分析ルール・閾値カスタマイズ」タブを新設し、売上高成長率・ROE・PER などの評価基準を数値入力／スライダーで自由に設定できるようにして、その値をシステムプロンプトへ自動埋め込みしてスコアリングに反映させる。③閾値の保存とプロンプト埋め込みの Unit Test。

**対応（① ツールチップ・チュートリアル）**:
- `Tooltip` を新設。**`title` 属性は表示まで 1〜2 秒待たされ、改行もできない。**
  初心者向けの案内はすぐ出て読みやすい必要があるので自前で持つ
  - 画面端でははみ出しを検出して自動で反対側へ寄せる
  - 文字サイズは `.ui-fixed` で固定（本文を拡大しても吹き出しは崩さない）
- 案内文は `src/lib/ui/tooltipText.ts` に集約。
  **表示場所ごとに散らすと、同じ機能の説明が画面によって食い違う**ため
- 配置: `?` ヘルプボタン / ショートカット設定 / 役割選択 / 検討中銘柄パネルの見出し
- `WelcomeTour`（5 ステップ）を新設。初回起動時だけ自動表示し、
  閉じたら `localStorage` に印を残して二度と自動では出さない。
  各ステップから設定画面やヘルプへ直接飛べる
- ヘルプ窓の下部に「はじめかたを見る」と
  「欲しい機能がありませんか？開発者へリクエストを送る」を追加

**対応（② 可変閾値）**:
- `src/lib/prompts/thresholds.ts` に 7 項目を定義
  （売上高成長率 / 営業利益率 / ROE / PER / PBR / D/E / 配当利回り）。
  それぞれ「以上で合格」「以下で合格」の向き、既定値、範囲、刻みを持つ
- **定義と既定値はここが唯一の出所。** Rust 側には
  「ユーザーが既定から変えた項目」だけを保存する。
  既定値を両方に置くと、既定を変えたときに古い値が残ってしまうため
- `Settings.thresholds`（`BTreeMap<String, f64>`）を追加。
  `settings_save` は有限の数値だけ通し、渡された内容で丸ごと置き換える
  （項目を消したいときに残ってしまわないように）
- 設定モーダルに「分析ルール・閾値」タブを追加。
  スライダーと数値入力の両方で調整でき、変更は即座に保存される。
  **AI に実際に送られる文面をその場で確認できる**ボタンも付けた
- `buildSystemPrompt(thresholds)` が閾値セクションを差し込む。
  「以下の閾値基準を厳密に適用して合格/不合格を判定せよ」＋各項目の条件行 ＋
  「根拠欄に合否を明記」の指示
- **数値が取れない場合は「基準未判定（データなし）」とし、不合格として扱わない**よう明記。
  資料不足と実力不足の区別がつかなくなるため
- 例示の数値（「基準 15% 以上」）も設定値に追従させ、説明と基準がずれないようにした

**対応（③ テスト）**:
- フロント 26 件: 定義の健全性（ID 重複・既定値が範囲内）/ 丸め（範囲外・刻み・NaN）/
  保存値の復元（知らない ID の無視・数値以外の無視・冪等性）/
  文字列化（`>=` と `<=` の使い分け）/ プロンプト埋め込み（設定値がそのまま入る・
  例示が追従する・データなしを不合格にしない指示）/ システムプロンプトへの反映
- Rust 3 件: 既定では空であること / 保存と読み込みの往復 /
  **閾値を持たない旧設定ファイルも読めること**

**変更ファイル**:
- 追加(TS): `lib/prompts/thresholds.ts` / `.test.ts`, `lib/ui/tooltipText.ts`
- 追加(UI): `Tooltip.tsx`, `ThresholdSettings.tsx`, `WelcomeTour.tsx`
- 変更: `settings.rs`（`thresholds` + テスト 3 件）, `commands.rs`
- 変更: `systemPrompt.ts`, `buildPrompt.ts`, `analysisRunner.ts`,
  `SettingsModal.tsx`, `StatusBar.tsx`, `ShortcutSettings.tsx`,
  `PromptLibraryMenu.tsx`, `CandidateStocksPanel.tsx`, `HelpAssistant.tsx`,
  `App.tsx`, `types/index.ts`
- 変更: `PROGRESS.md`, `改修記録.md`

**コミット**: (次のコミットで記録)

**検証**: `npm run verify`（typecheck → test → build）✅ / `cargo test --lib` ✅
フロント **190 件** / Rust **90 件** が全通過。既存の分析・チャット機能のテストも維持。

**備考**: 機能リクエストのリンク先は仮の GitHub Issues URL
（`lib/ui/tooltipText.ts` の `FEATURE_REQUEST_URL`）にしています。
実際の受け付け先が決まったら差し替えてください。

---

## 2026-08-02 — Phase 3 Step 3: データ取得元の切替・ドラッグ可能モーダル・ヘルプ AI

**指示**: 3 つの大型機能。①設定画面にデータプロバイダー選択 UI（Yahoo Finance / FMP[推奨] / Alpha Vantage）を追加し、それぞれ指定の注記を併記。FMP と Alpha Vantage は APIキー入力欄を表示・保存。Rust 側は `MarketDataProvider` トレイト等で抽象化し、キー未設定時は分かりやすいアラートを出す。②各種モーダルをヘッダードラッグで移動できるようにし、外側の暗転部分クリックでは閉じないようにする。③`?` ヘルプボタンからアプリ専用ナレッジを内蔵した AI アシスタント窓を起動する。④ドラッグ状態管理・プロバイダー切替・ヘルスチェックの Unit Test。（配置指示: ヘルプボタンは右上ではなく最下部バーの右側、バージョン表記の左）

**対応（① データ取得元）**:
- `src-tauri/src/market/` を新設。`MarketDataProvider` トレイトに
  `fundamentals` と `health_check` を定義し、Yahoo / FMP / Alpha Vantage が実装する。
  **どれを選んでも同じ `Fundamentals` を返す**ので UI 側は取得元を意識しない
- `Settings.market_provider` を追加。キーは `market:<id>` として既存のキー辞書に入れるので、
  マスク表示・保存・削除の仕組みをそのまま使える（**生の値はフロントに出ない**）
- `status_of()` が「キーが要るか / いま使えるか / 使えない理由」を純粋関数で返す。
  **キー未設定のときは通信する前に理由つきで止める**
- 設定モーダルをタブ化し「データ取得元」タブを追加。
  ラジオ選択・注記の併記・キー入力・接続テスト・キー削除（確認ダイアログ付き）
- FMP は profile / key-metrics-ttm / ratios-ttm の 3 本、
  Alpha Vantage は OVERVIEW / GLOBAL_QUOTE の 2 本を組み合わせる。
  **指標が欠けてもエラーにせず「—」で埋める**（全部そろわないと何も出ないのは使いづらいため）
- Alpha Vantage は失敗も HTTP 200 で返すため、本文の `Note` / `Error Message` /
  `Information` を見て失敗と判定する
- 四半期推移（4Q モメンタム）は SEC XBRL との突き合わせが必要なため Yahoo + SEC 固定。
  UI にもその旨を明記した

**対応（② モーダル）**:
- `ModalShell` を新設し、設定 / 役割ライブラリ / 銘柄インポート / 資料プレビューを統一
- **ヘッダーをドラッグして移動できる。** 位置は絶対座標ではなく
  「中央からのずれ」で持つ。絶対座標だとウィンドウをリサイズしたとき画面外に取り残される
- `clampOffset` で必ず画面内に収める。タイトルバーを掴めなくなると閉じることも
  動かすこともできなくなるため。ウィンドウリサイズ時にも引き戻す
- **背景に `onClick` を付けない。** 誤クリックで入力途中の内容が消える事故を防ぐ。
  閉じるのは ✕ / キャンセル / Esc だけ。背後の操作は塞ぐ
- 移動したときだけ「中央に戻す」ボタンが出る

**対応（③ ヘルプ AI）**:
- **最下部ステータスバーの右側（`Phase 1 / v0.1.0` の左）**に「? ヘルプ」ボタンを配置
- 押すと右下にドロワー（`HelpAssistant`）。
  銘柄分析の対話とは履歴を分け、保存もしない（操作の質問が分析ログに混ざらないように）
- ナレッジは `src/lib/prompts/helpKnowledge.ts` が組み立てる
  - 指定どおりの役割文 ＋ 画面の見方 ＋ 使い方の流れ ＋ 分析のコツ ＋ APIキーの設定手順
  - 3 つのデータ取得元の違いとキーの取得先 URL
  - **いま実際に割り当てられているショートカット表**（既定値を書くと変更後に誤案内するため）
  - 現在の設定サマリ（LLM プロバイダ / キー登録状況 / 取得元 / SEC User-Agent）
  - 「投資判断そのものは助言しない」「無い機能をでっち上げない」制約
- 質問例のチップ（「ショートカットキーはどこで変える？」等）を初期表示に出す

**対応（④ テスト）**:
- Rust: 取得元の判定 9 件（既定 / 壊れた値の丸め / キー要否 / 空白キー / 取得元ごとの独立性 /
  キーの保存先が LLM と衝突しないこと / キー未設定時は通信前に失敗すること）
- Rust: FMP 4 件・Alpha Vantage 7 件（文字列数値・`"None"`・パーセント文字列・
  ゼロ除算・上限エラー・欠損時のダッシュ埋め）
- フロント: `modalDrag` 13 件（可動域 / 画面外に出さない / リサイズ時の引き戻し / NaN）
- フロント: `helpKnowledge` 16 件（現在の割り当てを出すこと / market キーを混ぜないこと 等）

**変更ファイル**:
- 追加(Rust): `market/mod.rs`, `market/fmp.rs`, `market/alphavantage.rs`
- 追加(TS): `lib/ui/modalDrag.ts` / `.test.ts`, `lib/prompts/helpKnowledge.ts` / `.test.ts`
- 追加(UI): `ModalShell.tsx`, `MarketProviderSettings.tsx`, `HelpAssistant.tsx`
- 変更: `settings.rs`, `commands.rs`, `lib.rs`, `yahoo.rs`（整形関数を公開）, `Cargo.toml`
- 変更: `SettingsModal.tsx`, `PromptLibraryModal.tsx`, `CandidateImportModal.tsx`,
  `DocumentPreviewModal.tsx`, `StatusBar.tsx`, `App.tsx`, `Icons.tsx`,
  `types/index.ts`, `lib/config/settingsStore.ts`, `lib/ui/toastStore.ts`
- 変更: `PROGRESS.md`, `改修記録.md`

**コミット**: `32b3f04`

**検証**: `npm run verify`（typecheck → test → build）✅ / `cargo test --lib` ✅
フロント **164 件** / Rust **87 件** が全通過。既存の分析・チャット機能のテストも維持。

**備考**: FMP / Alpha Vantage の実データ疎通は、キーを登録したうえで
設定画面の「接続テスト」で確認をお願いします（キーが無いためこちらでは未検証）。
モーダルのドラッグ操作感も実機確認が必要です。

---

## 2026-08-01 — Phase 3 Step 2: UI/UX 仕上げ・コンプライアンス表現・ショートカット基盤

**指示**: 7 件。①既定役割「テンバガー発掘アナリスト」の名称・説明が将来の株価騰貴を断定・保証する表現で法的リスクがあるため「高成長・グロース株アナリスト」＋客観的な説明へ変更。②確認ダイアログのフォントサイズ崩れを直し、余白・ホバー・角丸を整えたモダンなデザインに。③チャット入力欄に `resize-y` か自動リサイズを導入。④「最小化中」の復元ボタンを白の太字＋黄色系アクセント背景で強く目立たせる。⑤「検討中銘柄」小窓を境界線ドラッグでリサイズ、ヘッダーで折りたたみ、縦横スクロール、さらにティッカーの改行羅列だけでも取り込めるようパース強化。⑥`Ctrl+Enter`（mac は `Cmd+Enter`）で送信・`Enter` で改行に変更し明示。⑦ショートカットキーの管理基盤（テーブル）と設定画面のタブを新設。

**対応（① コンプライアンス）**:
- `DEFAULTS` を「**高成長・グロース株アナリスト**」に改称し、本文も
  「売上・利益成長率や競争優位性を分析する」「将来の株価や騰落を断定・保証する表現は用いない」へ
- **すでに配布済みの DB に入っている行も差し替える。** `migrate` に
  `rename_legacy_defaults` を追加し、`builtin = 1` かつ旧名の行だけを更新する。
  ユーザーが自分で作った役割（`builtin = 0`）には触れない

**対応（② ダイアログ）**:
- `ConfirmDialog` を `.ui-fixed` にして本文の可変フォントから切り離した。
  可変のままだとタイトル・本文・ボタンの比率が崩れて野暮ったく見えるため
- アイコン付きヘッダー / `rounded-xl` / `backdrop-blur-sm` / `ring-1` /
  ホバーとフォーカスリング / `border-t` で区切ったアクション行

**対応（③ 入力欄）**:
- `scrollHeight` に合わせて高さを自動調整（一度 `auto` に戻さないと縮まない）。
  上限 `max-h-64` を超えたぶんはスクロール。加えて `resize-y` で手動調整も可能

**対応（④ 最小化中）**:
- `bg-amber-500` ＋ 白の太字 ＋ `ring-1` ＋ シャドウ。ラベルも `font-bold text-amber-300`

**対応（⑤ 検討中銘柄）**:
- 上の履歴との境界線をドラッグして高さ変更。`clampFirstSize`（既存のテスト済み関数）で
  履歴側の最小 160px を割り込まないよう丸め、高さと折りたたみ状態を localStorage に保存
- ヘッダーのクリック、または ⌄ ボタンで折りたたみ
- `overflow-auto` ＋ 内側 `w-max min-w-full` で縦横スクロール。
  ✕ ボタンは `sticky right-0` にして横スクロールしても押せる
- **パース強化**: パイプが無い行は「ティッカーだけの羅列」とみなす。
  改行区切りに加えてカンマ・読点・タブ区切りにも対応（貼り付け元がまちまちなため）

**対応（⑥ Ctrl+Enter）**:
- `Enter` は改行、`Ctrl+Enter` / `Cmd+Enter` で送信。長文の途中で誤送信しないため
- プレースホルダ・ツールチップ・入力欄下の注記の 3 か所で明示。mac では `⌘+Enter` と表示

**対応（⑦ ショートカット基盤）**:
- `shortcuts` テーブルを新設（`library.db`）。
  **既定値は保存せず、ユーザーが変更した割り当てだけを持つ。**
  既定を Rust と TS の両方に置くと、既定を変えたときに古い値が残ってしまうため
- アクション一覧と既定キーは `src/lib/ui/shortcutStore.ts`
  （新規チャット `Ctrl+N` / 検討中銘柄の追加 `Ctrl+Shift+A` /
  ティッカー入力へ `Ctrl+L` / AI分析 `Ctrl+Shift+Enter` /
  サイドバー `Ctrl+B` / 設定 `Ctrl+,` / チャット送信 `Ctrl+Enter`）
- キーの正規化・イベント変換・mac 表記・衝突検出は `shortcutKeys.ts`（純粋関数）
- 設定モーダルをタブ化し「ショートカット」タブを追加。
  「変更」を押して次に押したキーを割り当てる方式（Esc で中止、✕ で既定に戻す）
- **入力欄にフォーカスがあるときは文字入力を優先**し、
  `allowInInput` のアクションだけ発火させる

**変更ファイル**:
- 追加(Rust): `shortcuts.rs`
- 追加(TS): `lib/ui/shortcutKeys.ts` / `.test.ts`, `lib/ui/shortcutStore.ts` / `.test.ts`
- 追加(UI): `ShortcutSettings.tsx`
- 変更: `prompts.rs`（既定役割の差し替え移行）, `commands.rs`, `lib.rs`,
  `parseCandidates.ts` / `.test.ts`, `App.tsx`, `Sidebar.tsx`, `CandidateStocksPanel.tsx`,
  `ChatPanel.tsx`, `ConfirmDialog.tsx`, `PanelRestoreBar.tsx`, `SettingsModal.tsx`,
  `TickerInput.tsx`
- 変更: `PROGRESS.md`, `改修記録.md`

**コミット**: `9f97445`

**検証**: `npm run verify`（typecheck → test → build）✅ / `cargo test --lib` ✅
フロント **135 件** / Rust **65 件** が全通過。既存の分析・チャット機能のテストもすべて維持。

**備考**: 境界線ドラッグの手触りと、ショートカットの実キー入力は実機確認が必要。

---

## 2026-08-01 — Phase 3 Step 1: 検討中銘柄・チャットのアーカイブ・役割ライブラリ

**指示**: Phase 3（複数銘柄の比較・管理基盤）にあたり 4 件。①「検討中銘柄」小窓と「＋ 追加」モーダル。パイプ区切り `ティッカー|社名|ジャンル` を複数行パースして `candidate_stocks` へ一括保存。コンパクトなリスト（縦横スクロール）、右端の ✕ と右クリックメニューで削除、クリックで上部ティッカー入力欄に連動して即分析。②チャット履歴一覧に独立した縦スクロール、各スレッドに「📁 アーカイブ」（`is_archived` を追加・マイグレーション）。③システムプロンプト（役割）のストック・切替ライブラリ（`prompts` テーブル）と選択 UI。④パース処理・削除処理の Unit Test、および新設テーブルのマイグレーションで既存データが壊れないことのテスト。

**対応（① 検討中銘柄）**:
- **`library.db` を新設**し `candidate_stocks` を作成。
  既存の `analyses.db` / `chats.db` には触れないので、
  新テーブルを増やしても過去の分析・会話が影響を受けない
- パースは `src/lib/candidates/parseCandidates.ts`（純粋関数、UI 非依存）
  - 全角パイプ `｜` / 全角スペース / CRLF / `#` メモ行 / 空行を吸収
  - ティッカーは大文字化。`BRK.B` `7203.T` `^GSPC` を通す文字種チェック
  - 同一貼り付け内の重複は後勝ちでまとめ、`duplicates` として返す
  - **取り込めなかった行は捨てず、行番号・原文・理由を `errors` で返す。**
    黙って落とすと「貼ったのに入っていない」に気づけないため
- モーダルは保存前に「取り込む銘柄 / 重複 / エラー」を必ず表示する
- 小窓は 1 行 1 銘柄のコンパクト表示。`overflow-auto` で縦横ともスクロール
- 削除は **✕ ボタン**と**右クリックメニュー**の 2 系統（どちらも `removeCandidate` を通る）
- クリックで `TickerInput` に `preset` を流し込みつつ分析を開始する。
  同じ銘柄を続けて選べるよう、値ではなく連番 `seq` の変化で反映している

**対応（② アーカイブ）**:
- 履歴 `<nav>` に `min-h-0` + `overflow-y-auto`。
  **`min-h-0` が無いと中身の高さを主張し、下の「検討中銘柄」を押し出す**
- `chat_sessions` に `is_archived` を `ALTER TABLE ... DEFAULT 0` で追加。
  既存行は自動的に「未アーカイブ」になるため、過去の会話は 1 件も失われない
- サイドバー右上の 📁 で「履歴 / アーカイブ」を切り替え。各行の 📁 で移動・復元
- 一覧は `ORDER BY is_archived ASC, updated_at_ms DESC`（アーカイブは常に後ろ）

**対応（③ 役割ライブラリ）**:
- `prompts` テーブルを新設。既定 3 種（プロの株式ジャーナリスト /
  テンバガー発掘アナリスト / 慎重な高配当アナリスト）を**初回だけ**投入する
- 投入済みの印を `prompt_seed` に残すので、
  **削除した既定役割が再起動で勝手に復活しない**
- 対話パネルのヘッダーから `PromptLibraryMenu` でワンタップ切替。
  どれを使うかは端末ごとの好みなので localStorage に保持する
- 追加・編集・削除は `PromptLibraryModal`。既定役割も編集できる

**対応（④ テスト）**:
- フロント: パース 18 件（正常系 / エラー検知 / 重複）＋ 削除ストア 7 件
- Rust: **旧スキーマ（`is_archived` 無し）の DB を再現**し、
  移行後もセッション・メッセージ・ティッカーが残ることを検証。
  マイグレーションを 3 回流してもアーカイブ状態が初期化されないことも確認
- `candidate_stocks` と `prompts` が同じ DB に同居しても互いを壊さないことを検証

**変更ファイル**:
- 追加(Rust): `library.rs`, `candidates.rs`, `prompts.rs`
- 追加(TS): `lib/candidates/parseCandidates.ts` / `.test.ts`,
  `lib/candidates/candidateStore.ts` / `.test.ts`, `lib/prompts/promptLibrary.ts`
- 追加(UI): `CandidateStocksPanel.tsx`, `CandidateImportModal.tsx`, `ContextMenu.tsx`,
  `PromptLibraryMenu.tsx`, `PromptLibraryModal.tsx`
- 変更: `chats.rs`（`migrate` 分離 + `is_archived`）, `commands.rs`, `lib.rs`,
  `App.tsx`, `Sidebar.tsx`, `ChatHistoryItem.tsx`, `ChatPanel.tsx`, `CommandBar.tsx`,
  `TickerInput.tsx`, `Icons.tsx`, `types/index.ts`, `lib/chat/chatStore.ts`
- 変更: `PROGRESS.md`, `改修記録.md`

**コミット**: `8d6f210`

**検証**: `npm run verify`（typecheck → test → build）✅ / `cargo test --lib` ✅
フロント **101 件** / Rust **56 件** が全通過。

**備考**: 検討中銘柄の右クリックメニューと ✕ の実操作感は実機確認が必要。

---

## 2026-08-01 — 修正: フォントUIのメニューバー移設・折りたたみ余白ゼロ化・パネル移動の根本改修

**指示**: スクリーンショットで判明したレイアウト崩れと仕様不備 5 件。①フォント調整 UI をコマンドバー / パネルから完全撤去し、最上位 MenuBar の右側へ埋め込む（変更時に UI パーツが動かないこと）。②設定モーダルの「未設定」が「未 / 設定」と 2 行に折り返される崩れを修復。③パネルを折りたたむと右側に無駄な帯が残り、残ったパネルが 100% に広がらない。畳んだパネルは小さな復元ボタンへ退避させ余白をゼロに。④グリップのドラッグが実機（Tauri / WebView2）で機能しない。Pointer Events 等を用いて根本改修。⑤APIキー削除時に確認ダイアログ（「登録されているAPIキーを削除しますか？」/ [キャンセル] [削除する]）。

**対応（① メニューバー移設）**:
- `FontSizeControl` を `CommandBar` から撤去し、`MenuBar` の右端に配置
- **`MenuBar` 自体を `.ui-fixed` ＋ 高さ `h-9` 固定にした。**
  ここに調整 UI を置く以上、バーの高さが変わるとスライダーがカーソルの下から動くため
- **これは前回の「メニューバーも文字サイズに連動させる」要件と両立しない。**
  今回の「UI パーツが動かないこと」を優先し、最上部バーのみ連動から外した
  （本文・パネル・サイドバー・資料チップは従来どおり全域連動する）

**対応（② 未設定の縦割れ）**:
- `flex` の子は既定で縮むため、ラベル側に `min-w-0 truncate`、
  状態表示（`未設定` / `sk-…3f9a` ＋ 削除ボタン）側に `shrink-0 whitespace-nowrap` を明示

**対応（③ デッドスペース）**:
- **畳んだ枠を分割そのものから外す**方式に変更。`ResizableSplit` の `collapsed` を廃止し、
  `App.renderWorkspace()` が表示中の枠だけで分割を組み立てる。
  1 枚だけになったら分割せずそのまま 100% を占有する
- 畳んだパネルは `PanelRestoreBar`（メニューバー右端の小さな復元ボタン）へ退避
- 最後の 1 枚は畳めないようにした（画面が空になるため）。ボタンは無効化して理由を表示

**対応（④ パネル移動の根本改修）**:
- HTML5 DnD をやめ、`src/lib/ui/panelDrag.ts`（Pointer Events + 外部ストア）を新設
  - `setPointerCapture` でポインタを捕捉するため、パネルの外へ出ても取りこぼさない
  - ドロップ先は `document.elementFromPoint` ＋ `data-panel-slot` 属性で判定。
    **ヘッダーだけでなくパネル全体が落とし先**になるので狙いやすい
  - Esc / `pointercancel` で中断できる。ドラッグ中は `body.is-dragging-panel` で見た目を変える
- **`draggable` との併用は不可能。** ネイティブドラッグが pointermove を奪うため、
  同じ要素で両立させるとどちらかが必ず死ぬ。Pointer Events に一本化した

**対応（⑤ APIキー削除の確認）**:
- `SettingsModal` に `deletingKeyOf` を持たせ、既存の `ConfirmDialog` を再利用。
  指定どおりの文面と [キャンセル] / [削除する]（赤ボタン）

**変更ファイル**:
- 追加: `src/lib/ui/panelDrag.ts`, `src/lib/ui/panelDrag.test.ts`, `src/components/PanelRestoreBar.tsx`
- 変更: `src/App.tsx`（`renderWorkspace()` を新設）, `MenuBar.tsx`, `CommandBar.tsx`,
  `PanelHeader.tsx`, `ResizableSplit.tsx`, `WorkspacePanel.tsx`, `AnalysisPanel.tsx`,
  `ChatPanel.tsx`, `SettingsModal.tsx`, `src/styles.css`
- 変更: `PROGRESS.md`, `改修記録.md`

**コミット**: `e1365ce`

**検証**: `npm run verify`（typecheck → test → build）✅ / `cargo test --lib` ✅
フロント **76 件**（パネル移動の判定を 10 件追加）/ Rust 35 件が全通過。

**備考**: パネル移動の実ドラッグ感、折りたたみ後に本当に余白が消えているかは実機確認が必要。

---

## 2026-08-01 — 修正: コントロール類のサイズ固定 ＆ パネル D&D の進入禁止マーク解消

**指示**: 実機操作で発生した 2 件の不具合（Web 標準仕様の考慮漏れ）を至急修正。①フォントサイズを変えるとスライダーやコマンドバーのパーツまで拡大縮小し、カーソルから逃げて操作不能になる。スライダーや A− / A+ には絶対値を直接指定し `--fs-base` の影響を一切受けない完全固定にすること。②グリップをドラッグしても進入禁止マーク（🚫）が出てドロップできない。ドロップ先のヘッダーに `onDragOver`（必要なら `onDragEnter`）の `preventDefault()` を実装すること。

**対応（① サイズ固定）**:
- **真因は `:root { font-size: var(--fs-base) }`。** `:root` の font-size は `rem` の基準であり、
  Tailwind の余白・幅・高さはすべて `rem` で表現されている。
  そのため文字を大きくするとボタンやスライダーまで太り、
  **カーソルの下から要素が逃げて操作不能**になっていた
- 指定を `:root` → `body` へ移動。`rem` の基準は 16px のまま固定され、
  文字だけが継承でアプリ全域に連動する（Step 5.4 の要件④は維持）
- `.ui-fixed`（12px 固定）を新設し、右上のコントロール群
  （AI設定ボタン ＋ フォント調整 UI）をまとめて適用。
  **上の AI設定ボタンが太るとスライダーが下へずれる**ため、群ごと固定する必要がある
- `FontSizeControl` の各部品を絶対値 px で固定
  （ボタン `h-[22px] w-[26px]` / スライダー `h-[4px] w-[72px]` / 表示幅 `w-[34px]`）
- `ProviderMenu` の `t-label` / `t-body` を外し、`.ui-fixed` の 12px を継承させた

**対応（② D&D）**:
- HTML5 Drag and Drop は、ドロップ先が **`dragenter` と `dragover` の両方で
  `preventDefault()` を呼ばない限りブラウザ既定の拒否が働き 🚫 が出る**。
  従来は `dragover` のみ、しかも MIME 種別の判定を通ったときだけ打ち消していた
- 判定を挟まず必ず打ち消す `allowDrop` に統一し、`onDragEnter` にも割り当てた
- `dragstart` で `text/plain` にも同じ値を入れ、カスタム MIME を落とす環境でも成立させた
- `dragleave` は `relatedTarget` が子要素なら無視し、枠のちらつきを解消
- なお `tauri.conf.json` の `dragDropEnabled: false` は設定済みで、
  WebView 側で HTML5 D&D が有効になっていることを確認した

**変更ファイル**: `src/styles.css`, `src/components/FontSizeControl.tsx`,
`src/components/ProviderMenu.tsx`, `src/components/CommandBar.tsx`,
`src/components/PanelHeader.tsx`, `PROGRESS.md`, `改修記録.md`

**コミット**: `b9f5b49`

**検証**: `npm run verify`（typecheck → test → build）✅ / `cargo test --lib` ✅
フロント 64 件 / Rust 35 件が全通過。

---
