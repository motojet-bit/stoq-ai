# 改修記録 — StockAnalyzer

指示を受けて改修を行うたびに、**新しい項目を先頭に**追記する。

書式:

```
## YYYY-MM-DD — 見出し
**指示**: ユーザーからの指示（要約）
**対応**: 実際に行った変更
**変更ファイル**: 追加/変更/削除したファイル
**コミット**: <short-sha>
**備考**: 補足・積み残し（なければ省略）
```

---

> 古い記録は [docs/archive/改修記録_2026-08.md](docs/archive/改修記録_2026-08.md) に繰り下げてあります。

---

## 2026-08-02 — アプリ名「株究」の多言語切替 ＋ 免責同意（EULA）ブロック ＋ 撤回

**指示**: ①アプリ名と権利表記を言語で切り替える（ja=「株究 - StoQ AI Analyzer」/ en=「StoQ AI Analyzer」）。`productName` は ASCII の基盤名。ウィンドウタイトルも動的に。OS 言語で初期言語を判定。②初回起動時に閉じられない免責同意モーダル。3 条項（投資の自己責任 / API課金とキー管理 / 現状有姿とデータ保護）。③設定に「同意の撤回」。撤回してもライセンスは保持し、即座に再ロック。④テストと最終ビルド。

**対応（① 名称の多言語化）**:
- **名称の出どころを定数から辞書へ移した。** 言語で変わる以上、定数では持てない。
  `app.name` / `app.nameAccent` / `app.nameRest` / `app.copyright` を ja/en に追加
- `appMeta.ts` は読み出し口（`appName()` / `appCopyright()` / …）と、
  **言語で変わらない `PRODUCT_NAME`** だけを持つ
- **読み込み時に一度だけ組み立てていた文字列を関数化した。**
  `DISCLAIMER_SECTIONS` → `disclaimerSections()`、`OVERVIEW` → `overview()`、
  `FEATURE_REQUEST_URL` → `featureRequestUrl()`、`WelcomeTour` の `STEPS` → `buildSteps()`。
  定数のままだと、言語を切り替えても免責文やロゴが**古い名前を名乗り続ける**
- ウィンドウタイトルは Rust の `window_set_title` コマンド経由で差し替える。
  WebView から Window API を直接叩くには capability の追加が要るうえ、
  このアプリは「OS に触る操作は Rust 側」で統一しているため
- OS 言語の判定に `detectLocale` を新設。
  **`normalizeLocale` とは fallback の向きが逆**で、日本語環境だけ `ja`、
  それ以外は `en` にする。ドイツ語環境の利用者に日本語の画面を出しても読めない

**対応（② 免責同意のブロック）**:
- `ModalShell` に `blocking` を追加。**Esc も ✕ も外す**（背景クリックは元から無効）
- `EulaModal` は同意ボタン以外に先へ進む道が無い。
  素通りできると、免責に同意しないまま分析結果を見られてしまい、
  法的な防衛線として意味をなさなくなる
- 同意状態は Rust の設定ファイル（`eula_agreed` / `eula_agreed_at_ms`）が持つ
- **時刻の無い同意フラグは信用しない。** 設定ファイルを手で書き換えて
  素通りさせられないようにする
- **状態が分からないうちもブロックする。** 読み込み中に素通りさせると、
  未同意のまま一瞬でも操作できてしまう

**対応（③ 撤回）**:
- 設定「一般」タブに `EulaSettings` を追加。同意内容の確認（展開表示）と撤回
- **撤回してもライセンスキーには触れない。** 同意はアプリを使う条件であって、
  買ったライセンスを取り上げる理由にはならない
- 撤回すると `eula_agreed: false` になり、その場で同意モーダルが前面に戻る

**対応（④ Windows インストーラーの文字コード）**:
- 一度 `productName` を「株究 - StoQ AI Analyzer」にしたところ、
  WiX の `light.exe` が `LGHT0311`（コードページ 1252 に無い文字）で失敗した。
  MSI は既定で en-us（cp1252）で作られるため、日本語を製品名に入れられない。
  指示どおり `productName` を ASCII の基盤名に戻したことで解消（回避策は不要になった）

**対応（⑤ テスト）**: 36 件追加（フロント 499 → 535 件 / Rust 209 → 214 件）
- 名称の多言語 17 件: ja/en の表示名と権利表記 / ロゴ分割の連結が表示名に戻る /
  **英語画面に「株究」が出ないこと** / 免責事項とヘルプ AI がいまの名称を名乗る /
  **OS 言語判定（ja→ja、de/fr/zh→en）** / `productName` が ASCII であること
- EULA 19 件: 3 条項の順序と要点（ja/en）/ **未確認のうちもブロック** /
  同意で保存 / **撤回で即ロック** / **撤回がライセンスに触れないこと**（フロント・Rust 両方）/
  モーダルが閉じる手段を持たないこと
- Rust 5 件: 同意・撤回の状態遷移 / 時刻の無い同意フラグを信用しない

**変更ファイル**:
- 追加: `src-tauri/src/eula.rs`、`src/lib/legal/{eula.ts,eula.test.ts,eulaStore.ts,eulaStore.test.ts}`
- 追加: `src/components/{EulaModal,EulaSettings}.tsx`
- 変更(Rust): `commands.rs`（`eula_*` / `window_set_title`）、`settings.rs`、`lib.rs`
- 変更: `appMeta.ts`、`i18n.ts`、`locales.ts`、`locales/{ja,en}.json`、`tauri.conf.json`、`index.html`
- 変更: `ModalShell.tsx`、`StatusBar.tsx`、`MenuBar.tsx`、`WelcomeTour.tsx`、`DisplaySettings.tsx`、`App.tsx`
- 変更: `disclaimer.ts`、`helpKnowledge.ts`、`tooltipText.ts`、`HelpAssistant.tsx`、`LegalDisclaimerModal.tsx`

**コミット**: 90c9500

**備考**:
- **インストーラー名・実行ファイル名は `StoQ AI Analyzer` のままです。**
  日本語を入れると Windows の MSI 生成が通らないため（上記 ④）。
  画面上の表示名・ウィンドウタイトル・権利表記は日本語環境で「株究 - StoQ AI Analyzer」になります。
- ブラウザ（`npm run dev`）で開いたときは EULA を素通りさせています。
  配布物は必ず Tauri 内で動くため、ブロックが効くのは実機のみです。

---

## 2026-08-02 — 提携（アフィリエイト）導線の撤去 ＋ 表記の一元化 ＋ テスト追加

**指示**: ①アプリ名変更・コピーライト更新の表示テスト、IBKR リンクの表示/非表示切り替え、クラウド同期の Unit Test を更新・追加。②テスト全件通過後に本番バイナリをビルド。
（確認の結果、**IBKR リンクは切替ではなく完全削除／アフィリエイト構想は全面破棄**となった）

**対応（① 提携導線の撤去）**:
- `FreeTierLimitModal` から「🎁 無料でライセンスを取得（IBKR等）」ボタンを削除
- `onOpenGuide` プロパティごと廃止し、`App.tsx` の受け渡しも撤去。
  **受け口を残すと呼び出し側で復活する**ため、口ごと閉じた
- `PanelRestoreBar` のコメントにあった「IBKR 等」も一般名詞へ書き換え
- 残るボタンは「あとで」と「ライセンスキーを入力」の 2 つ

**対応（② アプリ名・権利表記の一元化）**:
- **名称を変えるときに直すのは `appMeta.ts` と辞書 JSON だけ**になるようにした。
  画面ごとに直書きされていると、ロゴだけ旧名が残る／免責文だけ旧名が残る、
  といった中途半端な改名が起きる
- `APP_NAME` を参照するよう書き換え: `MenuBar`（ロゴ）/ `WelcomeTour` /
  `FreeTierLimitModal` / `disclaimer.ts` / `helpKnowledge.ts` / `tooltipText.ts`
- ロゴの色分け（先頭語だけ緑）も `APP_NAME_ACCENT` / `APP_NAME_REST` として
  `appMeta.ts` に寄せた
- `statusBar.test.ts` の権利表記チェックは文字列の直書きから**形の検証**へ変更
  （改名しても「© 年 名称. All Rights Reserved.」の形は変わらない）

**対応（③ テスト）**: 40 件追加（フロント 459 → 499 件）
- 提携導線 8 件: **`src/` 全ファイルを走査**して `IBKR` / `アフィリエイト` /
  `affiliate` / `紹介プログラム` / `口座開設` が 1 つも無いことを確認。
  走査が空振りして「違反ゼロ」になるのを防ぐため、読めたファイル数も検査する
- 表記の一貫性 11 件: 権利表記の組み立て / 辞書の `app.name` との一致 /
  **免責事項とヘルプ AI が現在のアプリ名を名乗ること** /
  表示側がアプリ名を直書きしていないこと / 版と製品名の一致
- クラウド同期ストア 21 件: 初期状態は未連携 / 状態の取り込みと失敗時の維持 /
  連携・解除 / **バックアップ後に状態を読み直す** / 復元時に `fileId: null` を明示 /
  自動バックアップの切替 / 失敗しても空配列を返す

**変更ファイル**:
- 追加: `src/components/freeTierLimitModal.test.ts`、`src/lib/ui/appMeta.test.ts`、`src/lib/cloud/cloudStore.test.ts`
- 変更: `src/components/{FreeTierLimitModal,MenuBar,WelcomeTour,PanelRestoreBar,statusBar.test}.tsx|ts`
- 変更: `src/App.tsx`、`src/lib/ui/{appMeta,tooltipText}.ts`、`src/lib/legal/disclaimer.ts`、`src/lib/prompts/helpKnowledge.ts`

**コミット**: ad5497c

**備考**:
- **アプリ名とコピーライトの新しい文言はまだ受け取っていません。**
  指示 1〜3 が届いていないため、現行の `StoQ AI Analyzer` / `© 2026` のままです。
  一元化は済んでいるので、新名称をいただければ `appMeta.ts` の 1 行と
  辞書 2 か所（`app.name` / `menu.help.about`）の変更だけで全画面に反映されます。
- `tauri.conf.json` の `productName` は `StockAnalyzer`（インストーラー名）で、
  表示名 `StoQ AI Analyzer` とは別です。改名時はどちらを変えるかご指示ください。

---

## 2026-08-02 — Google Drive クラウドバックアップ／復元 ＋ 開発者用マスターキー

**指示**: ①設定に「クラウド同期」タブを新設し、`drive.appdata` スコープのみの Google OAuth 2.0 (PKCE) 連携、「☁️ クラウドへバックアップ」「🔄 クラウドから復元」「起動時に自動バックアップ」を実装する。②クラウド同期ロジックの自動テスト。③最終本番バイナリのビルド。

**対応（① OAuth 2.0 PKCE）**:
- `src-tauri/src/cloud/pkce.rs` — `code_verifier` / `code_challenge`(S256) / `state` の生成、
  認可 URL の組み立て、コールバックのクエリ解析
  - **クライアントシークレットを持たない。** 配布バイナリに秘密を埋めても
    誰でも取り出せるので意味がない。PKCE はそのシークレット無しで
    認可コードの横取りを防ぐ仕組み
  - `access_type=offline` と `prompt=consent` を必ず付ける。
    これが無いとリフレッシュトークンが返らず、毎回ログインになる
- `src-tauri/src/cloud/oauth.rs` — ループバック受け口 / トークン交換 / 更新
  - 受け口は **`127.0.0.1` にだけ**立てる（外から届くポートを開けない）。
    ポートは OS に選ばせる（固定にすると他アプリと衝突する）
  - `state` が一致しない応答は中止する
  - アクセストークンは期限の 60 秒手前で先に取り直す（通信途中で切れないため）

**対応（② アプリ専用領域だけを触る）**:
- `src-tauri/src/cloud/drive.rs` — `appDataFolder` への一覧 / アップロード / ダウンロード / 削除
  - 要求するスコープは **`drive.appdata` ひとつだけ**。
    ユーザーのマイドライブ（写真・書類）は一覧すらできない
  - `pkce.rs` のテストで、認可 URL に `drive.file` / `drive.readonly` /
    `auth/drive` が**入らないこと**を固定している

**対応（③ バックアップの中身）**:
- `src-tauri/src/cloud/bundle.rs` — 3 つの DB を 1 つの束にまとめる
  - このアプリのデータは `analyses.db` / `chats.db` / `library.db` に分かれている。
    **1 つだけ戻すと「ポートフォリオには居るのに分析結果が無い」**という
    食い違いが起きるので、まとめて上げてまとめて戻す
  - **`settings.json` は束に入れない。** APIキーとライセンスキーが入っており、
    外部（Google のサーバー）へ送ってよい情報ではない
  - 復元先は既知の 3 ファイル名だけを通す。束は外から来たデータなので、
    名前をそのままパスに使うと `../` でアプリの外へ書き込まれうる
  - 記録したサイズと実際のバイト数が合わなければ復元しない（転送中に切れた束を上書きしない）
- 復元は**全部読めてから書き始める**。途中で失敗して半端に上書きしないため。
  上書き前の現物は `<名前>.before-restore-<時刻>` として同じフォルダに退避する
- バックアップ名は毎回変える（`stoq-backup-<時刻>.json`）。
  **1 つを上書きし続けると、壊れたデータを同期した瞬間に戻せなくなる。**
  世代は 10 件を残して古いほうから整理する

**対応（④ 画面）**:
- `CloudSyncSettings.tsx` を新設し、設定モーダルに「クラウド同期」タブを追加
- 「アクセスする範囲」を画面上でも明示（`drive.appdata` のみ／APIキーは送らない）
- クライアント ID は**通信の前に形式を確認**する。打ち間違えたまま連携すると、
  ブラウザが開いてから Google 側のエラー画面で止まってしまう
- 復元は手元のデータを置き換えるので確認ダイアログを挟む

**対応（⑤ マスターキー）**:
- `license.rs` に `STOQ-DEV-MASTER-2026` を追加。形式チェックを通さずに有効化できる
  （UUID 形式ではないため）

**対応（⑥ テスト）**: Rust 66 件 / フロント 18 件
- PKCE 13 件: RFC 7636 付録 B の検証ベクタ / スコープに個人ファイル権限が入らないこと /
  `access_type=offline` / コールバックの解析
- Drive 12 件: 最新の選択 / 世代整理 / 壊れた応答 / マルチパートの親が `appDataFolder`
- 束 14 件: 往復 / サイズ不一致を弾く / **パスをまたぐ名前を復元しない** / 設定ファイルを含めない
- OAuth 12 件: 応答の解析 / 期限の前倒し判定 / **受け口がループバックのみ** / 実際の待受
- 状態 11 件: **生のトークンがフロント向けの表現に出ないこと**
- フロント 18 件: クライアント ID の形式 / 経過時間の表示 / 一覧の並び / 説明文

**変更ファイル**:
- 追加: `src-tauri/src/cloud/{mod,pkce,oauth,drive,bundle}.rs`
- 追加: `src/lib/cloud/{cloudBackup.ts,cloudBackup.test.ts,cloudStore.ts}`、`src/components/CloudSyncSettings.tsx`
- 変更: `src-tauri/src/{lib,commands,settings,license}.rs`、`src-tauri/Cargo.toml`
- 変更: `src/components/SettingsModal.tsx`、`src/lib/config/settingsStore.ts`、`src/types/index.ts`、`src/locales/{ja,en}.json`

**コミット**: abfafc6

**備考**:
- **Google Cloud の OAuth クライアント ID はユーザー側で発行する必要があります。**
  こちらで作成することはできないため、設定画面から入力する方式にしました
  （Google Cloud Console →「認証情報」→ OAuth クライアント ID →「デスクトップアプリ」）。
- **バックアップに独自の暗号化はかけていません。** 保護は HTTPS と、
  Drive のアプリ専用領域（他アプリからも、ユーザーのドライブ画面からも見えない）によるものです。
  鍵の管理方法が無いまま独自の暗号を足すと、安全になるどころか
  「鍵を失うと戻せない」というリスクだけが増えるため見送りました。
- **マスターキー `STOQ-DEV-MASTER-2026` はバイナリに直接埋まっています。**
  `strings` などで取り出せるため、これは「秘密」ではなく「合鍵」です。
  販売用の実キーと同じ強度は期待できません。
- 復元後は**アプリの再起動**が必要です（DB ファイルごと差し替わるため、
  画面に開いたままの内容は古いままになります）。

---

## 2026-08-02 — フリーミアム（無料 3 銘柄まで）＋ ライセンス誘導ダイアログ

**指示**: ①ライセンス未認証時、これまでに分析したユニークな銘柄数を数え、3 銘柄に達した状態で 4 銘柄目の分析（データ取得・AI分析）を実行しようとしたら処理を中断して制限ダイアログを出す。既存 3 銘柄の再分析・対話・過去ログ閲覧は無制限。②指定文面の誘導モーダル（ボタン 2 つ）。③ライセンス有効化で即座に解除。④テスト。

**対応（① カウントと制限）**:
- `src/lib/license/freeTier.ts`（純粋関数）に判定を集約
  - **数えるのは「銘柄数」であって実行回数ではない。**
    回数で数えると、同じ銘柄を決算のたびに見直す使い方が引っかかり、
    このアプリ本来の価値（決算期をまたいだ追跡）が損なわれる
  - `evaluateAccess` は「ライセンス有効 → 常に許可」「既存銘柄 → 許可」
    「未使用で枠あり → 許可」「未使用で枠なし → 拒否」の 4 分岐
  - 大文字小文字の違いは同じ銘柄として扱う
- 使用済み銘柄は **Rust の設定ファイルに保存**（`Settings.free_tickers`）。
  localStorage だとブラウザデータの消去で制限が外れてしまう
- `registerTicker` は**上限を超えて積まない**。制限中に何度も押したぶんまで
  記録すると、解除後に「使った覚えのない銘柄」が並ぶ
- `App.ensureAccess()` を `handleTickerSubmit`（データ取得）と
  `handleRunAnalysis`（AI分析）の両方の入口に置いた

**対応（② 誘導ダイアログ）**:
- `FreeTierLimitModal` を新設。指定どおりのタイトル・説明文・ボタン 2 つ
- 「ライセンスキーを入力」は**設定モーダルをライセンスタブで直接開く**
  （`SettingsModal` に `initialTab` を追加）
- **何が使えなくなったのかを具体的に示す。** 分析済みの 3 銘柄を列挙し、
  「この 3 銘柄は何度でも使える」と明記した。
  「上限です」だけだと、既存銘柄まで止まったのかが分からず不安になる

**対応（③ 解除）**:
- 判定が `getLicense().activated` を見るだけなので、
  有効化した瞬間から 4 銘柄目が通る（再起動や再読込は不要）
- ライセンスが有効なときは使用済みに登録しない（無制限なので数える意味がない）

**対応（④ テスト）**: 32 件
- 純粋ロジック 21 件: 1〜3 銘柄目は通る / **4 銘柄目で止まる** /
  既存銘柄は上限後も通る / 大文字小文字 / 残り枠 / 保存値の重複 /
  ライセンス有効時は無制限 / 上限を超えて積まない / 通しの流れ
- ストア 11 件: 設定からの取り込み / Rust への保存 / 同じ銘柄では保存しない /
  ライセンス有効時は数えない / **有効化で即座に解除** / 保存失敗時も進む

**変更ファイル**:
- 追加: `lib/license/freeTier.ts` / `.test.ts`,
  `lib/license/freeTierStore.ts` / `.test.ts`, `components/FreeTierLimitModal.tsx`
- 変更(Rust): `settings.rs`（`free_tickers`）, `commands.rs`（`free_tier_set`）, `lib.rs`
- 変更: `App.tsx`, `SettingsModal.tsx`（`initialTab`）, `settingsStore.ts`,
  `types/index.ts`, `helpKnowledge.test.ts`
- 変更: `PROGRESS.md`, `改修記録.md`

**コミット**: `9754352`

**検証**: `npm run verify` ✅ / `cargo test --lib` ✅
フロント **441 件** / Rust **143 件** が全通過。

**備考**: 「🎁 無料でライセンスを取得（IBKR等）」はヘルプ画面を開くところまでです。
**IBKR 等での無料配布の具体的な条件は把握していないため、
ヘルプ AI のナレッジには何も書いていません**（書くと事実でない案内をしてしまいます）。
実際の入手方法が決まったら `helpKnowledge.ts` に追記してください。

---

## 2026-08-02 — i18n 基盤（日本語 / English）＋ チャット添付の分離

**指示**: ①チャット入力欄の 📎 で読み込んだファイルが、上部ドロップゾーンの「一時保存中の資料」と混ざる不具合を修正し、会話ごとの使い捨てとして分離する。②`src/locales/` に `ja.json` / `en.json` を置き、UI テキストを辞書キー管理へ。JSON を足すだけで言語を増やせる構造に。③設定「一般」タブに言語ドロップダウンを置き、英語選択時は Rust 側でシステムプロンプトに英語出力の指示を付与。④テスト。

**対応（① 添付の分離）**:
- `src/lib/chat/chatAttachments.ts` を新設。**抽出だけを行い、どこにも保存しない**
  （`documents_stage` を呼ばないので、上部の「読み込み済み資料」の件数にも入らない）
- **混ぜてはいけない理由**: 上部の資料は 20項目分析に毎回渡る恒久的なコンテキスト、
  チャットの添付は「いまの会話で 1 回使うだけ」。混ざると、
  最新のプレスリリースを 1 度見せたつもりが以降すべての分析に混入する
- 送信時に本文の前へ `<資料 name="…">` で囲んで載せ、**送ったら破棄**する
- 履歴（SQLite）に保存するのは**質問本文だけ**。資料の全文を残すと肥大化する
- 会話を切り替えたときも捨てる（`selectSession` で `clearChatAttachments`）
- 同名は差し替え、上限 5 件を超えたら古いものから外す
- チップで一覧表示し、個別に外せる

**対応（② i18n 基盤）**:
- `src/locales/ja.json`（既定）と `en.json`。キーは `menu.*` `status.*`
  `settings.*` `general.*` `common.*` などの名前空間で分けた
- `lib/i18n/locales.ts` が登録簿。**言語を増やす手順はここ 1 か所**
  （`<code>.json` を置いて配列に 1 行足すだけ）。
  将来の候補（`zh-TW` / `zh-CN` / `th` / `fr` / `de` / `ru` / `pl` / `cs`）も明記した
- `lib/i18n/i18n.ts` が辞書引き・フォールバック・`{{var}}` 差し込み・ストア。
  **外部ライブラリを入れていない。** 必要なのはこの 3 つだけで、
  i18n フレームワークはバンドルが増えるわりに得るものが少ないため
- 訳が無ければ既定言語へ、それも無ければキーをそのまま返す
  （空文字を返すと画面から文字が消えて原因が分からなくなる）
- `en-US` のような地域つきでも言語部分で拾う。初回はブラウザ設定に寄せる
- 移行済み: メニューバー / ステータスバー / ティッカー入力 / AI設定 /
  設定モーダルのタブ・一般タブ / サイドバーのモードタブ

**対応（③ 言語切替と AI 連動）**:
- 設定の「表示」タブを「一般」に改め、先頭に「表示言語（Language）」を配置
- `LlmRequest` に `locale` を追加し、`streamChat` が**送信時に現在の言語を入れる**。
  画面ごとに書き分けると設定と食い違うため 1 か所で決める
- Rust の `language_directive()` が英語のときだけ指示を返す。
  **日本語のときは何も足さない**（本文がもともと日本語なので重ねる意味がない）
- 分析は `build_system_prompt` の末尾に、対話・ヘルプは `llm::send` で
  システムプロンプトの後ろに付ける。**後ろにあるほど守られやすい**
- **見出しは訳さないよう明示**した。訳されると `parseAnalysis` が結果を読めなくなる

**対応（④ テスト）**: 43 件
- i18n 22 件: 登録簿 / **すべての言語が同じキーを持つこと（訳し忘れの検出）** /
  空の訳が無いこと / 地域つきの正規化 / フォールバック / `{{var}}` /
  切り替えと永続化 / `<html lang>` / 同値なら通知しない / AI へ渡す言語名
- チャット添付 14 件: **一時保存を一切触らないこと** / 同名の差し替え /
  上限 / 非対応形式 / 抽出失敗時の継続 / 送信後の破棄 / タグ付けの形
- Rust 7 件: 日本語では足さない / 英語の文面 / 見出しを訳さない指示 /
  末尾に置かれること / 言語を変えても評価項目は変わらないこと

**変更ファイル**:
- 追加: `locales/ja.json`, `locales/en.json`, `lib/i18n/locales.ts`,
  `lib/i18n/i18n.ts` / `.test.ts`, `lib/chat/chatAttachments.ts` / `.test.ts`
- 変更(Rust): `prompts/mod.rs`（`locale` と `language_directive`）, `llm/mod.rs`
- 変更: `ChatPanel.tsx`, `DisplaySettings.tsx`, `SettingsModal.tsx`, `MenuBar.tsx`,
  `StatusBar.tsx`, `TickerInput.tsx`, `ProviderMenu.tsx`, `Sidebar.tsx`,
  `sidebarMode.ts`, `llm/client.ts`, `chatStore.ts`, `App.tsx`,
  `disclaimer.test.ts`, `statusBar.test.ts`, `archive.test.ts`
- 変更: `PROGRESS.md`, `改修記録.md`

**コミット**: `717c5f3`

**検証**: `npm run verify` ✅ / `cargo test --lib` ✅
フロント **409 件** / Rust **143 件** が全通過。

**備考**: 辞書化は主要な UI（メニュー・ステータス・設定・入力欄）まで進めました。
**分析結果パネル・比較・ポートフォリオの本文はまだ日本語のまま**です。
キーを足して差し替えていけば同じ仕組みで移行できます。
