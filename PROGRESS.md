# PROGRESS — StockAnalyzer

> このファイルは**作業開始時に必ず読み込む**こと。
> 作業終了・区切り時には「現在のステータス」と「作業履歴」を更新すること。

---

## 現在のステータス

**フェーズ: 3 — 複数銘柄の比較・管理基盤（着手）**
**Phase 3 完了（Step 1 〜 10）＋ i18n 全面対応 ＋ フリーミアム ＋ クラウド同期
＋ 自動更新 ＋ 販売 LP 公開 ＋ AIクロスディベート ＋ 参照付き根拠／ v1.0 リリース候補**
**第65〜72章まで完了**
**自動テスト 1,061 件（フロント 806 / Rust 255）で保護されている**

**残る外部作業**:
① ~~GitHub の Settings → Pages で `gh-pages` / `(root)` を有効化する~~ →
   **有効化済みを確認**。https://motojet-bit.github.io/stoq-ai/ は配信中（2026-08-03）。
   ただし**まだ告知していない先行公開**であり、LP に載せたクロスディベートは
   配信ビルド v0.1.0 に入っていない。**告知の前に新しいビルドを出すこと**
② `stoq-releases` の v0.1.0 にある **`latest.json` を差し替える**。
   GitHub がアセット名の空白をドットへ置き換えるため、
   公開中のものは URL が 404 になっている（正しいものは `dist-release/latest.json`）。
   `gh release upload v0.1.0 -R motojet-bit/stoq-releases dist-release/latest.json --clobber`
③ 実機での見た目の確認（設定モーダルの収まり／履歴削除／2 分割ディベート）

### 自動テスト

```
npm run verify   # typecheck → test → build を通しで実行
npm run test     # フロントエンド 806 件（vitest）
cargo test --manifest-path src-tauri\Cargo.toml --lib   # Rust 255 件
```

| 対象 | 件数 | 主な検証内容 |
| --- | --- | --- |
| フロントエンド | 806 | **直書き日本語の検出（辞書化の抜け・訳してはいけない内部キーの両方）/ 更新バージョンの比較** / **アプリ名の多言語切替・OS 言語判定 / EULA のブロックと撤回（ライセンス保持）** / 提携導線が残っていないこと / アプリ名・権利表記の一貫性 / クラウド同期ストア** / クラウド同期の表示ロジック（クライアント ID の形式・経過時間・一覧の並び）** / 保存構造のシリアライズ / CSV・MD・JSON 出力 / ヒートマップ** / ドロップダウンの位置決め・モーダル背景・タブ並べ替え** / サイドバーのモード切替・アーカイブのグループ化** / 横並び比較のデータ整形・スコア集計** / 免責事項の文面・開閉・メニュー呼び出し** / プロンプトの秘匿（核心文言がフロントに残っていないこと）** / 可変閾値の保存・丸め / モーダルのドラッグ位置 / **ヘルプ AI のナレッジ生成** / ショートカットキーの正規化・衝突検出 / **パイプ区切りとティッカー羅列のパース** / **検討中銘柄の削除** / リサイズ境界値とドラッグ方向 / パネル移動の判定 / フォント範囲 10〜28px / 配置の正規化 / 分析結果パーサ / プロンプト予算 / トークン概算 |
| Rust | 255 | **エラーコードの一意性・payload 形式** / **免責同意の状態遷移 / PKCE（RFC 7636 検証ベクタ・個人ファイル権限を要求しないこと）/ バックアップ束（サイズ不一致・パス越えの拒否・設定ファイルを含めない）/ ループバック受け口 / 生のトークンが外へ出ないこと** / 書き出し先の決定・ライセンス形式 / ポートフォリオの CRUD と CASCADE / 秘匿プロンプトの結合・役割選択・閾値注入** / 閾値の永続化と旧設定ファイルの読み込み / データ取得元の切替とキー判定 / **FMP・Alpha Vantage の応答パース** / 旧 DB へのマイグレーション / **ショートカット割り当ての保存** / **検討中銘柄・役割ライブラリの CRUD** / チャット履歴（自動命名・CASCADE・アーカイブ）/ 期末日の突き合わせ / マスク処理 / HTML 抽出 |

Phase 2 の全体設計を `docs/設計.md` に記載。
Step 1 として設定モーダル・APIキーの安全な保存・LLM 接続基盤・対話パネルからの
ストリーミング疎通確認までを実装し、Step 1.1 で OpenAI互換プロバイダを
固定 1 枠から**自由に追加・削除できるリスト**へ拡張した。

### Phase 2 のステップ

| Step | 内容 | 状態 |
| --- | --- | --- |
| **Step 1** | APIキー設定画面（モーダル UI）と LLM 接続基盤 | ✅ 完了 |
| **Step 1.1** | OpenAI互換プロバイダの可変長リスト化（追加・削除） | ✅ 完了 |
| **Step 2** | Financial Data Fetcher（Yahoo Finance / SEC EDGAR）と UI 接続 | ✅ 完了 |
| **Step 3** | PDF パーサ ＋ ステージング ＋ トークンカウンター | ✅ 完了 |
| **Step 4** | Prompt Engine（20項目）・ストリーミング・中断 | ✅ 完了 |
| **Step 4.1** | スマート圧縮（中間カット廃止）＋ 4Q モメンタム | ✅ 完了 |
| **Step 5** | フレキシブル UI ＋ 分析結果の永続化（SQLite） | ✅ 完了 |
| **Step 5.1** | 3 ペイン構成への再構築 ＋ 破壊的操作の確認 ＋ プロバイダ UI | ✅ 完了 |
| **Step 5.2** | 2 カラム化 ＋ 分析根拠表示 ＋ チャット履歴の実機能化 | ✅ 完了 |
| **Step 5.3** | レイアウト修復 ＋ フォント可変 ＋ パネル移動 ＋ 自動テスト | ✅ 完了 |
| **Step 5.4** | IBKR 風の操作感（リサイズ方向・グリップ移動・フォント全域） | ✅ 完了 |
| **Step 5.5** | コントロール類のサイズ固定 ＋ D&D の 🚫 解消 | ✅ 完了 |
| **Step 5.6** | フォント UI のメニューバー移設 ＋ 折りたたみの余白ゼロ化 ＋ Pointer Events 移行 | ✅ 完了 |
| Step 6 | 銘柄比較・分析結果の書き出しなど | → Phase 3 へ |

### Phase 3 のステップ

| Step | 内容 | 状態 |
| --- | --- | --- |
| **Step 1** | 検討中銘柄 ＋ チャットのアーカイブ ＋ 役割ライブラリ | ✅ 完了 |
| **Step 2** | UI/UX 仕上げ ＋ コンプライアンス表現 ＋ ショートカット基盤 | ✅ 完了 |
| **Step 3** | データ取得元の切替 ＋ ドラッグ可能モーダル ＋ ヘルプ AI | ✅ 完了 |
| **Step 4** | ツールチップ ＋ 初回チュートリアル ＋ 可変閾値 | ✅ 完了 |
| **Step 5** | プロンプトのバイナリ秘匿 ＋ 4大分析パターン ＋ 表示設定 | ✅ 完了 |
| **Step 6** | 免責事項（Legal Disclaimer）の全文モーダル ＋ 常時テロップ | ✅ 完了 |
| **Step 7** | ティッカー入力案内 ＋ 複数銘柄の横並び比較ダッシュボード | ✅ 完了 |
| **Step 8** | サイドバーのモード切替 ＋ 複数ポートフォリオ（フォルダ型） | ✅ 完了 |
| **Step 9** | UI 不具合修正 ＋ マイポートフォリオ専有レイアウト ＋ 保存導線 | ✅ 完了 |
| **Step 10** | 保存構造の標準化 ＋ エクスポート ＋ ヒートマップ ＋ ライセンス | ✅ 完了 |
| Step 2 | 複数銘柄の横断比較ビュー | ⬜ |
| Step 3 | 分析結果の書き出し（Markdown / PDF） | ⬜ |

### Phase 3 Step 1 の実装内容

| 項目 | 状態 |
| --- | --- |
| 検討中銘柄の SQLite 保存（`library.db`） | ✅ `src-tauri/src/candidates.rs` |
| パイプ区切りテキストの一括インポート | ✅ `CandidateImportModal.tsx` |
| パース（全角パイプ・空白除去・重複・エラー検知） | ✅ `parseCandidates.ts` |
| 小窓 UI（縦横スクロール・✕ 削除・右クリック） | ✅ `CandidateStocksPanel.tsx` / `ContextMenu.tsx` |
| クリックで上部ティッカー入力欄へ連動 → 即分析 | ✅ `TickerInput` の `preset` |
| チャット履歴の独立縦スクロール | ✅ `Sidebar.tsx`（`min-h-0` + `overflow-y-auto`） |
| チャットのアーカイブ / 復元（`is_archived`） | ✅ `chats.rs` の `ALTER TABLE` 移行 |
| 役割（システムプロンプト）ライブラリ | ✅ `src-tauri/src/prompts.rs` |
| 対話ヘッダーからの役割ワンタップ切替 | ✅ `PromptLibraryMenu.tsx` |
| 役割の追加・編集・削除 UI | ✅ `PromptLibraryModal.tsx` |

### Step 1 の実装内容

| 項目 | 状態 |
| --- | --- |
| 設定の永続化（OS のアプリ設定ディレクトリ） | ✅ `src-tauri/src/settings.rs` |
| APIキーのマスク表示（生の値をフロントに渡さない） | ✅ |
| 設定モーダル UI | ✅ `src/components/SettingsModal.tsx` |
| プロバイダ選択（組み込み 3 種 + カスタム任意個） | ✅ |
| **OpenAI互換プロバイダの追加・削除（可変長リスト）** | ✅ ラベル / Base URL / キー / モデル名 |
| モデル名のコンボボックス入力（候補選択 + 手入力） | ✅ `src/components/ModelCombo.tsx` |
| SEC User-Agent の設定 | ✅ |
| LLM 共通インターフェース | ✅ `src-tauri/src/llm/` |
| SSE ストリーミング → Tauri Channel | ✅ |
| 対話パネルからの疎通確認 | ✅ **実機で往復成功（OpenAI / gpt-5.6）** |
| メニュー「ファイル > 設定…」/「ヘルプ > APIキーの設定…」から起動 | ✅ |
| APIキーインジケーターのクリックで起動 | ✅ |
| `Ctrl+,` ショートカット | ✅ |

### 重要な設計上の判断（詳細は `docs/設計.md`）

1. **HTTP はすべて Rust 側から発行する。** CORS 回避、SEC が要求する User-Agent の指定、
   APIキーの秘匿、Yahoo の Cookie 保持が理由。`src/lib/` は Tauri コマンドの薄いラッパ。
2. **PDF は `pdf-parse` ではなく `pdfjs-dist` を使う。** `pdf-parse` は Node 専用で
   WebView では動作しないため（Step 3 で実装）。
3. **Anthropic には `temperature` を送らない。** Claude Opus 5 では `temperature` が
   廃止されており、送ると HTTP 400 になる。深さの制御は `output_config.effort` で行う。
   OpenAI / Gemini / Custom には設計どおり `temperature: 0.0` を送信。
4. **`.env` の `VITE_*` によるキー読み取りは廃止。** Phase 1 の暫定実装を削除し、
   Rust 側の設定ファイルへ移行した。

---

### Step 2 の実装内容

| 項目 | 状態 |
| --- | --- |
| Yahoo Finance クライアント（Cookie + crumb 認証） | ✅ `src-tauri/src/yahoo.rs` |
| 主要指標 6 グループ 36 項目の取得・整形 | ✅ |
| `quoteSummary` 失敗時も株価だけは返すフォールバック | ✅ |
| SEC 提出状況の軽量照会（本文を落とさない） | ✅ `edgar::fetch_status` |
| ティッカー入力 →「分析」でのデータ取得 | ✅ YF と SEC を並行実行 |
| 指標カードへの描画 | ✅ `MetricCard.tsx` |
| 資料準備インジケーター（🟢/🟡/🔴） | ✅ `FilingStatusBadge.tsx` |
| ローディング表示（スケルトン / スピナー） | ✅ `MetricCardSkeleton.tsx` |
| エラー時のトースト通知と再試行ボタン | ✅ `ToastHost.tsx` |

### Step 3 の実装内容

| 項目 | 状態 |
| --- | --- |
| PDF テキスト抽出（`pdfjs-dist`、動的 import） | ✅ `src/lib/parser/pdf.ts` |
| DOCX テキスト抽出（`fflate` で ZIP 展開） | ✅ `src/lib/parser/docx.ts` |
| HTML / TXT / MD / CSV / JSON の取り込み | ✅ `src/lib/parser/extractText.ts` |
| D&D とファイル選択ダイアログの両対応 | ✅ `PdfDropZone.tsx` |
| `temp_documents/` への永続化（再起動後も保持） | ✅ `src-tauri/src/documents.rs` |
| 一時保存中の資料一覧（🟢 バッジ付き） | ✅ `DocumentTray.tsx` |
| プレビュー（抽出テキスト全文をモーダル表示） | ✅ `DocumentPreviewModal.tsx` |
| リネーム（ダブルクリックでその場編集） | ✅ `StagedFileChip.tsx` |
| 個別削除（✕）と一括クリア | ✅ |
| トークン概算とメーター（🟢/🟡/🔴） | ✅ `TokenMeter.tsx` / `tokenCount.ts` |
| 取り込み中のローディング表示 | ✅ 抽出中 / 保存中 の 2 段階 |

### Step 4 の実装内容

| 項目 | 状態 |
| --- | --- |
| 20項目の評価基準（1 ファイルで差し替え可能） | ✅ `src/lib/prompts/criteria.ts` |
| システムプロンプト（出力フォーマット厳守・幻覚対策） | ✅ `systemPrompt.ts` |
| 統合プロンプトのパッキングと予算配分 | ✅ `buildPrompt.ts` |
| SEC 本文の節優先抽出（Risk Factors / MD&A 等） | ✅ |
| トークン上限超過時の自動切り詰め + 最終検算 | ✅ `enforceLimit` |
| Markdown テーブルのパース（途中経過にも対応） | ✅ `parseAnalysis.ts` |
| 分析実行のオーケストレーション | ✅ `analysisRunner.ts` |
| ストリーミング逐次描画（評価カード） | ✅ `AnalysisPanel.tsx` |
| 中断ボタン（途中結果を保持） | ✅ Rust `llm_cancel` |
| PPTX 対応 / ✏️ リネーム / ドロップゾーン注記 | ✅ Step 3 の微修正 |

### Step 5 の実装内容

| 項目 | 状態 |
| --- | --- |
| 3 ペイン構成（左: 市場データ / 右: 分析結果 / 下: 対話） | ✅ Step 5.1 で再構築 |
| ドラッグでのリサイズ（縦横どちらも） | ✅ `ResizableSplit.tsx` |
| 各ペインの最小化（`_` ボタン） | ✅ `PanelHeader.tsx` |
| 文字サイズ 3 段階（A− / A+、localStorage 保存） | ✅ `textScale.ts` |
| 評価テーブルの列幅・行間・スコアドットの拡大 | ✅ |
| 生成完了と同時に SQLite へ自動保存 | ✅ `analyses.rs` |
| 銘柄タブを開いたときの自動復元 | ✅ |
| 「クリア」ボタン（明示操作時のみ削除） | ✅ |

---

## 次にやること（TODO）

### 実装済み（第 72 章）

`docs/次回実装アイデア.md` に挙げた 3 件はすべて実装済み。
設計の理由は同ファイルに残してある（着手時の判断を後から追えるようにするため）。

- [x] 決算期のユーザー承認ダイアログ
- [x] SEC 自動フォールバック（10-Q / 10-K）
- [x] アドホック分析の入れ子ツリー（`parent_id` / `branch_no`）

### Phase 4 以降の候補
- [ ] 対話パネルから分析結果を踏まえた追加質問ができるようにする
- [ ] 比較結果の並べ替え・列の入れ替え
- [ ] 比較ダッシュボードの書き出し
- [ ] 分析結果の書き出し（Markdown / PDF）
- [ ] メニューバー各項目に実際の動作を割り当てる
- [ ] 画像のみの PDF（スキャン資料）への対応方針を決める（OCR を入れるか否か）
- [ ] 20項目の評価基準をユーザーが UI 上で編集できるようにする

---

## 作業履歴

> 古い履歴は [docs/archive/PROGRESS_履歴_2026-08.md](docs/archive/PROGRESS_履歴_2026-08.md)
> に繰り下げてあります。

### 2026-08-03 — LP: 購入前ダイアログの件数ずれ（4 点 → 5 点）を修正 ✅
- 項目は 5 つあるのにリード文が「4 点」のままだった（5 つ目は V0 価格の回に追加）
- **件数は JS が数え直す**（`[data-consent-count]`）。
  文言側の数字を直し忘れても、以後は同じずれが起きない。
  **HTML にも正しい数字を残す**（JS が動かなくても決済へ通す作りのため）
- **公開**: `git subtree push --prefix lp origin gh-pages` ✅（`64a70f6` → `b199f60`）

### 2026-08-03 — LP: ヒーローを誠実なトーンへ ＋ 改行調整 ＋ 再デプロイ ✅
- メインを「情報や判断を他人に依存する投資は／**卒業**」へ。
  **「卒業」だけを 2 行目に落として色を乗せる**（1 行に流すと結論が埋もれる）
- 前回の「スクール頼みの投資は、もう卒業。」を撤回。
  **特定の学び方を否定する形になっていた**ため、主語を読み手の状態へ寄せた
- 本文は指定の 3 文をそれぞれ 1 行に。「投資学習を徹底サポート」の立ち位置を明示
- Core Value の見出しを「1つの画面で完結する、／次世代の決算分析体験。」へ。
  **読点でも改行する**（入れないと幅しだいで意味の切れ目以外で折り返す）
- **公開**: `git subtree push --prefix lp origin gh-pages` ✅（`4c0c95e` → `64a70f6`）。
  配信中の HTML に新コピーが載り、旧コピーが残っていないことも確認

### 2026-08-03 — LP: 自立志向の投資家へ文面刷新（なぜなぜ分析）✅
- ヒーローを「スクール頼みの投資は、もう卒業。／専属 AI と『なぜ』を突き詰める、
  自立型・決算アナライザー。」へ。**`sm:text-6xl` → `sm:text-5xl`**
  （文字数がほぼ倍になり、6xl だと 4 行に割れて圧が出る）
- `#pains`（こんな壁を感じていませんか？）を**ヒーローと Core Value のあいだ**に新設。
  4 項目。**警告色を使わず、主語は常に読み手側に置く**
  （「スクールが悪い」ではなく「通うのはハードルが高い」）
- AI クロスディベートを `#features` の**先頭に単独で**配置。
  強気派（Bull）／慎重派（Bear）の役割は実装のプロンプトに合わせた
- 機能カード ③ を「BYOK」→「完全ローカル・買い切り」へ改題し、
  月額費用・コミュニティへの気遣いが不要である旨を追記
- **検証**: `lp/index.html` のみの変更（アプリ側のコードには触れていない）
- **注意**: `lp/index.html` は並行セッションのコミット `56bab50` に巻き込まれて入った。
  **add したまま別セッションの commit が走った**ため
- **公開**: `git subtree push --prefix lp origin gh-pages` ✅（`faccc69` → `4c0c95e`）。
  https://motojet-bit.github.io/stoq-ai/ が 200 で新コピーを配信中（実機確認用の先行公開）
- **GitHub Pages は既に有効だった**（`gh-pages` / `(root)`）。残作業 ① は消してよい
- **未解消**: LP に載せたクロスディベートは**配信ビルド（v0.1.0）に未反映**。
  いま落とせるインストーラーにこの機能は入っていない。
  **告知の前に、この機能を含む版をリリースすること**

### 2026-08-03 — 第72章: 決算期の承認 ＋ SEC 自動取得 ＋ 入れ子ツリー ✅
- 資料つきの分析は**決算期を確認してから**進む。判別できなかったときも聞く
  （黙って期なしで進めると、あとから期と突き合わせられない）
- 確定した期はメモリのみ。永続化すると資料を入れ替えても古い確定が効く
- 添付 0 件なら SEC の 10-Q（無ければ 10-K）を自動取得。
  **自動取得した旨をトースト・根拠欄・プロンプトの 3 か所に出す**
  （渡していない資料が根拠に入るため。プロンプトにも書かないと
  「添付いただいた資料」と書かれる）
- `analysis_history` に `parent_id` / `branch_no` を追加（ALTER TABLE）。
  期中の開示を `Q2-01` 形式で四半期の下へ
- **親を消しても子は残す。** 親を失った子は根として出す
- **枝番は保存時に採番。** 表示のたびに数え直すと、削除で既存の番号がずれる
- アドホックは `analyses`（最新 1 件）を上書きしない
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 806 件 / Rust 255 件）

### 2026-08-03 — 第69・71章: 参照付き根拠 ＋ 決算期の自動特定と YF 連動 ✅
- 秘匿プロンプトに「数値の出所」節を新設。添付資料の数値には
  `【参照: p.XX "原文"】` を必ず添えさせる
- **「参照を付けられない数値は、書いてはいけない数値」**という言い方に統一。
  「推測で断定しない」だけでは、それらしい値を置く余地が残る
- 出力に「参照した原文」節を追加。`citations.ts` で参照を機械的に抽出できる
  （**確かめられなければ、守られていない出力に気づけない**）
- `fiscalPeriod.ts` で決算期を自動特定（英日・全角・通期に対応）。
  **先頭 4000 字だけを見る**（本文の前年同期に引っ張られないため）
- **判別できなければ null**。推測で埋めると別の期のデータと突き合わせた分析が出る
- `periodLink.ts` で Yahoo の四半期に突き合わせ、当時の値を表でプロンプトへ載せ、
  資料の数値との食い違いを指摘させる
- **当たらなくてもエラーにしない。** 取得できなかった旨を明記して PDF 単体で続ける
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 783 件 / Rust 255 件）
- **未了**: 参照の抽出結果を画面に出す導線（バッジ等）は次の区切りで

### 2026-08-03 — 2分割 AIクロスディベート（Stage 2）✅
- 設定に「ディベート」タブ。**メイン分析とは別のプロバイダ／モデル**を選べる
- 未設定ならメインへ落とす。ただし同じになる場合は画面で注意する
  （同じモデルでは同じ思い込みがそのまま残る）
- 分析画面を左右 2 分割（🔥 で開閉、既定は閉）。
  **閉じているときは分割そのものを作らない**（幅ゼロのペインは掴めるのに動かない）
- 「🔥 1ターンディベート実行」は **批判 → 反論 の 1 往復で打ち止め**。
  自動で次を始める経路が無く、走行中の再実行も拒否する
- 反論はメイン分析と同じプロバイダが答える（元の分析を書いた本人が答える構図）
- ディベートのプロンプトは Rust の `prompts/debate_*.md`。フロントへは返さない

### 2026-08-03 — 設定モーダル ＋ SEC UA 補完 ＋ 同期ガイド ＋ 履歴削除 ✅
- タブ名を折り返さず横スクロールへ。**単語の途中で切れるとどのタブか読めない**
- モーダルを 90vw × 85vh に収め、中身は縦横スクロール。
  **`max-w` を二重に指定せず、覆いの余白を vw/vh で取った**
  （二重指定はどちらが勝つか CSS の並び順まかせになる）
- SEC の User-Agent はメールだけなら `StoQ-App <mail>` を補う。
  名前入りはそのまま通す（重ねると二重になる）
- クラウド同期に折り畳みガイド（`CloudSyncGuide.tsx`、既定は閉）
- 分析履歴に削除を追加。**開く操作とは別ボタン**にし、確認ダイアログを挟む。
  画面からは先に消して、失敗したら戻す
- 直書き検出テストが `{式} 日本語` を素通りしていたのを修正 →
  出てきた 11 ファイル・21 件を辞書へ（辞書 948 キー）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 728 件 / Rust 246 件）
- **要確認**: 同期ガイドの文面が実装（OAuth・フォルダ選択なし）と合っていない
