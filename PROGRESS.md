# PROGRESS — StockAnalyzer

> このファイルは**作業開始時に必ず読み込む**こと。
> 作業終了・区切り時には「現在のステータス」と「作業履歴」を更新すること。

---

## 現在のステータス

**フェーズ: 3 — 複数銘柄の比較・管理基盤（着手）**
**Phase 2: Step 1 〜 5.6 ✅ 完了 ／ Phase 3: Step 1 / 2 / 3 ✅ 完了**
**自動テスト 251 件（フロント 164 / Rust 87）で保護されている**

### 自動テスト

```
npm run verify   # typecheck → test → build を通しで実行
npm run test     # フロントエンド 164 件（vitest）
cargo test --manifest-path src-tauri\Cargo.toml --lib   # Rust 87 件
```

| 対象 | 件数 | 主な検証内容 |
| --- | --- | --- |
| フロントエンド | 164 | **モーダルのドラッグ位置** / **ヘルプ AI のナレッジ生成** / ショートカットキーの正規化・衝突検出 / **パイプ区切りとティッカー羅列のパース** / **検討中銘柄の削除** / リサイズ境界値とドラッグ方向 / パネル移動の判定 / フォント範囲 10〜28px / 配置の正規化 / 分析結果パーサ / プロンプト予算 / トークン概算 |
| Rust | 87 | **データ取得元の切替とキー判定** / **FMP・Alpha Vantage の応答パース** / 旧 DB へのマイグレーション / **ショートカット割り当ての保存** / **検討中銘柄・役割ライブラリの CRUD** / チャット履歴（自動命名・CASCADE・アーカイブ）/ 期末日の突き合わせ / マスク処理 / HTML 抽出 |

Phase 2 の全体設計を `docs/設計.md` に記載。
Step 1 として設定モーダル・APIキーの安全な保存・LLM 接続基盤・対話パネルからの
ストリーミング疎通確認までを実装し、Step 1.1 で OpenAI互換プロバイダを
固定 1 枠から**自由に追加・削除できるリスト**へ拡張した。

### Phase 2 のステップ

| Step | 内容 | 状態 |
| --- | --- | --- |
| **Step 1** | APIキー設定画面（モーダル UI）と LLM 接続基盤 | ✅ 完了 |
| **Step 1.1** | OpenAI互換プロバイダの可変長リスト化（追加・削除） | ✅ 完了 |
| **Step 2** | Financial Data Fetcher（Yahoo Finance / SEC EDGAR）と UI 接続 | ✅ 完了 |
| **Step 3** | PDF パーサ ＋ ステージング ＋ トークンカウンター | ✅ 完了 |
| **Step 4** | Prompt Engine（20項目）・ストリーミング・中断 | ✅ 完了 |
| **Step 4.1** | スマート圧縮（中間カット廃止）＋ 4Q モメンタム | ✅ 完了 |
| **Step 5** | フレキシブル UI ＋ 分析結果の永続化（SQLite） | ✅ 完了 |
| **Step 5.1** | 3 ペイン構成への再構築 ＋ 破壊的操作の確認 ＋ プロバイダ UI | ✅ 完了 |
| **Step 5.2** | 2 カラム化 ＋ 分析根拠表示 ＋ チャット履歴の実機能化 | ✅ 完了 |
| **Step 5.3** | レイアウト修復 ＋ フォント可変 ＋ パネル移動 ＋ 自動テスト | ✅ 完了 |
| **Step 5.4** | IBKR 風の操作感（リサイズ方向・グリップ移動・フォント全域） | ✅ 完了 |
| **Step 5.5** | コントロール類のサイズ固定 ＋ D&D の 🚫 解消 | ✅ 完了 |
| **Step 5.6** | フォント UI のメニューバー移設 ＋ 折りたたみの余白ゼロ化 ＋ Pointer Events 移行 | ✅ 完了 |
| Step 6 | 銘柄比較・分析結果の書き出しなど | → Phase 3 へ |

### Phase 3 のステップ

| Step | 内容 | 状態 |
| --- | --- | --- |
| **Step 1** | 検討中銘柄 ＋ チャットのアーカイブ ＋ 役割ライブラリ | ✅ 完了 |
| **Step 2** | UI/UX 仕上げ ＋ コンプライアンス表現 ＋ ショートカット基盤 | ✅ 完了 |
| **Step 3** | データ取得元の切替 ＋ ドラッグ可能モーダル ＋ ヘルプ AI | ✅ 完了 |
| Step 2 | 複数銘柄の横断比較ビュー | ⬜ |
| Step 3 | 分析結果の書き出し（Markdown / PDF） | ⬜ |

### Phase 3 Step 1 の実装内容

| 項目 | 状態 |
| --- | --- |
| 検討中銘柄の SQLite 保存（`library.db`） | ✅ `src-tauri/src/candidates.rs` |
| パイプ区切りテキストの一括インポート | ✅ `CandidateImportModal.tsx` |
| パース（全角パイプ・空白除去・重複・エラー検知） | ✅ `parseCandidates.ts` |
| 小窓 UI（縦横スクロール・✕ 削除・右クリック） | ✅ `CandidateStocksPanel.tsx` / `ContextMenu.tsx` |
| クリックで上部ティッカー入力欄へ連動 → 即分析 | ✅ `TickerInput` の `preset` |
| チャット履歴の独立縦スクロール | ✅ `Sidebar.tsx`（`min-h-0` + `overflow-y-auto`） |
| チャットのアーカイブ / 復元（`is_archived`） | ✅ `chats.rs` の `ALTER TABLE` 移行 |
| 役割（システムプロンプト）ライブラリ | ✅ `src-tauri/src/prompts.rs` |
| 対話ヘッダーからの役割ワンタップ切替 | ✅ `PromptLibraryMenu.tsx` |
| 役割の追加・編集・削除 UI | ✅ `PromptLibraryModal.tsx` |

### Step 1 の実装内容

| 項目 | 状態 |
| --- | --- |
| 設定の永続化（OS のアプリ設定ディレクトリ） | ✅ `src-tauri/src/settings.rs` |
| APIキーのマスク表示（生の値をフロントに渡さない） | ✅ |
| 設定モーダル UI | ✅ `src/components/SettingsModal.tsx` |
| プロバイダ選択（組み込み 3 種 + カスタム任意個） | ✅ |
| **OpenAI互換プロバイダの追加・削除（可変長リスト）** | ✅ ラベル / Base URL / キー / モデル名 |
| モデル名のコンボボックス入力（候補選択 + 手入力） | ✅ `src/components/ModelCombo.tsx` |
| SEC User-Agent の設定 | ✅ |
| LLM 共通インターフェース | ✅ `src-tauri/src/llm/` |
| SSE ストリーミング → Tauri Channel | ✅ |
| 対話パネルからの疎通確認 | ✅ **実機で往復成功（OpenAI / gpt-5.6）** |
| メニュー「ファイル > 設定…」/「ヘルプ > APIキーの設定…」から起動 | ✅ |
| APIキーインジケーターのクリックで起動 | ✅ |
| `Ctrl+,` ショートカット | ✅ |

### 重要な設計上の判断（詳細は `docs/設計.md`）

1. **HTTP はすべて Rust 側から発行する。** CORS 回避、SEC が要求する User-Agent の指定、
   APIキーの秘匿、Yahoo の Cookie 保持が理由。`src/lib/` は Tauri コマンドの薄いラッパ。
2. **PDF は `pdf-parse` ではなく `pdfjs-dist` を使う。** `pdf-parse` は Node 専用で
   WebView では動作しないため（Step 3 で実装）。
3. **Anthropic には `temperature` を送らない。** Claude Opus 5 では `temperature` が
   廃止されており、送ると HTTP 400 になる。深さの制御は `output_config.effort` で行う。
   OpenAI / Gemini / Custom には設計どおり `temperature: 0.0` を送信。
4. **`.env` の `VITE_*` によるキー読み取りは廃止。** Phase 1 の暫定実装を削除し、
   Rust 側の設定ファイルへ移行した。

---

### Step 2 の実装内容

| 項目 | 状態 |
| --- | --- |
| Yahoo Finance クライアント（Cookie + crumb 認証） | ✅ `src-tauri/src/yahoo.rs` |
| 主要指標 6 グループ 36 項目の取得・整形 | ✅ |
| `quoteSummary` 失敗時も株価だけは返すフォールバック | ✅ |
| SEC 提出状況の軽量照会（本文を落とさない） | ✅ `edgar::fetch_status` |
| ティッカー入力 →「分析」でのデータ取得 | ✅ YF と SEC を並行実行 |
| 指標カードへの描画 | ✅ `MetricCard.tsx` |
| 資料準備インジケーター（🟢/🟡/🔴） | ✅ `FilingStatusBadge.tsx` |
| ローディング表示（スケルトン / スピナー） | ✅ `MetricCardSkeleton.tsx` |
| エラー時のトースト通知と再試行ボタン | ✅ `ToastHost.tsx` |

### Step 3 の実装内容

| 項目 | 状態 |
| --- | --- |
| PDF テキスト抽出（`pdfjs-dist`、動的 import） | ✅ `src/lib/parser/pdf.ts` |
| DOCX テキスト抽出（`fflate` で ZIP 展開） | ✅ `src/lib/parser/docx.ts` |
| HTML / TXT / MD / CSV / JSON の取り込み | ✅ `src/lib/parser/extractText.ts` |
| D&D とファイル選択ダイアログの両対応 | ✅ `PdfDropZone.tsx` |
| `temp_documents/` への永続化（再起動後も保持） | ✅ `src-tauri/src/documents.rs` |
| 一時保存中の資料一覧（🟢 バッジ付き） | ✅ `DocumentTray.tsx` |
| プレビュー（抽出テキスト全文をモーダル表示） | ✅ `DocumentPreviewModal.tsx` |
| リネーム（ダブルクリックでその場編集） | ✅ `StagedFileChip.tsx` |
| 個別削除（✕）と一括クリア | ✅ |
| トークン概算とメーター（🟢/🟡/🔴） | ✅ `TokenMeter.tsx` / `tokenCount.ts` |
| 取り込み中のローディング表示 | ✅ 抽出中 / 保存中 の 2 段階 |

### Step 4 の実装内容

| 項目 | 状態 |
| --- | --- |
| 20項目の評価基準（1 ファイルで差し替え可能） | ✅ `src/lib/prompts/criteria.ts` |
| システムプロンプト（出力フォーマット厳守・幻覚対策） | ✅ `systemPrompt.ts` |
| 統合プロンプトのパッキングと予算配分 | ✅ `buildPrompt.ts` |
| SEC 本文の節優先抽出（Risk Factors / MD&A 等） | ✅ |
| トークン上限超過時の自動切り詰め + 最終検算 | ✅ `enforceLimit` |
| Markdown テーブルのパース（途中経過にも対応） | ✅ `parseAnalysis.ts` |
| 分析実行のオーケストレーション | ✅ `analysisRunner.ts` |
| ストリーミング逐次描画（評価カード） | ✅ `AnalysisPanel.tsx` |
| 中断ボタン（途中結果を保持） | ✅ Rust `llm_cancel` |
| PPTX 対応 / ✏️ リネーム / ドロップゾーン注記 | ✅ Step 3 の微修正 |

### Step 5 の実装内容

| 項目 | 状態 |
| --- | --- |
| 3 ペイン構成（左: 市場データ / 右: 分析結果 / 下: 対話） | ✅ Step 5.1 で再構築 |
| ドラッグでのリサイズ（縦横どちらも） | ✅ `ResizableSplit.tsx` |
| 各ペインの最小化（`_` ボタン） | ✅ `PanelHeader.tsx` |
| 文字サイズ 3 段階（A− / A+、localStorage 保存） | ✅ `textScale.ts` |
| 評価テーブルの列幅・行間・スコアドットの拡大 | ✅ |
| 生成完了と同時に SQLite へ自動保存 | ✅ `analyses.rs` |
| 銘柄タブを開いたときの自動復元 | ✅ |
| 「クリア」ボタン（明示操作時のみ削除） | ✅ |

---

## 次にやること（TODO）

### Phase 3 Step 2 以降
- [ ] 複数銘柄の横断比較ビュー（検討中銘柄をまとめて評価する）
- [ ] 対話パネルから分析結果を踏まえた追加質問ができるようにする
- [ ] 分析結果の書き出し（Markdown / PDF）
- [ ] メニューバー各項目に実際の動作を割り当てる
- [ ] 画像のみの PDF（スキャン資料）への対応方針を決める（OCR を入れるか否か）
- [ ] 20項目の評価基準をユーザーが UI 上で編集できるようにする

---

## 作業履歴

### 2026-08-02 — Phase 3 Step 3: データ取得元の切替・ドラッグ可能モーダル・ヘルプ AI ✅
3 つの大型機能を実装した。

1. **データ取得元の選択（利用規約リスクの回避）**
   - `MarketDataProvider` トレイトを新設し、Yahoo / FMP / Alpha Vantage を差し替え可能にした。
     **どれを選んでも同じ `Fundamentals` を返す**ので UI 側は取得元を意識しない
   - 設定モーダルに「データ取得元」タブを追加。3 つをラジオで選び、
     それぞれに指定どおりの注記（非公式/推奨/無料枠）を必ず併記する
   - FMP / Alpha Vantage はキー入力欄と「接続テスト」を表示。
     キーは `market:<id>` として LLM のキーと同じ仕組みで保存する（生の値はフロントに出ない）
   - **キー未設定のときは通信する前に理由つきで止める**（`status_of` で判定）
   - 四半期推移は SEC XBRL との突き合わせが要るため Yahoo + SEC 固定。UI にも明記した
2. **モーダルの操作性**
   - `ModalShell` を新設し、設定 / 役割ライブラリ / 銘柄インポート / 資料プレビューを統一
   - **ヘッダーをドラッグして自由な位置へ動かせる。** 位置は「中央からのずれ」で持つので、
     ウィンドウをリサイズしても画面外に取り残されない（`modalDrag.ts` + `ResizeObserver` 相当）
   - **背景クリックでは閉じない。** 入力途中の内容が消える事故を防ぐ。
     閉じるのは ✕ / キャンセル / Esc だけ
3. **ヘルプ AI アシスタント**
   - 最下部ステータスバー右側（バージョン表記の左）に「? ヘルプ」ボタンを新設
   - 押すと右下にドロワーが立ち上がる。銘柄分析の対話とは履歴を分ける（保存もしない）
   - ナレッジは `helpKnowledge.ts` が組み立てる。画面の見方・使い方の流れ・分析のコツ・
     APIキーの設定手順・取得元の違いに加え、**いま実際に割り当てられているショートカット**を
     埋め込む。既定値を書き込むと、変更後に誤案内してしまうため
   - 投資助言はせず操作案内に徹する、無い機能をでっち上げない、という制約も明示
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 164 件 / Rust 87 件）

### 2026-08-01 — Phase 3 Step 2: UI/UX 仕上げ・コンプライアンス表現・ショートカット基盤 ✅
7 件の改善を一括で実装した。

1. **既定役割のコンプライアンス表現**
   「テンバガー発掘アナリスト」→「**高成長・グロース株アナリスト**」に改称し、
   本文も「売上・利益成長率や競争優位性を分析する」客観的な表現へ。
   **すでに配布済みの DB に入っている行も `migrate` で差し替える**。
   ユーザーが自分で作った役割（`builtin = 0`）には触れない
2. **確認ダイアログの刷新** — `.ui-fixed` で文字サイズを固定し、
   アイコン付きヘッダー・角丸 `rounded-xl`・`backdrop-blur`・
   ホバー/フォーカスリングを追加。本文の可変フォントに引きずられて崩れなくなった
3. **チャット入力欄** — 入力量に応じて自動で伸び（上限 `max-h-64` でスクロール）、
   `resize-y` で手動調整もできる
4. **「最小化中」の視認性** — 黄色背景（`bg-amber-500`）＋白の太字＋リング。
   パネルが隠れていることを見落とさない
5. **検討中銘柄の小窓**
   - 上の履歴との境界線をドラッグして高さを変更（`clampFirstSize` で丸め、localStorage に保存）
   - ヘッダークリック / ⌄ ボタンで折りたたみ
   - `overflow-auto` ＋ `w-max` で縦横スクロール。✕ は `sticky right-0` で常に押せる
   - **ティッカーの改行羅列（`AAPL⏎NVDA`）だけでも取り込めるようパースを強化**。
     カンマ・読点・タブ区切りにも対応
6. **Ctrl+Enter で送信 / Enter で改行**（mac は ⌘+Enter）。
   長文プロンプトの途中で誤送信しないための割り当て。入力欄の下に常時表示する
7. **ショートカット基盤**
   - `shortcuts` テーブルを新設。**既定値は保存せず、変更した割り当てだけを持つ**
     （既定を両方に置くと、既定を変えたときに古い値が残るため）
   - アクション一覧と既定キーは `src/lib/ui/shortcutStore.ts`
   - キーの正規化・照合・衝突検出は `shortcutKeys.ts`（純粋関数、テスト 21 件）
   - 設定モーダルに「ショートカット」タブを追加。押したキーをそのまま割り当てられる
   - 入力欄にフォーカスがあるときは文字入力を優先し、`allowInInput` のものだけ発火する
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 135 件 / Rust 65 件）

### 2026-08-01 — Phase 3 Step 1: 検討中銘柄・アーカイブ・役割ライブラリ ✅
4 件の特注要件を一括で実装した。

1. **検討中銘柄（ミニウィンドウ ＋ インポート）**
   - `library.db` を新設し `candidate_stocks` を作成。**既存の `analyses.db` /
     `chats.db` には触れない**ので、新テーブルを増やしても投資データが壊れない
   - パースは `src/lib/candidates/parseCandidates.ts`（純粋関数）。
     全角パイプ `｜`・全角スペース・CRLF・`#` メモ行・重複（後勝ち）を吸収し、
     **取り込めなかった行は捨てずに行番号つきで返す**。
     黙って落とすと「貼ったのに入っていない」に気づけないため
   - モーダルは保存前に解析結果（取り込む銘柄 / 重複 / エラー）を必ず見せる
   - 小窓は 1 行 1 銘柄。件数が増えたら縦、社名が長ければ横にスクロールする
   - 削除は **✕ ボタン**と**右クリックメニュー**の 2 系統（どちらも `removeCandidate`）
   - クリックすると上部のティッカー入力欄にセットし、そのまま分析を開始する
     （同じ銘柄を続けて選べるよう `preset` に連番を添えている）
2. **チャット履歴の独立スクロール ＋ アーカイブ**
   - 履歴 `<nav>` に `min-h-0` + `overflow-y-auto`。
     **`min-h-0` が無いと中身の高さを主張して「検討中銘柄」を押し出す**
   - `chat_sessions` に `is_archived` を `ALTER TABLE` で追加。
     既存行は既定値 0 になるため、**過去の会話は 1 件も失われない**（テスト済み）
   - サイドバー右上の 📁 で「履歴 / アーカイブ」を切り替え。各行の 📁 で移動・復元
   - 削除ダイアログに「残すならアーカイブを」の案内を追加
3. **AI の役割ライブラリ**
   - `prompts` テーブルを新設し、既定 3 種（プロの株式ジャーナリスト /
     テンバガー発掘アナリスト / 慎重な高配当アナリスト）を**初回だけ**投入。
     投入済みの印を `prompt_seed` に残すので、**削除した既定役割が再起動で復活しない**
   - 対話パネルのヘッダーからワンタップで切替（`PromptLibraryMenu`）。
     選択中の役割は端末ごとの好みなので localStorage に保持する
   - 追加・編集・削除は `PromptLibraryModal` から
4. **自動テストとデータ保護**
   - パース 18 件（正常系 / エラー検知 / 重複）、削除ストア 7 件
   - Rust: 旧スキーマ（`is_archived` 無し）の DB を再現し、
     **移行後も会話・メッセージ・ティッカーが残ること**を検証。
     マイグレーションを 3 回流してもアーカイブ状態が初期化されないことも確認
   - `candidate_stocks` と `prompts` が同じ DB に同居しても互いを壊さないことも検証
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 101 件 / Rust 56 件）

### 2026-08-01 — Phase 2 Step 5.6: メニューバー移設・折りたたみの余白ゼロ化・パネル移動の根本改修 ✅
スクリーンショットで判明した 5 件を一括で実装した。

1. **フォント調整 UI を最上位メニューバーへ完全移設**
   - `CommandBar` から撤去し、`MenuBar` の右端（ファイル / 表示 / 分析 / ヘルプ の並びの右）に配置
   - **`MenuBar` 自体を `.ui-fixed` ＋ 高さ `h-9` 固定にした。**
     ここに調整 UI を置く以上、バーの高さが変わるとスライダーがカーソルの下から動いてしまうため。
     結果として最上部バーだけは文字サイズ連動から外れる（本文・パネル・サイドバーは従来どおり連動）
2. **設定モーダルの「未設定」縦割れを修復**
   - `flex` の子は既定で縮むため、ラベル側に `min-w-0 truncate`、
     状態表示側に `shrink-0 whitespace-nowrap` を明示的に振り分けた
3. **折りたたみ時のデッドスペースを完全に排除**
   - **畳んだ枠は分割そのものから外す**方式に変更。`ResizableSplit` の `collapsed` を廃止し、
     `App.renderWorkspace()` が表示中の枠だけで分割を組み立てる。
     1 枚だけになったら分割せずそのまま 100% を占有する
   - 畳んだパネルは `PanelRestoreBar`（メニューバー右端の小さな復元ボタン）へ退避
   - 最後の 1 枚は畳めないようにした（画面が空になるため）
4. **パネル移動を Pointer Events で根本改修**
   - HTML5 DnD をやめ、`src/lib/ui/panelDrag.ts` を新設。
     `setPointerCapture` でポインタを捕捉し、`document.elementFromPoint` ＋
     `data-panel-slot` 属性でドロップ先を判定する
   - **`draggable` との併用は不可能**。ネイティブドラッグが pointermove を奪うため、
     同じ要素で両立させると必ずどちらかが死ぬ。Pointer Events に一本化した
   - ドロップ先は各パネル全体（ヘッダーだけでなく本文も可）。Esc で中断できる
5. **APIキー削除に確認ダイアログ**（「登録されているAPIキーを削除しますか？」/ [キャンセル] [削除する]）
- **検証**: `npm run verify` ✅ / `cargo test --lib` ✅（フロント 76 件 / Rust 35 件）
  パネル移動は状態遷移として 10 件のテストで検証（入れ替え / 自枠 / 枠外 / 中断 / 二重終了）

---

**これより古い履歴は [`docs/archive/PROGRESS_履歴_2026-08.md`](docs/archive/PROGRESS_履歴_2026-08.md) に繰り下げ。**
