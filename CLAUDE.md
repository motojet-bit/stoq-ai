# CLAUDE.md — 開発・コード記述ルール

このリポジトリで作業する際、AI アシスタントは以下のルールに必ず従うこと。

---

## 1. 作業開始時

- **必ず `PROGRESS.md` を読み込む**こと。現在のステータスと TODO を確認してから指示に取りかかる。

## 2. 変更後・作業終了時

- **`PROGRESS.md` を必ず更新する**（「現在のステータス」と「作業履歴」）。
  - 何を変更したか
  - 次に何をするか
- `改修記録.md` の先頭に、その改修の記録を追記する（書式はファイル冒頭を参照）。
- Git にコミットし、コミットハッシュを `改修記録.md` に記入する。
- **コミット前に `npm run verify`（typecheck → test → build）と
  `cargo test --manifest-path src-tauri\Cargo.toml --lib` を通すこと。**
  純粋なロジックを足したらテストも足す（UI から切り離して書けるようにする）。

## 2.1 ドキュメントのローテーション

md が長くなりすぎたら**繰り下げて新しいファイルに切り替える**こと。目安と手順:

| ファイル | 目安 | 繰り下げ方 |
| --- | --- | --- |
| `改修記録.md` | 400 行 / 40KB | 古い分を `docs/archive/改修記録_YYYY-MM.md` へ移し、本体には直近のみ残す |
| `PROGRESS.md` | 300 行 | 「作業履歴」の古い分を `docs/archive/作業履歴_YYYY-MM.md` へ移す |
| `docs/設計.md` | 500 行 | フェーズ単位で `docs/設計_Phase{N}.md` に分割する |

繰り下げたら、元のファイルの冒頭に**アーカイブへのリンクを必ず残す**こと。
「現在のステータス」「次にやること」など、常に読む必要がある部分は本体に残す。

## 3. コード記述ルール

### 3.1 1 機能 1 ファイル

- **1 つの機能・1 つのコンポーネントにつき 1 ファイル**とする。1 ファイルに複数の機能を詰め込まない。
- React コンポーネントは `src/components/` に 1 ファイル 1 コンポーネントで置き、`export default` する。
- 純粋なロジック・ユーティリティは `src/lib/` に、機能ごとに 1 ファイルで切り出す。
- 型定義は `src/types/` に集約する。
- Rust 側も同様に、機能単位でモジュールを分割する（`src-tauri/src/`）。
- ファイルが 200 行を超えそうなら分割を検討する。

### 3.2 命名・スタイル

- ファイル名: React コンポーネントは `PascalCase.tsx`、それ以外は `camelCase.ts`。
- import は `@/` エイリアス（`src/` を指す）を使う。相対パスの `../../` を連ねない。
- TypeScript は `strict` 前提。`any` を使わない。
- スタイリングは Tailwind CSS のユーティリティクラスで行う。独自 CSS は `src/styles.css` の全体設定に限る。
- コメント・UI 文言は日本語。識別子は英語。

### 3.3 やらないこと

- APIキーなどの秘密情報をコードにハードコードしない。
- **秘密情報をフロントエンド（WebView）に渡さない。** APIキーは Rust 側（`src-tauri/src/settings.rs`）で
  OS のアプリ設定ディレクトリに保存し、フロントへはマスク済み文字列と `configured` フラグのみ返す。
- マスク処理は `src-tauri/src/settings.rs` の `mask_secret` を使う。
- 外部 API への HTTP は必ず Rust 側から発行する（CORS 回避・User-Agent 指定・キー秘匿のため）。
  理由の詳細は `docs/設計.md` の「4.1 なぜ HTTP を Rust 側に置くのか」を参照。

## 4. Git

- ブランチ: `main`
- 区切りごとに随時コミットする。まとめて放置しない。
- コミットメッセージは `種別: 概要` 形式（例: `feat: SEC EDGAR クライアントを追加`）。日本語可。
- 秘密情報（API キー、`.env`）は絶対にコミットしない。

## 5. 技術スタック

| 層 | 採用技術 |
| --- | --- |
| デスクトップシェル | Tauri 2.0（Rust） |
| フロントエンド | React 19 + TypeScript |
| ビルド | Vite |
| スタイル | Tailwind CSS v4 |
| LLM | OpenAI / Claude / Gemini（切替可能にする） |
| データ源 | SEC EDGAR API / Yahoo Finance / ユーザー投入 PDF |

## 6. プロジェクト概要

米国株＆グローバル株の AI ファンダメンタル分析デスクトップアプリ。
超軽量であることを重視する。詳細は `README.md` / `docs/設計.md` を参照。

**レイアウト方針**: Chatbox 風の会話履歴サイドバー + Cursor 風マルチタブ
＋ 最上部メニューバー ＋ 直下のティッカー / PDF ドロップゾーン。

## 7. 言語

- ユーザーとのやりとり・ドキュメント・コミットメッセージ: **日本語**
