# CLAUDE.md — 作業ルール

このリポジトリで作業する際、AI アシスタントは以下のルールに必ず従うこと。

## 1. 作業開始時

- **必ず `PROGRESS.md` を読み込む**。現在のステータスと TODO を確認してから指示に取りかかる。

## 2. 作業終了・区切り時

- `PROGRESS.md` の「現在のステータス」と「作業履歴」を**自動で**更新する。
  - 何を変更したか
  - 次に何をするか
- `改修記録.md` の先頭に、その改修の記録を追記する（書式はファイル冒頭を参照）。
- Git に**コミットする**。コミット後、`改修記録.md` の該当項目にコミットハッシュを記入する。

## 3. Git

- ブランチ: `main`
- 区切りごとに随時コミットする。まとめて放置しない。
- コミットメッセージは日本語可。`種別: 概要` の形式を推奨（例: `feat: SEC EDGAR クライアントを追加`）。
- 秘密情報（API キー、`.env`）は絶対にコミットしない。

## 4. プロジェクト概要

米国株スクリーナー。詳細は `README.md` および `docs/設計.md` を参照。

- データ源: Yahoo Finance API / SEC (EDGAR) API / ユーザー投入の一次資料
- LLM が企業の将来性を評価する
- **デスクトップを先に開発**し、その後モバイル

## 5. 言語

- ユーザーとのやりとり・ドキュメント・コミットメッセージ: **日本語**
- コード内の識別子: 英語
